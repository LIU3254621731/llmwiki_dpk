你是一名资深 Tauri + React + Rust 桌面软件工程师。当前项目已经完成工程框架搭建和 UI 页面实现，现在请在现有代码基础上实现核心功能。不要重构 UI 风格，不要重新设计页面，不要加入已否决功能。请先阅读项目结构、AGENTS.md、README、src、src-tauri，再按现有代码风格实现。

一、产品核心定义

本软件是一个本地优先的 LLM 驱动 Wiki 维护工作台，不是普通 RAG，不是 OCR 软件，不是本地 PDF 深度解析工具，不是多模态图片理解系统，也不是自由 Agent 群聊。

核心闭环：

用户上传文档
→ 本地保存 Raw Source
→ 生成 source_id、file_hash
→ Prompt + 文档附件提交给 DeepSeek
→ DeepSeek 返回严格 JSON
→ 本地 JSON Schema 校验
→ Candidate Search / Resolution 去重消歧
→ Relationship 建立实体概念关系
→ Wiki Update Plan
→ Review Diff
→ 用户确认或低风险自动应用
→ WikiWriter 写入 Markdown Wiki
→ VersionManager 快照
→ index.md / log.md 更新
→ 用户基于 Wiki 问答
→ 高价值回答沉淀为 Wiki 页面

请牢记：
交互是入口；本地多 Agent Runtime 是流程；DeepSeek 是能力；Markdown Wiki 是长期资产。

二、必须排除的内容

不要实现：
1. 本地 PDF 深度解析。
2. OCR。
3. 多模态图片理解。
4. 图片 caption。
5. PDF 页面切片。
6. 表格视觉识别。
7. 公式 OCR。
8. Tesseract、PaddleOCR、OpenCV OCR、LayoutLM、Nougat、MinerU、Marker 等依赖。
9. 传统向量 RAG 作为核心。
10. 自由 Agent 群聊。
11. LLM 直接修改 Wiki 文件。
12. Agent 直接写 Markdown。
13. 云同步、Web 端、移动端、多人协作。

文档类文件只作为附件提交给 DeepSeek；图片类文件只保存为 asset，不参与知识抽取。

三、当前任务目标

请在现有 UI 和框架中实现以下核心功能：

1. Workspace 本地知识库目录初始化。
2. SQLite 数据表和 migration。
3. DeepSeek 模型配置、连接测试、JSON 输出测试、附件能力测试。
4. Source 文件上传、hash、source_id、保存。
5. 图片 asset 保存但不抽取。
6. 本地 TaskQueue、任务状态机、task_events。
7. Agent Runtime 和 Coordinator 调度。
8. SourceIngestAgent 调用 DeepSeek 附件分析。
9. JSON Schema 校验与 JSON Repair 流程。
10. CandidateSearchEngine。
11. ResolutionAgent 去重消歧。
12. RelationshipAgent 标准化关系。
13. WikiUpdateAgent 生成更新计划。
14. ReviewEngine / DiffEngine 生成审阅项。
15. WikiWriter 统一写入 Markdown。
16. VersionManager 页面快照和回滚。
17. IndexService / LogService 更新 index.md 和 log.md。
18. QueryAgent 基于 Wiki 问答。
19. 回答保存为 Wiki 页面。
20. Recovery Check 和 Workspace Reconcile。
21. Token usage 或 usage 估算。
22. 错误日志与 API Key 脱敏。

四、正式架构要求

React 前端只负责展示和交互。核心功能放到 Rust Core Runtime。

Rust Core 需要实现或补齐以下模块，名称可按现有项目调整：

AppKernel
ConfigService
SecretService
WorkspaceService
DatabaseService
TaskQueue
EventBus
AgentRuntime
CoordinatorAgent
SourceIngestAgent
ResolutionAgent
RelationshipAgent
WikiUpdateAgent
ReviewAgent
QueryAgent
HealthCheckAgent
PromptRegistry
PromptBuilder
ModelGateway
DeepSeekClient
JsonSchemaValidator
CandidateSearchEngine
ResolutionEngine
RelationshipEngine
ReviewEngine
DiffEngine
WikiWriter
VersionManager
IndexService
LogService
GraphService
SearchService
RecoveryCheck
WorkspaceReconcile

所有模型调用必须经过 ModelGateway。
所有文件写入必须经过 WikiWriter。
所有版本快照必须经过 VersionManager。
所有 Agent 输入输出必须保存到 .runtime/tasks/{task_id}/。
所有任务状态必须持久化到 SQLite。
前端通过 Tauri commands 和 events 获取任务状态、Agent Activity Timeline、审阅项、Wiki 页面内容。

五、Workspace 目录结构

每个知识库目录如下：

workspace/
  raw/
    sources/
      documents/
      webclips/
    assets/
      images/
      attachments/
  wiki/
    index.md
    log.md
    overview.md
    sources/
    concepts/
    entities/
    topics/
    questions/
    reviews/
  schema/
    AGENTS.md
    WIKI_SCHEMA.md
    PROMPTS.md
    TEMPLATE.yaml
  drafts/
    ingest/
    wiki_updates/
  versions/
    snapshots/
    pages/
  .runtime/
    tasks/
    logs/
  db/
    app.sqlite
  config/
    kb.config.json

raw 是原始事实来源，不允许 AI 修改。wiki 是长期知识资产。schema 保存模板和规则。.runtime 保存任务中间产物。

六、SQLite 表

请创建或补齐这些表：

knowledge_bases(id,name,path,template_name,created_at,updated_at)
sources(id,kb_id,file_name,file_path,file_type,file_size,file_hash,status,created_at,updated_at)
assets(id,kb_id,file_name,file_path,file_type,file_size,file_hash,created_at)
wiki_pages(id,kb_id,title,path,page_type,canonical_name,tags,content_hash,created_at,updated_at)
knowledge_items(id,kb_id,canonical_name,item_type,page_path,summary,created_at,updated_at)
aliases(id,item_id,alias,normalized_alias,language,created_at)
relationships(id,kb_id,source_item_id,target_item_id,relation,evidence_source_id,evidence_location,confidence,status,created_at)
tasks(id,kb_id,task_type,status,current_agent,model_profile_id,input_ref,output_ref,error_message,retry_count,created_at,updated_at,locked_at,completed_at)
task_events(id,task_id,event_type,agent_name,message,created_at)
reviews(id,kb_id,task_id,status,summary,risk_level,created_at,updated_at)
review_items(id,review_id,operation,target_path,base_version_hash,old_content,new_content,status,risk_level,created_at,updated_at)
versions(id,kb_id,page_path,content_hash,snapshot_path,task_id,operation_id,created_at)
operations(id,kb_id,task_id,operation_hash,target_path,status,applied_at)
graph_nodes(id,kb_id,node_type,label,path,metadata)
graph_edges(id,kb_id,source_node_id,target_node_id,edge_type,metadata)
model_profiles(id,provider,name,base_url,model_name,encrypted_api_key_ref,role,temperature,max_tokens,timeout,retry_count,created_at,updated_at)
schema_migrations(version,applied_at)

必须支持 migration，迁移前备份数据库。

七、任务状态机

所有后台任务必须使用持久化状态机：

created
queued
locked
prompt_built
sent_to_model
model_returned
json_validating
json_valid
json_invalid
json_repairing
json_repaired
candidate_searching
resolution_running
relationship_running
update_plan_generating
review_generating
review_pending
applying
applied
failed
cancelled
interrupted

每个任务目录：

.runtime/tasks/{task_id}/
  input.json
  prompt.md
  model_raw_response.txt
  ingest_result.json
  resolution_result.json
  relationship_result.json
  update_plan.json
  review_items.json
  error.log

软件启动时执行 Recovery Check，恢复 queued / interrupted / review_pending / applying 状态任务，允许用户继续、重试、取消或回滚。

八、DeepSeek 与 ModelGateway

设置页需要能保存 DeepSeek 配置：

base_url
api_key
chat_model
reasoner_model
temperature
max_tokens
timeout
retry_count
stream

API Key 必须使用系统凭据存储，不能写入配置、数据库、日志、Prompt Snapshot。

ModelGateway 负责：
API Key 注入、模型选择、附件上传、超时、重试、指数退避、限流处理、错误归一化、流式输出、原始响应保存、JSON 修复、usage 统计或估算。

必须处理：
timeout、429、5xx、余额不足、API Key 无效、附件过大、上下文过长、模型不可用、网络断开、JSON 非法、文件上传失败。

错误提示必须具体可读，不要只显示“请求失败”。

九、文件处理

文档文件：
pdf、docx、md、txt、html

处理：
保存到 raw/sources/documents/
生成 source_id
计算 SHA256 hash
创建 Source Ingest Task
作为附件提交给 DeepSeek

图片文件：
png、jpg、jpeg、webp、gif

处理：
保存到 raw/assets/images/
生成 asset_id
不 OCR
不视觉理解
不参与知识抽取
UI 明确提示“图片仅保存为资产，不参与 AI 知识抽取”。

文件上传前检查：文件存在、可读、大小限制、扩展名、是否重复导入、是否被占用、中文路径/空格路径兼容。

十、Agent 流程

用户上传文档后执行：

CoordinatorAgent
→ SourceIngestAgent
→ CandidateSearchEngine
→ ResolutionAgent
→ RelationshipAgent
→ WikiUpdateAgent
→ ReviewAgent
→ Review Center
→ WikiWriter
→ VersionManager
→ IndexService / LogService

Agent 不直接互相调用，只返回结构化 output 和 next_actions，由 Coordinator 调度。

Agent 不直接写文件。

十一、JSON 输出要求

所有 LLM 输出必须严格 JSON。非法 JSON 流程：

1. 严格 parse。
2. 提取 JSON block。
3. 调用 JSON Repair 一次。
4. 仍失败则 task failed，保存 raw output，允许用户重试。

SourceIngestAgent 需要返回：
source_summary、coverage_report、entities、concepts、topics、claims、relationships、proposed_wiki_updates、conflicts、questions_for_user。

coverage_report 必须包含：
document_sections_seen
possibly_missing_sections
confidence_in_coverage
notes

claims 必须包含：
claim
confidence
source_id
location
citation_status

citation_status：
model_reported、user_verified、verified、missing

因为系统不做本地 PDF 深度解析，默认引用位置多为 model_reported，UI 可显示“来源位置由模型报告，未经过本地解析验证”。

十二、Prompt 核心规则

Prompt 必须定义：

Source：用户上传原始资料，事实来源，不允许修改。
Entity：明确指代对象，如人物、组织、论文、项目、产品、数据集、方法、软件库。
Concept：抽象知识点、理论、机制、原则、术语、方法类别。
Topic：更高层主题集合。
Claim：可验证事实性陈述，需要来源。
Citation：来源位置，默认 model_reported。
Conflict：新 claim 与已有 Wiki 不一致或有张力。
Wiki Page：要创建或更新的 Markdown 页面。
Wiki Update Plan：页面创建、修改、合并或跳过计划。
Alias：等价名称、翻译、缩写、大小写变体。相关概念、上下位概念、实现方式、同主题概念不能作为 alias。
Relationship：标准化关系，必须使用 relation enum。

Prompt 要求模型：
不要在未检查 existing_candidates 前新建页面。
不要把相关概念当 alias。
不要把子概念当 alias。
没有 evidence 的关系不要输出为可写入关系。
优先返回 JSON，不要解释性文字。

十三、去重消歧

创建页面前必须执行 Candidate Search 和 Resolution。

候选依据：
title、canonical_name、aliases、normalized_name、slug、page_type、source metadata、index.md 摘要、SQLite knowledge_items、字符串相似度。

Resolution 决策：
create_new
update_existing
append_to_existing
add_alias
merge_suggestion
skip
needs_user_review

Alias 规则：
完全同义可自动添加。
翻译同义可低风险应用。
缩写同义需要确认。
相近概念不能作为 alias，只能 related_to。
上下位概念不能作为 alias，只能 is_a / part_of。
无法判断进入审阅中心。
merge、delete、overwrite 必须审阅。

十四、关系图谱

RelationshipAgent 输出关系必须使用固定 enum：

is_a
part_of
uses
depends_on
improves
compares_with
contradicts
cites
mentions
related_to
has_alias
belongs_to_topic
evaluated_on
proposed_by
applies_to
derived_from

写入规则：
source/target 必须映射到 canonical Wiki page。
没有 evidence 不写入。
低置信度进入审阅。
每次导入高置信关系最多 20 条，related_to 最多 5 条，避免图谱噪声。
关系写入 graph_edges，同时更新 Markdown Related Pages。

十五、Wiki 页面模板

不要让模型自由生成最终 Markdown。模型提供结构化内容块，本地模板渲染页面。

页面 frontmatter 示例：

---
title: Self-Attention
type: concept
canonical_name: Self-Attention
aliases:
  - self attention
  - 自注意力
sources:
  - src_xxx
tags:
  - attention
confidence: medium
status: active
created:
updated:
last_updated_by_task:
---

稳定 section：

# Title

## Summary <!-- section:summary -->

## Definition <!-- section:definition -->

## Key Points <!-- section:key_points -->

## Evidence <!-- section:evidence -->

## Related Pages <!-- section:related_pages -->

## Source Contributions <!-- section:source_contributions -->

## Open Questions <!-- section:open_questions -->

WikiWriter patch 时使用 section id，避免标题变化导致 patch 失败。

十六、审阅与风险

维护模式：
Strict：所有修改审阅。
Balanced：默认。低风险可自动；已有正文修改、冲突、合并需审阅。
Auto：低中风险自动，高风险必须审阅，全部可回滚。

低风险：
新增 source summary、添加 alias、related link、追加 log、新建无冲突页面、标签。

中风险：
修改已有 section、调整结构、添加上下位关系、保存问答为页面。

高风险：
合并页面、删除页面、覆盖核心定义、修改 canonical_name、标记 contradiction、重命名页面、覆盖旧结论。

审阅中心必须支持：
按任务/页面/风险分组，高风险置顶，低风险折叠，一键接受低风险，逐条接受，全部拒绝，编辑后接受，重新生成，查看 Prompt、模型原始输出、来源、版本快照、回滚。

每个修改必须说明：
改了什么、为什么改、基于哪个 source、能否撤销。

十七、WikiWriter 与版本安全

所有写入必须通过 WikiWriter：

1. 获取页面锁。
2. 读取当前页面。
3. 检查 base_version_hash。
4. 创建版本快照。
5. 校验 update_plan。
6. 生成目标 Markdown。
7. 写临时文件。
8. 原子 rename 替换正式文件。
9. 更新 SQLite。
10. 更新 index.md。
11. 追加 log.md。
12. 释放锁。

每个 operation 必须有 operation_id、operation_hash、target_path、source_task_id、applied_at。

应用前检查：
operation_hash 是否已应用；
页面是否锁定；
当前页面 hash 是否等于生成 Diff 时的 base_version_hash；
alias 是否已存在；
relationship 是否已存在。

如果页面被用户手动修改，不能直接应用旧 Diff，必须提示重新生成 Diff。

十八、Chat / QueryAgent

Chat 不是软件核心全部，只是入口之一。

支持范围：
整个知识库、当前页面、指定文件、指定标签、指定目录。

QueryAgent：
读取相关 Wiki 页面、Source Summary、aliases、related pages。
构造 Query Prompt。
调用 DeepSeek。
返回带 citations 的 JSON。
显示本次使用上下文。
支持保存回答为 Wiki 页面或追加到当前页面。
保存为 Wiki 页面也必须经过 Review / WikiWriter / VersionManager / index/log。

十九、Search / Health / Reconcile

搜索：
先实现本地全文搜索，覆盖 Wiki 页面、Source Summary、index.md、log.md、标题、标签、frontmatter、aliases。

HealthCheckAgent：
检查重复页面、孤立页面、缺引用、过时内容、冲突、图谱噪声、index/log 不一致，只生成报告，不直接修改。

WorkspaceReconcile：
检查 SQLite、Markdown 文件、index.md、log.md、versions、raw sources 是否一致，生成修复建议。

二十、UI 绑定

不要改现有 UI 风格。把核心功能接入现有页面：

Dashboard：统计知识库、任务、待审阅、最近页面。
Sources：上传文件、查看 source、重新导入。
ImportTasks：展示 Agent Activity Timeline。
Review：展示 Diff、风险、来源、操作。
Wiki：浏览/编辑/预览、右侧上下文。
Chat：问答、保存为 Wiki。
Graph：展示 graph_nodes/graph_edges。
Search：本地搜索。
Settings：DeepSeek、模型测试、知识库、Prompt、Agent、外观。
HealthCheck：健康报告和修复建议。
TaskDetail：查看 input、prompt、raw output、validated output、events、error。

二十一、日志与安全

必须脱敏 API Key。
Prompt Snapshot、日志、错误报告不能包含密钥。
日志包括 app、task、model、error、review、version。
支持用户查看任务日志。
支持导出诊断包，但必须脱敏。

二十二、验收标准

请实现后运行项目检查，并确保：

1. 能启动桌面应用。
2. 能初始化知识库目录。
3. 能安全保存 DeepSeek API Key。
4. 能测试连接和 JSON 输出。
5. 能上传 PDF/DOCX/MD/TXT/HTML。
6. 能保存 source 并计算 hash。
7. 能创建任务并显示事件流。
8. 能调用 DeepSeek 附件分析。
9. 能校验 JSON，失败可 repair。
10. 能生成 source summary。
11. 能执行候选检索和消歧。
12. 能建立标准化关系。
13. 能生成 review diff。
14. 能接受审阅并写入 Markdown。
15. 能更新 index.md 和 log.md。
16. 能保存版本快照并回滚。
17. 能基于 Wiki 问答。
18. 能保存回答为 Wiki 页面。
19. 图片只保存 asset，不触发 OCR/识别。
20. 关闭重开可恢复任务。
21. 不会重复应用同一 operation。
22. 不会覆盖用户手动编辑。
23. 所有核心错误有明确提示。
24. API Key 不明文落盘。
25. 不改变既有 UI 风格。

请按现有项目结构实现，不要一次性删除重写。实现后请补充必要 README、开发说明、数据库迁移说明、核心模块说明，并尽量运行格式化、类型检查、构建测试。