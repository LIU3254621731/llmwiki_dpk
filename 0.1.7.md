你是一名资深 Tauri + React + Rust 桌面软件工程师，同时具备本地知识库系统、LLM Agent Pipeline、Markdown Wiki、知识图谱和桌面端 UX 经验。

当前项目是“智维 Wiki - LLM 知识维护工作台”，版本已进入 v0.2.0-dev。系统已经可以上传文档、调用 LLM、生成 Review Items、写入部分 Wiki 页面、展示 Source、展示文件树、展示任务、展示基础图谱统计。但是当前仍存在几个严重影响可用性的核心问题：

1. 审阅中心状态机不可靠：用户点击确认/接受后，系统可能返回“跳过”或状态不一致。
2. 任务取消机制不可靠：用户取消任务后，后台仍可能继续生成结果、写入中间文件、生成审阅或影响 Dashboard。
3. 知识去重和消歧能力不足：同一个实体/概念被反复创建，例如“刘汶林”在 Wiki 页面列表中重复出现大量条目。
4. 知识图谱功能仍不可用：已有 62 个节点、0 条边，但画布空白，用户无法看到任何可交互图谱。

本轮开发目标是 v0.2.1-dev：集中修复“审阅可靠性、任务可控性、知识去重消歧、思维导图图谱可视化”。不要继续泛化新增功能，不要做多视图图谱，不要做复杂图谱算法，不要做云同步、OCR、多模态图片理解、本地 PDF 深度解析。当前阶段只要求软件真正可用、行为可信、结果可解释。

============================================================
一、P0：重构审阅中心状态机，保证“点击什么就执行什么”
============================================================

当前问题：
审阅中心中用户点击绿色确认/接受按钮后，系统可能返回“跳过”或状态变成 skipped，导致用户无法信任审阅系统。

请彻底检查并重构 Review Item 的前端按钮、后端 apply 函数、数据库状态字段、operation_type 之间的映射关系。

必须建立明确状态机。

Review Item 的 operation_type 只能是以下枚举之一：

- create_page
- update_page
- append_section
- add_alias
- add_relation
- merge_suggestion
- skip
- unresolved
- invalid

Review Item 的 status 只能是以下枚举之一：

- pending
- applying
- applied
- rejected
- skipped
- apply_failed
- cancelled
- needs_manual_review

按钮行为必须严格对应：

1. create_page + 点击“接受”
   - 执行 create_page_apply
   - 成功后 status = applied
   - 不允许变成 skipped

2. update_page + 点击“接受”
   - 执行 update_page_apply
   - 成功后 status = applied
   - 如果目标页面不存在，必须报错或提示转为 create_page
   - 不允许静默 skipped

3. append_section + 点击“接受”
   - 执行 append_section_apply
   - 成功后 status = applied

4. add_relation + 点击“接受”
   - 写入 relationships / graph_edges
   - 成功后 status = applied

5. add_alias + 点击“接受”
   - 写入 aliases
   - 成功后 status = applied

6. merge_suggestion
   - 不显示普通“接受”
   - 只显示“人工合并”
   - 点击后进入人工合并面板
   - 不允许自动 applied

7. skip
   - 不显示绿色“接受”
   - 显示“确认跳过”
   - 点击后 status = skipped

8. unresolved / invalid
   - 不显示接受按钮
   - 显示“重新生成”“手动处理”“拒绝”

前端 UI 必须根据 operation_type 动态显示按钮，不允许所有 item 都显示同样的绿色勾和红色叉。

后端 apply_review_item(review_item_id) 必须：

1. 读取 operation_type。
2. 根据 operation_type 分发到不同 handler。
3. 每个 handler 返回明确结果：
   - success
   - failed
   - skipped
   - manual_required
4. 不允许在 apply_review_item 中随意把失败项标记为 skipped。
5. 不允许把 create_page 的结果标记为 skipped。
6. 所有状态变更必须写入 review_item_events 表或 log：
   - old_status
   - new_status
   - action
   - reason
   - timestamp

审阅中心每条 item 必须显示：

- 操作类型中文名；
- 当前状态；
- 是否可应用；
- 不可应用原因；
- 最近一次操作结果；
- 最近一次错误；
- 查看状态日志。

验收标准：

1. 点击 create_page 的“接受”后，只能出现 applied 或 apply_failed，不能出现 skipped。
2. 点击 skip 的“确认跳过”后，才能出现 skipped。
3. 点击 update_page 且目标文件不存在时，必须提示“目标页面不存在，是否转为创建页面？”
4. 批量接受低风险项时，结果中必须列出：
   - 成功 X；
   - 失败 Y；
   - 跳过 Z；
   - 需要人工处理 N。
5. skipped 数量只能来自明确 skip 操作，不能来自普通接受。

============================================================
二、P0：修复任务取消机制，取消后不得继续产生结果
============================================================

当前问题：
用户取消任务后，任务仍然可能继续调用模型、解析结果、生成 review、写入中间文件或影响 Dashboard。这说明取消只改变了 UI 或数据库状态，没有真正中断后台 pipeline。

请实现真正可靠的任务取消机制。

要求：

1. 每个任务必须有 cancellation token 或 cancel flag。
2. 用户点击取消任务后：
   - tasks.status = cancelling；
   - 写入 cancelled_requested_at；
   - 写入 cancel_reason = user_cancelled；
   - 通知正在运行的 Agent；
   - 后台每个阶段都必须检查 cancellation。

3. Pipeline 每个阶段开始前都必须检查：

- prompt_build 前；
- send_to_model 前；
- model_returned 后；
- json_validating 前；
- resolution_running 前；
- relationship_running 前；
- update_plan_generating 前；
- review_generating 前；
- wiki_write 前；
- graph_update 前。

如果发现任务已取消：

- 立即停止；
- 不再进入后续阶段；
- 不生成新的 review items；
- 不写入 wiki_pages；
- 不写入 graph_nodes；
- 不修改 Source 状态为 processed/applied；
- tasks.status = cancelled；
- 写入 timeline：“用户取消，Pipeline 已停止”。

4. 如果模型请求已经发出，无法真正中断 HTTP 请求，则必须在模型返回后再次检查 cancel flag：
   - 如果已取消，丢弃模型返回；
   - 可以保存到 task debug 文件，但不得进入正式 ingest_result/update_plan/review；
   - 不得生成审阅；
   - 不得写入知识库。

5. 取消后的任务状态必须清晰：

- cancelled：用户取消，已停止；
- cancelled_after_model_return：模型已返回但结果已丢弃；
- interrupted：应用异常或关闭导致中断；
- failed：真实错误；
- archived：用户归档。

6. 被取消的任务不应继续影响：

- 首页失败任务数量；
- 待审阅数量；
- Source 状态；
- Health Check 严重错误；
- Graph 统计。

7. 导入任务页面中，已取消任务应显示：

- 用户取消；
- 取消时间；
- 是否产生了可丢弃中间文件；
- 按钮：重新运行、归档、删除中间文件。

8. Source 状态规则：

如果 Source 只有 cancelled 任务，状态应为：

- saved 或 cancelled；
- 不应为 processed；
- 不应生成 review_pending。

验收标准：

1. 上传文档后立刻点击取消，任务必须停止在 cancelled。
2. 取消后不生成新的 Review Items。
3. 取消后 Dashboard 待审数量不增加。
4. 取消后 Source 不显示 processed。
5. 如果模型返回发生在取消之后，系统必须显示“模型返回已丢弃”，不得继续生成审阅。

============================================================
三、P0：修复知识去重与消歧，避免重复页面泛滥
============================================================

当前问题：
Wiki 页面列表中出现大量重复知识点，例如“刘汶林”重复出现很多次，路径类似 liu-wen-lin-xxx.md。这说明系统没有正确识别已有实体/概念，也没有合并重复页面。

本轮必须实现可靠的本地去重与消歧机制。

核心原则：
同一个知识项不能无限重复创建页面。LLM 可以提出候选，但最终必须经过本地规则、相似度匹配和人工审阅共同决定。

请实现 Knowledge Deduplication / Entity Resolution Service。

一、规范化 canonical_name

对每个候选知识项生成 normalized_name：

1. 去除首尾空格。
2. 中文保留原文。
3. 英文转小写。
4. 去除多余空格、下划线、连字符差异。
5. 全角半角统一。
6. 常见括号内容单独作为 alias。
7. rPPG、PPG、MRI 这类缩写保留大小写显示，但 normalized 用小写。
8. 人名不要直接 hash 化为多个页面。

例如：

- “刘汶林”
- “ 刘汶林 ”
- “liu wen lin”
- “liu-wen-lin-63f45...”

必须能被识别为同一候选或强相似候选。

二、创建页面前必须查重

在生成 create_page Review Item 前，必须先查找：

1. wiki_pages.title 完全匹配。
2. wiki_pages.canonical_name 完全匹配。
3. aliases 完全匹配。
4. knowledge_items.normalized_name 完全匹配。
5. slug 去 hash 后匹配。
6. 相似度匹配。

如果找到高度相似页面：

- 不生成 create_page；
- 改为 update_page 或 merge_suggestion；
- UI 显示“疑似已有页面”。

三、相似度规则

至少实现轻量级规则，不要求复杂 embedding：

1. exact match：1.0
2. normalized exact：0.95
3. alias match：0.92
4. slug base match：0.9
5. 中文完全包含：0.85
6. 英文 token set 相似：0.8
7. 编辑距离相似：0.75

阈值：

- >= 0.9：自动认为同一项，生成 update_page。
- 0.75 - 0.9：生成 merge_suggestion，需要人工确认。
- < 0.75：允许 create_page。

四、防止单次任务内重复创建

同一次 ingest_result / update_plan 中，如果出现多个相同 normalized_name：

1. 先在内存中去重；
2. 合并 evidence；
3. 合并 key points；
4. 合并 source references；
5. 只生成一个 Review Item。

五、防止已有重复页面继续扩大

健康检查新增：

“重复页面检测”

检测：

1. title 相同；
2. canonical_name 相同；
3. normalized_name 相同；
4. slug base 相同；
5. aliases 冲突。

对重复项提供：

- 合并页面；
- 保留一个主页面；
- 将其他页面改为 alias / redirect；
- 删除重复页面；
- 重新关联 knowledge_items；
- 重新关联 graph_nodes。

六、Wiki 页面列表优化

当前列表中重复项很多，用户体验很差。请优化：

1. 默认按 canonical_name 分组。
2. 重复页面显示 warning。
3. 同名页面合并显示为一组。
4. 页面副标题显示 type / source / updated_at，而不是重复 hash。
5. 对重复页面提供“合并”按钮。

七、LLM Prompt 层面约束

在 SourceIngestAgent / ResolutionAgent 的 Prompt 中加入明确规则：

1. 不要把同一个人名/概念拆成多个页面。
2. 如果一个实体多次出现，只输出一次。
3. 输出 canonical_name、aliases、type、evidence。
4. 不要把同一实体既作为 person 又作为 concept，除非确有必要。
5. 对不确定项标记 requires_review。

验收标准：

1. 同一个文档中多次出现“刘汶林”，只能生成一个候选知识项。
2. 如果 Wiki 中已有“刘汶林”，再次导入时不创建新页面，而是生成 update_page 或 merge_suggestion。
3. Wiki 页面列表不再出现十几个重复“刘汶林”。
4. 健康检查能检测并引导合并已有重复页面。


============================================================
四、P0：先把知识图谱做成默认可用的思维导图
============================================================

当前问题：
知识图谱页面显示 62 节点、0 边、62 警告，但画布是空的。用户无法理解，也无法操作。当前不要继续做复杂图谱视图，先把默认“思维导图”做成稳定可用。

本阶段图谱目标：
只做一个默认可用视图：思维导图视图。
不要求复杂力导向图，不要求时间图，不要求多维图谱。后续版本再扩展。

核心原则：
只要 graph_nodes > 0，就必须在画布中显示节点。即使 graph_edges = 0，也要用分类树方式展示。

默认布局：

中心节点：
- 当前知识库名称，例如“我的知识库”。

第一层分类节点：
- 概念 Concepts
- 实体 Entities
- 人物 Persons
- 方法 Methods
- 数据集 Datasets
- 来源 Sources
- 问题 Questions
- Wiki 页面 Wiki Pages
- 未分类 Others

第二层：
- 具体知识节点。

如果没有关系边：
- 也必须显示中心节点 → 类型分组 → 具体节点。
- 不要显示空白画布。

如果有关系边：
- 仍然默认显示思维导图；
- 关系边可作为节点之间的辅助连线；
- 但不要破坏分类结构。

节点显示要求：

1. 每个节点显示 title。
2. 节点颜色按 type 区分。
3. 未关联 Wiki 页面节点显示橙色边框。
4. 已关联 Wiki 页面节点显示正常颜色。
5. 低置信度节点显示 warning 图标。
6. 重复节点显示 duplicate 图标。
7. 节点过多时自动折叠分类节点。

交互要求：

1. 点击分类节点：
   - 展开 / 收起。
2. 点击知识节点：
   - 右侧打开详情面板。
3. 双击已关联页面节点：
   - 打开 Wiki 页面。
4. 双击未关联节点：
   - 提示“生成 Wiki 草稿”。
5. 右键节点：
   - 打开 Wiki；
   - 生成 Wiki 草稿；
   - 合并重复项；
   - 设为中心；
   - 在 Chat 中询问；
   - 查看 Source。

右侧详情面板显示：

- 名称；
- 类型；
- canonical_name；
- aliases；
- confidence；
- source；
- page_id；
- page_path；
- related nodes；
- evidence；
- 操作按钮。

顶部统计显示：

- 节点数；
- 边数；
- 孤立节点；
- 未关联页面；
- 重复节点；
- 低置信节点。

空/异常状态要求：

1. 如果 graph_nodes = 0 但 knowledge_items > 0：
   - 显示“已有知识项但尚未生成图谱节点”；
   - 按钮：从知识项重建图谱。

2. 如果 graph_nodes > 0 但 graph_edges = 0：
   - 不要空白；
   - 显示思维导图；
   - 顶部提示“当前关系边为 0，已按类型分类展示知识节点”。

3. 如果节点过多：
   - 默认折叠；
   - 分类节点显示数量。

技术实现建议：

1. 可以使用 SVG / HTML absolute layout / React Flow / 自定义树布局。
2. 当前阶段不要追求复杂动画。
3. 先实现稳定显示。
4. 不允许 graph_nodes > 0 时画布为空。

验收标准：

1. 当前 62 个节点必须在思维导图中可见。
2. 即使 0 条边，也必须显示分类树。
3. 点击节点能看到详情。
4. 已关联 Wiki 页面节点可打开页面。
5. 未关联节点可生成 Wiki 草稿。


============================================================
五、P1：让任务进度真正可理解
============================================================

当前问题：
任务页面只显示生成审阅，缺少清晰进度；用户不知道任务正在干什么、完成了哪些、下一步是什么。

请增强任务详情 UI，但不要做复杂动画。

任务卡片必须显示：

1. 文件名；
2. 当前阶段；
3. 当前 Agent；
4. 状态；
5. 进度阶段；
6. 开始时间；
7. 最近更新时间；
8. 是否可取消；
9. 是否可重试；
10. 下一步操作。

任务详情页必须包含：

一、阶段进度条

固定阶段：

1. 文件保存
2. 文本/Markdown 预览生成
3. Prompt 构建
4. 调用模型
5. 模型返回
6. JSON 校验
7. 消歧去重
8. 关系分析
9. 生成更新计划
10. 生成审阅
11. 等待用户审阅
12. 应用到 Wiki

每个阶段状态：

- pending
- running
- done
- failed
- skipped
- cancelled

二、事件时间线

显示：

- 时间；
- Agent；
- 阶段；
- 信息；
- 错误；
- 是否产生文件。

三、中间文件

现在已经有 prompt、model_raw_response、ingest_result、resolution_result、relationship_result、update_plan。请继续保留，但增加：

- 文件说明；
- 生成阶段；
- 是否用于正式结果；
- 预览按钮。

四、取消/重试

1. running 阶段显示取消。
2. failed 阶段显示重试。
3. cancelled 阶段显示重新运行。
4. review_pending 显示去审阅。
5. applied 显示查看 Wiki 页面。

五、状态一致性

如果任务 cancelled：
- 阶段进度条后续全部 cancelled/skipped。
- 不显示“生成审阅完成”。

如果任务 review_pending：
- 明确显示“流水线完成，等待用户审阅”。

验收标准：

1. 用户打开任务详情即可知道任务执行到哪一步。
2. 取消后不会继续显示后续阶段成功。
3. 任务产生的中间文件能解释用途。


============================================================
六、P1：改进审阅中心对重复项和低质量项的提示
============================================================

审阅中心中如果出现疑似重复页面，必须明确提示用户，而不是让用户盲目接受。

新增 Review Item 标记：

- duplicate_candidate
- merge_candidate
- missing_target
- low_quality_content
- no_evidence
- no_source
- auto_converted_from_update_to_create

UI 显示：

1. 如果是重复候选：
   - 显示“疑似重复”
   - 显示相似页面
   - 提供“合并到已有页面”“仍创建新页面”“拒绝”

2. 如果是从 update 自动转为 create：
   - 显示“原计划更新页面，但目标文件不存在，已转为创建页面”
   - 用户可以接受或重新选择目标页面。

3. 如果内容过短：
   - 显示“内容质量较低”
   - 提供“重新生成”。

4. 如果没有 evidence：
   - 显示“缺少证据来源”
   - 风险等级提高。

验收：
用户能识别重复项，不再批量生成大量低质量重复页面。

============================================================
七、P1：Source 与 Wiki 关联要更明确
============================================================

当前 Source 页面仍然显示 processed 和关联页面 0，容易让用户误解。

请修复 Source 状态和关联显示：

Source 状态规则：

- analyzed：已分析但未生成审阅；
- review_pending：有待审项；
- partially_applied：部分审阅已应用；
- applied：至少一个关联 Wiki 页面且无待审；
- applied_with_pending：已有页面但仍有待审；
- cancelled：任务被用户取消；
- failed：最新任务失败。

Source 详情页：

1. 如果有待审：
   - 显示“该文件已生成 X 个待审修改”
   - 按钮：去审阅。

2. 如果已有 Wiki 页面：
   - 显示关联页面列表。

3. 如果有知识项但没有页面：
   - 显示“已抽取 X 个知识项，但尚未生成 Wiki 页面”
   - 按钮：生成审阅 / 去审阅。

4. 如果任务取消：
   - 显示“分析已取消，未生成知识库内容”
   - 按钮：重新分析。

验收：
Source 的状态必须能准确说明“下一步做什么”。


============================================================
八、统一验收流程
============================================================

完成后请按以下流程自测：

测试 A：审阅可靠性

1. 新建知识库。
2. 上传 DOCX。
3. 等待生成审阅。
4. 打开审阅中心。
5. 确认新页面项显示为“创建页面”。
6. 点击接受。
7. 检查状态必须为 applied，不能是 skipped。
8. Wiki 页面数量增加。
9. Wiki 页面可以打开。

测试 B：取消可靠性

1. 上传 DOCX。
2. 在模型调用阶段点击取消。
3. 等待一段时间。
4. 检查任务状态为 cancelled。
5. 检查未生成新的 Review Items。
6. 检查 Source 不显示 applied/processed。
7. 检查 Dashboard 待审数量不增加。

测试 C：重复项控制

1. 同一个文档重复导入两次。
2. 第二次不应大量创建重复页面。
3. 已有页面应生成 update 或 merge_suggestion。
4. Wiki 页面列表不能出现大量重复“刘汶林”。
5. 健康检查能发现已有重复页面。

测试 D：图谱可用性

1. 当前已有 62 个 graph_nodes。
2. 打开知识图谱。
3. 即使 0 条边，也必须显示中心节点、分类节点、知识节点。
4. 点击节点能打开详情。
5. 已有关联页面节点能打开 Wiki。
6. 不能出现空白画布。

测试 E：任务进度

1. 打开任务详情。
2. 能看到阶段进度条。
3. 能看到每个阶段状态。
4. 取消后后续阶段不再显示成功。
5. 任务中间文件能预览。

============================================================
九、交付输出
============================================================

完成后请输出：

1. 审阅状态机修复说明。
2. Review operation_type 与按钮映射表。
3. 任务取消机制说明。
4. Pipeline cancellation 检查点列表。
5. 知识去重与消歧算法说明。
6. 重复页面检测和合并方案。
7. 思维导图图谱实现说明。
8. 任务进度 UI 实现说明。
9. 修改的文件列表。
10. 数据库 migration。
11. 手动测试结果。
12. 当前仍存在的问题。