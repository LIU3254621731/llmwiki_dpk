你是一名资深 Tauri + React + Rust 桌面软件工程师，同时具备本地知识库产品设计、Markdown Wiki 系统、LLM Agent 工作流和桌面端 UX 经验。

当前项目为“智维 Wiki - LLM 知识维护工作台”。项目定位是：本地优先、LLM 驱动、多 Agent 协作、面向用户维护知识库的桌面软件。它不是普通文件管理器，也不是纯聊天软件，而是一个让用户上传文件、由大模型分析文件、生成审阅建议、用户确认后写入本地 Markdown Wiki，并可搜索、问答和图谱化理解知识的工作台。

当前软件已经有基础 UI、文件上传、Source 管理、任务系统、审阅中心、健康检查、文件树、Source Preview、Markdown 预览、知识图谱入口等功能，但目前整体体验仍然不可用，存在大量“看起来有页面，但实际闭环不通”的问题。本轮开发目标不是继续堆功能，而是做一次 v0.2.0 可用性重构：修复核心闭环、统一状态语义、减少空白页面、让所有页面对用户有明确作用，让软件成为真正可用的本地知识库维护工具。

请不要推倒重写，但允许重构关键模块。请保持现有浅色、蓝白灰、简洁桌面工作台风格，不要做炫酷视觉风格。不要实现 OCR、多模态图片理解、本地 PDF 深度解析、云同步、多人协作、Web 端、移动端。扫描件 PDF 只提示无法处理，绝不做 OCR。

============================================================
一、当前必须正视的问题
============================================================

从当前运行状态看，软件存在以下严重问题：

1. 首页显示 Wiki 页面为 0，但 Source 已处理、待审 16，说明“文档分析结果没有真正变成 Wiki 页面”。
2. 审阅中心显示 16 个待审项，但目标页面文件不存在，Review Item 多为“更新页面”，而不是“创建页面”，导致用户接受时很可能失败。
3. Review Item 显示“AI 修改原因：消歧结果指示更新已有页面”，但实际已有页面不存在，说明 ResolutionAgent / WikiUpdateAgent 的 create/update 决策错误。
4. Source Preview 有 Markdown 预览，但只是原始 DOCX 文本提取结果，不是正式 Wiki 页面，也没有清楚告诉用户“这是原始文件预览，不是知识库页面”。
5. AI 摘要、抽取实体、抽取概念、关联 Wiki 页面为空，说明 Source 分析结果没有被正确写入 Source Preview 的各个 Tab。
6. 健康检查显示“所有检查项通过”，但系统仍然有 Wiki 页面为 0、知识项未关联、图谱无边、待审 16，这说明健康检查判断严重失真。
7. 知识图谱显示 16 节点、0 边，但画布空白，只显示提示，没有真正把节点以思维导图或孤立节点形式渲染出来。
8. 文件管理虽然有 Source 视图和文件树，但文件树、Source Preview、任务中间文件、Wiki 页面之间没有形成清晰跳转。
9. 软件大量页面存在“空状态”，但空状态没有有效下一步操作，用户不知道该做什么。
10. 底部版本号仍显示 v0.1.2，开发阶段和软件实际状态不一致。
11. 首页、审阅中心、健康检查、知识图谱、Source Preview 对同一份数据的状态判断不一致。
12. 目前最核心的问题是：Source → Task → LLM → Review → WikiWriter → Wiki Page → Search/Chat/Graph 这条主链路没有稳定闭环。

本轮必须以“真正可用”为目标，而不是局部修补 UI。

============================================================
二、v0.2.0 的核心验收目标
============================================================

本轮完成后，必须满足以下真实用户流程：

用户上传一个 DOCX 文档后：

1. 文件进入 Source 管理页面，状态显示为“已保存 / 分析中 / 待审阅 / 已应用”等明确状态。
2. 导入任务能显示当前阶段、模型调用状态、失败原因、可恢复动作。
3. LLM 分析完成后，审阅中心生成 Review Items。
4. 如果知识库中还没有对应页面，Review Item 必须是“创建页面”，而不是错误地显示“更新页面”。
5. 用户点击接受某个 Review Item 后，必须真实生成 Markdown Wiki 页面。
6. 首页 Wiki 页面数量必须增加。
7. Wiki 页面列表必须出现该页面。
8. Wiki 页面能以 Markdown 阅读模式打开。
9. Source Preview 的“关联 Wiki 页面”能看到该页面。
10. Search 能搜到该页面。
11. Chat 能读取该页面正文回答问题。
12. Graph 至少能显示该页面节点，即使没有关系边。
13. Health Check 不能在 Wiki 页面为 0、待审为 16、图谱无边时显示“全部通过”。
14. 用户从首页能清楚知道下一步应该去审阅、修复、重建图谱还是配置模型。

如果这条链路不通，其他功能都视为未完成。

============================================================
三、P0：重构 Review 生成逻辑，修复 create/update 错误
============================================================

当前审阅中心中大量 Review Item 显示为“更新页面”，但顶部提示“目标页面文件不存在”。这是严重逻辑错误。

请修复 ResolutionAgent / WikiUpdateAgent / ReviewGenerator 的 create/update 决策。

规则如下：

1. 如果目标 wiki page 不存在：
   - operation_type 必须是 create_page；
   - UI 显示“创建页面”；
   - 风险等级可为 medium；
   - 不允许显示“更新页面”。

2. 如果目标 wiki page 存在：
   - operation_type 才能是 update_page 或 append_section；
   - 必须有 base_content_hash；
   - 必须能读取旧页面内容。

3. 如果模型认为“需要更新已有页面”，但本地找不到该页面：
   - 自动降级为 create_page；
   - 或标记为 unresolved_candidate；
   - 不允许生成一个无法应用的 update_page。

4. 如果知识项存在但没有关联页面：
   - 生成 create_page review item；
   - 不要生成 update_page。

5. Review Item 字段必须包含：
   - operation_type；
   - target_title；
   - target_path；
   - page_type；
   - source_id；
   - knowledge_item_id；
   - proposed_markdown；
   - reason；
   - confidence；
   - risk_level；
   - base_content_hash；
   - apply_status；
   - apply_error。

6. 对 skip / merge_suggestion 这类操作：
   - 不显示普通绿色“接受”；
   - 显示“确认跳过”或“人工合并”；
   - 不允许用户误以为会写入页面。

验收：
新知识库第一次导入文档时，审阅中心主要应生成“创建页面”项，而不是一堆目标不存在的“更新页面”项。

============================================================
四、P0：修复 Review Apply → WikiWriter → Wiki Page 落盘闭环
============================================================

当前首页 Wiki 页面为 0，这是最大问题。请确保用户接受审阅后能真正生成页面。

接受 create_page Review Item 时必须执行：

1. 读取 review_item。
2. 校验 operation_type = create_page。
3. 获取 proposed_markdown。
4. 如果 proposed_markdown 为空，则根据 review item 的结构化字段生成 Markdown。
5. 创建目标目录：
   workspace/wiki/concepts/
   workspace/wiki/entities/
   workspace/wiki/methods/
   workspace/wiki/datasets/
   workspace/wiki/sources/
   workspace/wiki/questions/
6. 写入 Markdown 文件。
7. 写入 wiki_pages 表。
8. 更新 knowledge_items.page_id。
9. 更新 sources linked_pages_count。
10. 创建 graph_node。
11. 创建 version snapshot。
12. 更新 index.md。
13. 追加 log.md。
14. 设置 review_item.status = applied。
15. 刷新 Dashboard、Wiki 页面、Source Preview、Graph、Health。

接受 update_page 时必须执行：

1. 检查目标页面是否存在。
2. 检查 base_content_hash。
3. 如果页面不存在，提示“目标页面不存在，是否转为创建页面？”
4. 不允许静默失败。
5. 写入前创建版本快照。
6. 写入后更新 content_hash。

失败时必须：

1. review_item.status = apply_failed。
2. 写入 apply_error。
3. UI 显示具体错误。
4. 不允许标记 applied。
5. 不允许只在顶部显示一句“目标页面不存在”。

验收：
接受一个创建页面项后，首页 Wiki 页面数量从 0 变为 1，Wiki 页面列表出现页面，文件系统中真实存在 .md 文件。

============================================================
五、P0：Wiki 页面必须可阅读、可编辑、可溯源
============================================================

当前 Wiki 页面为空，必须把 Wiki 页面模块做成可用页面。

Wiki 页面布局要求：

左侧：
- 页面列表；
- 搜索页面；
- 按类型分组：概念、实体、方法、数据集、来源、问题；
- 显示页面数量。

中间：
- Markdown 阅读模式；
- 编辑模式；
- 源码模式。

右侧：
- 页面元信息；
- 来源 Source；
- 关联知识项；
- 关联关系；
- 版本历史；
- 健康状态；
- 操作按钮。

阅读模式要求：

1. 默认渲染 Markdown，不直接显示 YAML frontmatter。
2. frontmatter 显示到右侧元信息面板。
3. 支持标题、列表、表格、引用、代码块、链接。
4. 支持“打开 Source Preview”。
5. 支持“在 Chat 中询问”。
6. 支持“查看图谱节点”。
7. 支持“复制页面路径”。

编辑模式要求：

1. 用户可编辑正文。
2. 保存前创建版本快照。
3. 保存后更新 wiki_pages.content_hash。
4. 保存后刷新健康检查。

空状态要求：

如果没有 Wiki 页面，不要只显示“暂无页面”，而要显示：

- 当前有 16 个待审阅修改，建议先去审阅中心接受页面创建；
- 或者从 Source 重新生成审阅；
- 或者手动新建 Wiki 页面；
- 按钮：去审阅 / 新建页面 / 从 Markdown 重建索引。

验收：
Wiki 页面不是空壳，用户能从空状态直接进入有效操作。

============================================================
六、P0：Source Preview 必须显示真实分析结果
============================================================

当前 Source Preview 中：

- Markdown 预览有内容；
- AI 摘要为空；
- 抽取实体为 0；
- 抽取概念为 0；
- 关联 Wiki 页面为 0；
- 任务日志只有一个任务。

这说明 Source Preview 没有正确读取任务中间结果和数据库分析结果。

请修复 Source Preview 的数据来源。

Source Preview Tabs 要求：

1. 原始信息
   - 文件名；
   - 文件类型；
   - 文件大小；
   - hash；
   - source_id；
   - 状态；
   - 文件路径；
   - 最近任务；
   - 预览状态。

2. Markdown 预览
   - 显示从 DOCX/TXT/MD/HTML 提取的 Markdown 预览；
   - 明确标注：“这是原始文件的 Markdown 预览，不是正式 Wiki 页面。”

3. AI 摘要
   - 从 ingest_result / model_raw_response / source_summary 中读取；
   - 如果为空，显示“本次任务未生成 AI 摘要”，并提供“重新分析”按钮；
   - 不要只显示空白。

4. 抽取实体
   - 读取 knowledge_items 中 type=entity 且 source_id 当前文件；
   - 显示名称、类型、置信度、是否已关联页面、操作。

5. 抽取概念
   - 读取 knowledge_items 中 type=concept/method/dataset/topic；
   - 显示名称、类型、置信度、是否已关联页面、操作。

6. 关联 Wiki 页面
   - 读取 wiki_pages 与 source_id 的关联；
   - 如果没有页面但有待审阅，显示：
     “当前有 16 个待审阅页面创建建议，接受后将显示在这里。”
   - 提供按钮：去审阅。

7. 任务日志
   - 显示该 source 的所有任务；
   - 任务状态；
   - 时间；
   - 点击进入任务详情。

验收：
Source Preview 不允许大面积空白。没有数据时必须解释为什么没有，并提供下一步操作。

============================================================
七、P0：健康检查必须真实反映系统状态
============================================================

当前健康检查在系统明显不可用时显示“所有检查项通过”，这是严重问题。

请重写健康检查判断规则。

以下情况不能显示“通过”：

1. Source > 0 且 Wiki 页面 = 0。
2. 待审阅项 > 0。
3. knowledge_items 未关联页面数量 > 0。
4. graph_nodes > 0 但 graph_edges = 0。
5. Review Item 指向不存在的 target_path。
6. update_page 操作但目标文件不存在。
7. Source 状态为 processed 但没有关联页面。
8. Source 有任务，但 AI 摘要为空、抽取实体为空、抽取概念为空。
9. 首页显示有待审/失败/警告。
10. 图谱画布无法渲染节点。

健康检查必须分类：

- 严重错误：阻止核心闭环。
- 警告：功能可用但质量不足。
- 提示：建议优化。
- 通过：完全正常。

至少检查以下项目：

1. Source 状态一致性。
2. Task 状态一致性。
3. Review Item 可应用性。
4. Wiki 页面文件存在性。
5. wiki_pages 数据库一致性。
6. knowledge_items 是否关联页面。
7. graph_nodes 是否能渲染。
8. graph_edges 是否为空。
9. Source Preview 数据完整性。
10. Search index 是否存在。
11. Chat 是否能读取页面正文。
12. index.md / log.md 是否存在。
13. 版本快照是否存在。

修复按钮要求：

1. “修复目标页面不存在的 Review”
   - 将 update_page 转为 create_page；
   - 或标记为 unresolved，需要用户处理。

2. “从待审项生成页面”
   - 引导用户进入审阅中心；
   - 不直接绕过审阅。

3. “从 Markdown 重建 Wiki 索引”
   - 只用于已有 md 文件；
   - 显示扫描数量、成功数量、跳过原因。

4. “重建图谱节点”
   - 从 wiki_pages、knowledge_items、sources 生成节点。

5. “重建图谱关系”
   - 从 relationships 表生成边；
   - 如果 relationships 为空，提示需要重新运行关系分析。

验收：
当 Wiki 页面为 0、待审为 16 时，健康检查必须显示“需处理”，不能显示“所有检查项通过”。

============================================================
八、P0：知识图谱必须真正渲染节点
============================================================

当前图谱显示 16 节点、0 边，但画布空白。请修复图谱画布。

最低要求：

1. graph_nodes > 0 时必须显示节点。
2. graph_edges = 0 时仍然显示孤立节点。
3. 思维导图模式下：
   - 中心节点为当前知识库；
   - 第一层为类型分组：
     概念、实体、方法、数据集、来源、问题；
   - 第二层为具体节点。
4. 节点无 page_id 时显示橙色边框或警告图标。
5. 节点有 page_id 时可双击打开 Wiki 页面。
6. 点击节点显示右侧详情：
   - 名称；
   - 类型；
   - 置信度；
   - 来源；
   - 是否关联页面；
   - 操作：生成 Wiki 草稿、打开 Source、在 Chat 中询问。
7. 如果无边，画布上方提示：
   “当前有 16 个知识节点，但尚未建立关系。可先完成审阅生成页面，再运行关系重建。”

不要只显示提示框而不显示节点。

验收：
图谱页面至少能看到 16 个节点按类型分组显示。

============================================================
九、P0：审阅中心必须变成用户真正能处理的工作台
============================================================

当前审阅中心虽然列出 16 项，但用户体验差：不知道接受后会怎样，也有目标文件不存在问题。

请重构审阅中心的信息层级。

顶部必须显示：

- 当前任务；
- Source 文件名；
- 总审阅项；
- 创建页面数量；
- 更新页面数量；
- 关系数量；
- 高/中/低风险数量；
- 可直接接受数量；
- 需要人工处理数量；
- 不可应用数量。

每个审阅项必须显示：

1. 操作类型：创建页面 / 更新页面 / 添加关系 / 添加别名 / 人工合并 / 跳过。
2. 目标标题。
3. 目标路径。
4. 页面类型。
5. 来源文件。
6. 置信度。
7. 风险等级。
8. AI 原因。
9. 是否可应用。
10. 不可应用原因。

如果目标页面不存在：

- 如果 operation 是 update_page：
  - 显示红色错误；
  - 提供“转为创建页面”按钮。
- 如果 operation 是 create_page：
  - 显示正常。

Diff 展开必须显示：

- 将创建/更新的 Markdown 内容；
- frontmatter；
- Summary；
- Definition；
- Key Points；
- Evidence；
- Related Pages；
- Source 引用；
- 接受后影响。

批量操作：

1. 接受所有低风险可应用项。
2. 批量转为创建页面。
3. 批量拒绝不可应用项。
4. 批量重新生成 Review。
5. 批量应用后显示成功/失败明细。

验收：
用户能在审阅中心明确知道每个按钮会产生什么结果，并能实际创建页面。

============================================================
十、P1：任务系统要减少用户困惑
============================================================

任务页面目前有很多状态，但用户不一定理解。

请优化任务状态：

状态应统一为：

- 排队中
- 分析中
- 等待模型返回
- 解析模型结果
- 生成审阅
- 等待审阅
- 已应用
- 部分应用
- 失败
- 已取消
- 已归档

每个任务卡片显示：

1. Source 文件名。
2. 当前阶段。
3. 当前 Agent。
4. 创建时间。
5. 最近更新时间。
6. 失败原因。
7. 下一步操作。
8. 按钮：查看详情、重试、取消、归档、打开 Source、去审阅。

任务详情页：

1. 时间线。
2. 中间文件。
3. 模型配置。
4. Token 估计。
5. 错误原因。
6. 可恢复阶段。
7. 一键重试。

验收：
用户不需要看底部状态栏，也能理解任务当前发生了什么。

============================================================
十一、P1：首页必须成为可操作 Dashboard
============================================================

首页目前已经有推荐下一步，但还不够强。

首页必须显示：

1. 当前知识库状态：
   - 健康 / 待审 / 有错误 / 需要配置。
2. 统计卡片：
   - Wiki 页面；
   - Source 文件；
   - 待审阅；
   - 知识关系；
   - 失败任务；
   - 健康问题。
3. 推荐下一步：
   - 待审 16 → 去审阅；
   - Wiki 页面 0 但有待审 → 接受页面创建；
   - 图谱 0 边 → 完成页面后重建关系；
   - Source processed 但无页面 → 去审阅或重新分析。
4. 最近活动：
   - 最近上传；
   - 最近任务；
   - 最近生成页面；
   - 最近错误。
5. 快速操作：
   - 上传文档；
   - 去审阅；
   - 新建页面；
   - 打开 Source；
   - 健康检查；
   - 图谱。

首页的状态文案必须和健康检查一致。

验收：
用户打开首页就能知道下一步该做什么。

============================================================
十二、P1：Source 状态语义统一
============================================================

当前 Source 显示 processed，但关联 Wiki 页面为 0，这容易误导用户。

请统一 Source 状态：

- saved：已保存，未分析
- queued：等待分析
- analyzing：分析中
- analyzed：已分析，未生成审阅
- review_pending：已生成审阅，等待处理
- partially_applied：部分审阅已应用
- applied：已生成 Wiki 页面
- applied_with_warnings：已应用但有警告
- analysis_failed：分析失败
- asset_only：仅作为文件资产保存

规则：

1. 如果有待审项，Source 状态应为 review_pending。
2. 如果没有 Wiki 页面，不应显示 applied。
3. processed 这种模糊状态应废弃或映射为更明确状态。
4. Source 列表显示：
   - 关联页面数；
   - 关联知识项数；
   - 待审项数；
   - 最近任务状态；
   - 下一步操作。

验收：
用户能从文件管理页直接看出这个文件下一步该做什么。

============================================================
十三、P1：Search 和 Chat 至少要能处理已生成页面
============================================================

当 Wiki 页面生成后：

Search 必须能搜索：

- 标题；
- 正文；
- aliases；
- Source 文件名；
- tags。

Chat 必须：

1. 读取 Wiki 页面正文。
2. 显示引用来源。
3. 如果没有 Wiki 页面，提示：
   “当前知识库还没有正式 Wiki 页面，请先处理审阅。”
4. 不能只根据 Source Preview 原文回答，除非用户选择基于 Source 问答。

验收：
生成 1 个 Wiki 页面后，Search 和 Chat 都能使用它。

============================================================
十四、空状态设计要求
============================================================

所有空状态都必须有解释和下一步操作。

禁止只显示：

- 暂无数据
- 暂无页面
- 暂无摘要
- 暂无实体
- 暂无图谱数据

必须改成：

1. 为什么为空。
2. 当前系统已有何数据。
3. 用户下一步应该做什么。
4. 提供按钮。

示例：

Wiki 页面为空：
“当前还没有正式 Wiki 页面，但已有 16 个待审阅页面建议。接受审阅后会生成 Wiki 页面。”
按钮：去审阅 / 新建页面 / 从 Markdown 重建索引

AI 摘要为空：
“本次任务没有生成 Source 摘要，可能是模型返回中缺少 summary 字段。”
按钮：重新分析 / 查看模型原始响应

图谱为空：
“当前有 16 个知识节点，但没有关系边。可先完成页面审阅，再重建关系。”
按钮：显示孤立节点 / 重建图谱

Source 关联页面为空：
“该文件尚未生成正式 Wiki 页面，但已有待审阅修改。”
按钮：去审阅

============================================================
十五、统一数据状态刷新
============================================================

当前多个页面状态不一致。请实现统一刷新机制。

当以下事件发生时，必须刷新相关页面数据：

1. 上传 Source。
2. 任务状态变化。
3. Review 生成。
4. Review 被接受。
5. Wiki 页面写入。
6. Graph 重建。
7. Health 修复。
8. Source Preview 生成。

刷新对象：

- Dashboard；
- Source 列表；
- Review Center；
- Wiki 页面；
- Graph；
- Health；
- Bottom Status Bar。

不要出现首页显示待审 16，但审阅中心不同步，或健康检查显示通过但首页仍有问题。

============================================================
十六、版本号和开发阶段
============================================================

请更新版本号为：

v0.2.0-dev

要求：

1. package.json；
2. tauri.conf；
3. UI 底部状态栏；
4. 关于页面或设置页；
5. 构建信息。

不要继续显示 v0.1.2。

============================================================
十七、禁止事项
============================================================

本轮不要实现以下内容：

1. OCR。
2. 多模态图片理解。
3. 本地 PDF 深度解析。
4. 云同步。
5. 多人协作。
6. Web 端。
7. 移动端。
8. 插件市场。
9. 花哨动画。
10. 复杂 3D 图谱。
11. 绕过审阅直接自动写入 Wiki。

重点是核心闭环和用户体验。

============================================================
十八、开发顺序
============================================================

请严格按以下顺序开发：

1. 修复 create/update 判断错误。
2. 修复 Review Apply → WikiWriter → Wiki 页面落盘。
3. 修复 wiki_pages、knowledge_items、sources、graph_nodes 关联。
4. 修复 Wiki 页面模块，让页面可读。
5. 修复 Source Preview 各 Tab 数据来源。
6. 重写 Health Check 判断规则。
7. 修复 Graph 孤立节点渲染。
8. 优化审阅中心可解释性。
9. 优化 Dashboard。
10. 统一 Source 状态。
11. 优化任务系统。
12. 加空状态下一步操作。
13. 统一全局刷新。
14. 更新版本号。
15. 全链路测试。

============================================================
十九、完整验收流程
============================================================

必须按以下流程自测，并确保通过：

1. 新建一个空知识库。
2. 配置 DeepSeek。
3. 上传一个 DOCX。
4. Source 状态显示 analyzing。
5. 任务页面显示分析进度。
6. 模型返回后生成 Review Items。
7. 首页显示“待审 16”，并推荐去审阅。
8. 审阅中心中，新页面应显示为“创建页面”，不是“更新页面”。
9. 点击接受一个 create_page。
10. 页面真实写入 workspace/wiki/concepts 或对应目录。
11. wiki_pages 数据库新增记录。
12. 首页 Wiki 页面数量变为 1。
13. Wiki 页面列表显示该页面。
14. 点击页面能阅读 Markdown。
15. Source Preview 的关联 Wiki 页面显示该页面。
16. knowledge_item 关联到 page_id。
17. Graph 画布显示该节点。
18. Search 能搜到该页面。
19. Chat 能基于该页面回答。
20. Health Check 不再显示该项错误。
21. 如果仍有待审，Health 显示“待处理”，不能显示全部通过。
22. 全部低风险审阅接受后，Wiki 页面数量继续增加。
23. 首页、审阅、健康、图谱、Source 状态一致。

只有通过这条流程，才算本轮完成。

============================================================
二十、交付输出
============================================================

完成开发后，请输出：

1. 修复的问题列表。
2. 修改的文件列表。
3. 数据库 migration。
4. create/update 判断逻辑说明。
5. Review Apply 状态机说明。
6. WikiWriter 写入流程说明。
7. Source Preview 数据来源说明。
8. Health Check 新规则。
9. Graph 渲染规则。
10. Dashboard 状态规则。
11. 空状态设计说明。
12. 手动测试结果。
13. 当前仍未完成但不影响核心闭环的问题。