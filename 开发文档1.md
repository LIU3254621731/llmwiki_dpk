你是一名资深全栈桌面软件工程师、AI Agent 系统架构师、产品架构师和 Windows 桌面应用交付工程师。请你基于以下完整需求，从零开发一个可一键安装、本地运行、功能完整、架构稳定、UI 专业的桌面软件。

软件名称暂定为：

LLM Knowledge Wiki

中文名称可暂定为：

智维 Wiki / LLM 知识维护工作台

请严格按照本文档开发，不要自行加入已经被否决的功能方向。

============================================================
一、项目总定义
============================================================

本软件不是普通 RAG 问答工具，不是普通 Markdown 编辑器，不是 OCR 软件，不是本地 PDF 深度解析工具，不是多模态图片理解系统，也不是简单的 Chatbox / Cherry Studio 克隆。

本软件的核心定义是：

一个本地优先的、由大模型驱动的、多 Agent 协作式 Markdown Wiki 维护工作台。

它的核心目标是：

让用户通过上传文档、提问、审阅和编辑，驱动一个本地 Markdown Wiki 持续演化。DeepSeek 等大模型负责理解文档、抽取知识、判断实体概念关系、生成 Wiki 更新建议；本地多 Agent Runtime 负责任务调度、Agent 协作、结构化通信、状态机、审阅、版本快照、文件写入和稳定性控制；用户通过专业、简洁、清晰的交互界面维护自己的长期知识资产。

请牢记本软件的产品核心：

交互是入口；
本地多 Agent Runtime 是流程；
大模型是能力；
Markdown Wiki 是长期知识资产。

更具体地说：

用户负责意图、上传、提问、审阅和确认。
Agent 负责任务拆解、流程编排和结构化协作。
DeepSeek 负责文档理解、知识抽取、关系判断和内容生成。
本地系统负责存储、校验、审阅、写入、版本控制和安全。

本软件最终形成的闭环是：

上传资料
→ LLM 抽取结构化知识
→ 多 Agent 消歧与关联
→ 生成 Wiki 更新计划
→ 用户审阅 Diff
→ 本地写入 Markdown Wiki
→ 基于 Wiki 问答
→ 高价值回答沉淀为新 Wiki 页面
→ Wiki 持续演化

一句话概括：

从文档到 Wiki，从问答到知识资产。

============================================================
二、必须严格遵守的产品边界
============================================================

请明确排除以下功能，不要把它们作为开发内容：

1. 不做本地 PDF 深度解析。
2. 不做本地 OCR。
3. 不做多模态图片理解。
4. 不做图片 caption。
5. 不做 PDF 页面切片。
6. 不做表格视觉识别。
7. 不做公式 OCR。
8. 不引入 Tesseract、PaddleOCR、OpenCV OCR、LayoutLM、Nougat、MinerU、Marker 等复杂文档解析依赖。
9. 不做传统向量 RAG 作为核心架构。
10. 不做自由 Agent 群聊。
11. 不允许 Agent 直接修改本地 Wiki 文件。
12. 不允许大模型直接写入正式知识库。
13. 不做 Web 端部署。
14. 不做移动端。
15. 不做多人协作云端系统。
16. 不做复杂云同步。
17. 不要求用户安装 Node.js、Python、Rust、数据库或任何开发环境。
18. 不要求用户通过命令行启动。

本软件的核心路径必须是：

Prompt + 文档附件
→ DeepSeek
→ 严格 JSON 结构化结果
→ 本地 JSON Schema 校验
→ Candidate Search / Resolution
→ Wiki Update Plan
→ Review Diff
→ 用户确认或低风险自动应用
→ WikiWriter 写入本地 Markdown Wiki
→ VersionManager 保存快照
→ IndexService / LogService 更新 index.md 和 log.md

============================================================
三、默认模型与文件处理原则
============================================================

默认模型供应商：

DeepSeek

默认模型配置建议：

普通问答模型：deepseek-chat
长文档分析模型：deepseek-reasoner
Wiki 写作模型：deepseek-chat
冲突检测模型：deepseek-reasoner
复杂推理模型：deepseek-reasoner
图片理解模型：无，不启用
Embedding 模型：不作为核心依赖，可预留接口

默认假设：

1. DeepSeek 支持 PDF、DOCX、Markdown、TXT、HTML 等文档类附件输入。
2. DeepSeek 不作为图片理解模型使用。
3. 图片类文件只作为本地资产保存，不参与 AI 知识抽取。
4. 扫描件 PDF 仍作为 PDF 附件提交给 DeepSeek，但系统不额外 OCR。
5. 如果 DeepSeek 无法理解扫描件或图片内容，系统只记录提示，不做 fallback OCR。

文档文件处理规则：

支持：
- PDF
- DOCX
- Markdown
- TXT
- HTML

处理方式：
1. 保存到 raw/sources/documents/
2. 生成 source_id
3. 计算文件 hash
4. 创建 Source Ingest Task
5. 作为附件提交给 DeepSeek
6. 要求 DeepSeek 返回结构化 JSON
7. 本地校验、审阅、写入 Wiki

图片文件处理规则：

支持：
- PNG
- JPG
- JPEG
- WEBP
- GIF

处理方式：
1. 保存到 raw/assets/images/
2. 生成 asset_id
3. 不 OCR
4. 不视觉理解
5. 不参与知识抽取
6. 可由用户手动插入 Wiki 页面或作为附件保存
7. UI 必须明确提示：图片仅保存为资产，不参与 AI 知识抽取

必须实现模型能力检测：

设置页中应支持：
1. DeepSeek API 连接测试
2. 普通聊天测试
3. JSON 输出测试
4. PDF 附件上传测试
5. DOCX 附件上传测试
6. 最大文件大小提示
7. 超时与错误提示
8. 如果模型不支持附件输入，则禁用文档导入任务并明确提示

============================================================
四、目标交付形态
============================================================

本项目要求开发为一个普通用户可以一键安装、本地运行的 Windows 桌面软件。

目标平台：

Windows 10 / Windows 11

交付要求：

1. 生成 Windows 安装包。
2. 用户双击安装包即可安装。
3. 安装后创建桌面快捷方式。
4. 安装后创建开始菜单快捷方式。
5. 用户无需安装 Node.js、Python、Rust、SQLite 或其他开发环境。
6. 用户无需命令行启动。
7. 第一次打开进入初始化向导。
8. 用户知识库默认保存在本地用户文档目录。
9. 卸载软件时默认不删除用户知识库。
10. API Key 使用系统凭据安全存储，不得明文写入配置文件、数据库、日志、Prompt 快照或错误报告。

建议路径：

应用安装目录：
C:\Users\<User>\AppData\Local\Programs\LLM Knowledge Wiki\

用户数据目录：
C:\Users\<User>\Documents\LLM Knowledge Wiki\

应用配置目录：
C:\Users\<User>\AppData\Roaming\LLM Knowledge Wiki\

============================================================
五、推荐正式技术架构
============================================================

请直接按生产级正式架构开发，不要按 Demo、临时 MVP、第一版/第二版的方式开发。

推荐技术栈：

桌面框架：Tauri 2.x
前端：React + TypeScript
UI：Tailwind CSS + shadcn/ui
状态管理：Zustand
数据请求与缓存：TanStack Query
本地核心：Rust Core Runtime
数据库：SQLite
任务队列：Rust 本地任务队列 + SQLite 持久化状态
模型调用：Rust reqwest HTTP Client，通过统一 ModelGateway 调用 DeepSeek
JSON Schema 校验：Rust JSON Schema Validator
Markdown 编辑/预览：Milkdown / TipTap / Monaco + Markdown Preview 任选一种稳定方案
Diff 展示：稳定的文本 Diff 组件
知识图谱：React Flow 或 Cytoscape.js
密钥存储：Windows Credential Manager / 系统 Keychain
安装包：Tauri Bundler + NSIS / MSI

正式职责划分：

React UI 只负责：
- 页面渲染
- 用户交互
- 表单输入
- 状态展示
- Chat 输入输出
- 审阅 Diff 展示
- 图谱展示
- 设置界面

Rust Core Runtime 负责：
- AppKernel
- Workspace 管理
- SQLite 数据库
- TaskQueue
- AgentRuntime
- Coordinator 调度
- ModelGateway
- DeepSeekClient
- PromptBuilder
- PromptRegistry
- JSON Schema 校验
- Candidate Search
- Resolution Engine
- Relationship Engine
- Review Engine
- Diff Engine
- WikiWriter
- VersionManager
- IndexService
- LogService
- GraphService
- SearchService
- EventBus
- Recovery Check
- Workspace Reconcile
- 文件安全读写
- API Key 安全存储

整体架构：

React UI
→ Tauri Commands / Events
→ Rust AppKernel
→ TaskQueue
→ CoordinatorAgent
→ Specialized Agents
→ ModelGateway
→ DeepSeek API
→ JsonSchemaValidator
→ CandidateSearchEngine
→ ResolutionEngine
→ ReviewEngine
→ WikiWriter
→ VersionManager
→ Markdown Wiki + SQLite

============================================================
六、本地多 Agent Runtime 定义
============================================================

本软件中的多 Agent 系统必须在本地桌面应用内运行。

注意：

这里的“本地运行多 Agent”不是指在本地运行多个大模型，而是指本地运行 Agent Runtime、Task Queue、Prompt Builder、JSON Validator、WikiWriter、VersionManager、EventBus 和 Agent 调度逻辑。

DeepSeek 通过 API 提供大模型能力。

Agent 定义：

Agent = Role + Prompt + Tools + Context + Input Schema + Output Schema + State

Agent 不是自由聊天机器人，而是具有明确职责、固定输入输出协议和受控工具权限的任务处理模块。

Agent 不允许自由互相调用。
Agent 不允许直接修改本地 Wiki。
Agent 不允许绕过 Coordinator。
Agent 只返回结构化 JSON 和 next_actions。
Coordinator 根据 next_actions 调度下一个 Agent。
所有文件写入只能通过 WikiWriter。
所有模型调用只能通过 ModelGateway。
所有任务状态必须写入 SQLite。
所有 Agent 输入输出必须保存到 .runtime/tasks/{task_id}/。

需要注册的 Agent：

1. CoordinatorAgent
职责：
- 判断任务类型
- 拆解任务
- 调度专职 Agent
- 传递结构化输入
- 收集结构化输出
- 决定下一步流程
- 写入 task_events
- 处理失败和重试

2. SourceIngestAgent
职责：
- 根据知识库模板构建 Ingest Prompt
- 将 Prompt + 文档附件提交给 DeepSeek
- 要求返回严格 JSON
- 抽取 source_summary、entities、concepts、topics、claims、relationships、proposed_wiki_updates、coverage_report

3. ResolutionAgent
职责：
- 判断新抽取实体、概念、主题是否与已有页面重复
- 基于候选页面、aliases、canonical_name、index 片段进行消歧
- 决定 create_new / update_existing / add_alias / append_to_existing / merge_suggestion / skip / needs_user_review

4. RelationshipAgent
职责：
- 将实体、概念、主题之间的候选关系标准化
- 输出关系三元组
- 将 source 和 target 映射到 canonical Wiki page
- 控制图谱噪声
- 保留 evidence 和 confidence

5. WikiUpdateAgent
职责：
- 根据已消歧知识和关系生成 Wiki 更新计划
- 输出 wiki_update_plan
- 不直接写文件
- 标记 risk_level 和 requires_review

6. ReviewAgent
职责：
- 根据 Wiki Update Plan 生成 Review 和 Review Items
- 生成用户可读摘要
- 生成 Diff 数据
- 按风险等级分组
- 高风险修改必须进入审阅中心

7. QueryAgent
职责：
- 基于整个知识库、当前页面、指定文件、指定标签或指定目录回答问题
- 调用 DeepSeek 生成带引用的回答
- 支持将高价值回答保存为 Wiki 页面

8. HealthCheckAgent
职责：
- 检查重复页面、孤立页面、缺失引用、过时内容、冲突、图谱噪声、index/log 不一致
- 只生成报告和建议，不直接修改 Wiki

9. IndexAgent / IndexService
职责：
- 更新 index.md
- 维护页面索引、摘要、aliases、related pages

10. LogAgent / LogService
职责：
- 追加 log.md
- 记录导入、问答、审阅、应用修改、健康检查等事件

============================================================
七、Agent 通信协议
============================================================

Agent 之间不直接自由对话。

通信方式：

1. Coordinator 统一调度。
2. Agent 读取结构化 input.json。
3. Agent 输出结构化 output.json。
4. 所有输入输出保存到 .runtime/tasks/{task_id}/。
5. 所有开始、完成、失败写入 task_events。
6. 前端通过 EventBus 展示 Agent Activity Timeline。

统一 AgentMessage 格式：

{
  "message_id": "",
  "task_id": "",
  "from_agent": "",
  "to_agent": "",
  "message_type": "",
  "payload": {},
  "created_at": ""
}

message_type 包括：

SOURCE_INGEST_REQUEST
SOURCE_INGEST_RESULT
RESOLUTION_REQUEST
RESOLUTION_RESULT
RELATIONSHIP_REQUEST
RELATIONSHIP_RESULT
WIKI_UPDATE_REQUEST
WIKI_UPDATE_PLAN
REVIEW_REQUEST
REVIEW_CREATED
QUERY_REQUEST
QUERY_RESULT
HEALTH_CHECK_REQUEST
HEALTH_CHECK_REPORT
ERROR

Agent 输出中可以包含 next_actions，但 next_actions 只能由 Coordinator 执行。

============================================================
八、任务状态机与稳定性要求
============================================================

所有后台维护任务都必须是持久化状态机，不能只是内存里的 Promise。

任务状态至少包括：

created
queued
locked
prompt_built
sent_to_model
model_returned
json_validating
json_valid
json_invalid
json_repairing
json_repaired
candidate_searching
resolution_running
relationship_running
update_plan_generating
review_generating
review_pending
applying
applied
failed
cancelled
interrupted

每个任务必须保存：

task_id
kb_id
task_type
status
current_agent
model_profile_id
prompt_snapshot_path
input_json_path
raw_output_path
validated_output_path
retry_count
error_message
created_at
updated_at
locked_at
completed_at

每个任务目录结构：

.runtime/
  tasks/
    task_001/
      input.json
      prompt.md
      model_raw_response.txt
      ingest_result.json
      resolution_result.json
      relationship_result.json
      update_plan.json
      review_items.json
      error.log

软件启动时必须执行 Recovery Check：

1. 扫描 interrupted / queued / locked / applying 状态任务。
2. 对模型调用中断任务标记为 interrupted，可重试。
3. 对 review_pending 任务恢复审阅。
4. 对 applying 中断任务检查 operation_id 和版本快照，避免重复写入。
5. 允许用户继续、重试、取消或回滚。

============================================================
九、Wiki 写入安全协议
============================================================

LLM 不允许直接写 Wiki。
Agent 不允许直接写 Wiki。
所有写入必须通过 WikiWriter。

WikiWriter 写入流程：

1. 获取页面锁。
2. 读取当前页面。
3. 检查 base_version_hash。
4. 创建版本快照。
5. 校验 update_plan。
6. 生成目标 Markdown。
7. 写入临时文件。
8. 使用原子 rename 替换正式文件。
9. 更新 SQLite。
10. 更新 index.md。
11. 追加 log.md。
12. 释放页面锁。

必须防止：

1. 并发写同一页面。
2. 写到一半崩溃。
3. 重复应用同一 operation。
4. 覆盖用户手动编辑。
5. 删除未审阅内容。
6. 覆盖核心定义。
7. index.md、log.md、SQLite 与 Markdown 文件不一致。

每个 operation 必须有：

operation_id
operation_hash
target_path
source_task_id
applied_at

执行前检查：

1. operation_hash 是否已应用。
2. target_path 是否被锁定。
3. 当前页面 hash 是否等于生成 Diff 时的 base_version_hash。
4. source_id 是否已写入页面。
5. alias 是否已存在。
6. relationship 是否已存在。

如果当前页面已被用户手动修改，不能直接应用旧 Diff，必须提示重新生成 Diff。

============================================================
十、知识库目录结构
============================================================

每个知识库 workspace 结构如下：

workspace/
  raw/
    sources/
      documents/
      webclips/
    assets/
      images/
      attachments/
  wiki/
    index.md
    log.md
    overview.md
    sources/
    concepts/
    entities/
    topics/
    questions/
    reviews/
  schema/
    AGENTS.md
    WIKI_SCHEMA.md
    PROMPTS.md
    TEMPLATE.yaml
  drafts/
    ingest/
    wiki_updates/
  versions/
    snapshots/
    pages/
  .runtime/
    tasks/
    logs/
  db/
    app.sqlite
  config/
    kb.config.json

说明：

raw/：
保存原始文件，不允许 AI 修改，是事实来源。

wiki/：
保存 AI 生成和用户确认后的 Markdown Wiki 页面，是长期知识资产。

schema/：
保存知识库模板、Agent 规则、Prompt 规则、页面规范。

drafts/：
保存待审阅草稿。

versions/：
保存页面版本快照和回滚数据。

.runtime/：
保存任务运行时输入、Prompt、模型原始输出、结构化输出和错误日志。

db/：
保存 SQLite 数据库。

config/：
保存知识库配置。

============================================================
十一、页面格式与 Markdown 规范
============================================================

所有 Wiki 页面必须使用稳定模板，不允许模型自由生成完全随意格式的 Markdown。

页面必须包含 YAML frontmatter。

通用 frontmatter 示例：

---
title: Self-Attention
type: concept
canonical_name: Self-Attention
aliases:
  - self attention
  - 自注意力
  - 自注意力机制
sources:
  - src_20260506_001
tags:
  - attention
  - transformer
confidence: medium
status: active
created: 2026-05-06
updated: 2026-05-06
last_updated_by_task: task_20260506_001
---

正文 section 必须稳定，建议使用 section id：

# Title

## Summary <!-- section:summary -->

## Definition <!-- section:definition -->

## Key Points <!-- section:key_points -->

## Evidence <!-- section:evidence -->

## Related Pages <!-- section:related_pages -->

## Source Contributions <!-- section:source_contributions -->

## Open Questions <!-- section:open_questions -->

WikiWriter 应优先用模板渲染页面，模型只提供结构化内容块。

不要让模型输出完全不可控的 Markdown。

============================================================
十二、知识库模板系统
============================================================

软件必须支持知识库模板。

模板决定：

1. 默认目录结构
2. 页面类型
3. YAML frontmatter 字段
4. Agent 抽取策略
5. Prompt 内容
6. 审阅规则
7. 图谱节点类型
8. 图谱边类型
9. 默认问答风格
10. 默认页面模板

内置模板至少包括：

1. 通用知识库
2. 科研论文知识库
3. 课程学习知识库
4. 项目管理知识库
5. 读书笔记知识库

模板文件使用 YAML。

科研论文模板示例：

name: research_paper
display_name: 科研论文知识库
description: 用于论文阅读、综述写作和研究方向管理
default_language: zh-CN

directories:
  - wiki/papers
  - wiki/concepts
  - wiki/methods
  - wiki/datasets
  - wiki/questions
  - wiki/reviews

page_types:
  paper:
    fields:
      - title
      - authors
      - year
      - venue
      - research_problem
      - method
      - dataset
      - metrics
      - results
      - limitations
      - sources
  concept:
    fields:
      - name
      - definition
      - related_papers
      - related_methods
      - sources

review_policy:
  require_review_for_new_page: false
  require_review_for_update: true
  require_review_for_conflict: true

graph:
  node_types:
    - paper
    - concept
    - method
    - dataset
    - source
  edge_types:
    - cites
    - uses_method
    - related_to
    - contradicts

知识库创建后如果要切换模板，必须作为迁移操作处理，需要生成迁移预览，不能直接强制修改已有页面结构。

============================================================
十三、去重、消歧与关系建立机制
============================================================

必须避免大模型重复创建已有实体、概念或主题页面。

在 Source Ingest Task 之后、Wiki Update Task 之前，必须执行 Resolution Task。

流程：

1. DeepSeek 从文档附件中抽取 entities、concepts、topics、claims 和 relationships。
2. 本地 CandidateSearchEngine 根据抽取结果检索已有候选页面。
3. 候选检索依据包括：
   - title
   - canonical_name
   - aliases
   - normalized_name
   - slug
   - page_type
   - source metadata
   - index.md 摘要
   - SQLite 中的 knowledge_items
   - 字符串相似度
4. 本地将新抽取项和候选页面一起提交给 ResolutionAgent。
5. ResolutionAgent 判断：
   - create_new
   - update_existing
   - append_to_existing
   - add_alias
   - merge_suggestion
   - skip
   - needs_user_review
6. 不允许模型在未检查 existing_candidates 的情况下建议新建页面。
7. 如果新项目只是已有页面的别名、翻译、缩写或大小写差异，必须更新已有页面并添加 alias。
8. 如果新项目是已有概念的子概念或组成部分，应建立 is_a 或 part_of 关系，而不是错误合并。
9. 如果无法判断是否重复，必须进入审阅中心。
10. 高风险 merge、delete、overwrite 必须用户确认。

Alias 规则：

完全同义：可自动添加
翻译同义：可自动或低风险审阅
缩写同义：需要确认
相近概念：不能作为 alias，只能 related_to
上下位概念：不能作为 alias，只能 is_a 或 part_of
同一主题下的概念：不能作为 alias

必须在 Prompt 中强调：

相关概念不是 alias。
子概念不是 alias。
实现方式不是 alias。
同一主题下的概念不是 alias。

关系建立机制：

Ingest Prompt 必须要求模型输出 relationships。

relationships 必须使用固定 relation enum，不允许模型自造关系类型。

第一批支持关系：

is_a
part_of
uses
depends_on
improves
compares_with
contradicts
cites
mentions
related_to
has_alias
belongs_to_topic
evaluated_on
proposed_by
applies_to
derived_from

关系写入规则：

1. RelationshipAgent 输出标准化关系三元组。
2. 本地将 source 和 target 映射到 canonical Wiki page。
3. 映射成功后写入 graph_edges。
4. 同时更新对应 Markdown 页面的 Related Pages 区域。
5. 没有 evidence 的关系不能写入。
6. 低置信度关系进入审阅中心。
7. related_to 数量必须限制，避免图谱噪声。
8. 每次导入高置信关系最多 20 条，related_to 最多 5 条。

============================================================
十四、引用、证据与可信度机制
============================================================

每个事实性 claim 都必须尽量绑定 source_id 和 location。

但由于本软件不做本地 PDF 深度解析，不保证模型返回的页码完全可验证。因此引用状态需要分级：

citation_status:
- model_reported：模型报告的来源位置，本地未验证
- user_verified：用户确认过
- verified：本地可验证，后续如加入解析能力可用
- missing：缺少引用

第一阶段默认文档附件来源大多为 model_reported。

页面上应诚实显示：

来源位置由模型报告，未经过本地解析验证。

Claim 需要包含：

claim
confidence
source_id
location
citation_status
created_by_task
status

claim status：

active
uncertain
conflicted
deprecated
user_verified

没有 citation 的 factual claim 不允许自动写入正式 Wiki，必须降低置信度或进入审阅。

============================================================
十五、大模型输出 JSON Schema 要求
============================================================

所有大模型输出必须优先返回严格 JSON。

禁止只返回普通自然语言。

如果模型返回非法 JSON：

1. 尝试严格 JSON parse。
2. 尝试自动提取 JSON block。
3. 调用 JSON Repair Agent 修复一次。
4. 如果仍失败，任务标记 failed。
5. 保存 model_response_raw。
6. 用户可查看原始输出并重试。

Source Ingest Task 返回格式：

{
  "source_summary": {
    "title": "",
    "type": "",
    "language": "",
    "short_summary": "",
    "long_summary": "",
    "key_points": []
  },
  "coverage_report": {
    "document_sections_seen": [],
    "possibly_missing_sections": [],
    "confidence_in_coverage": "high | medium | low",
    "notes": ""
  },
  "entities": [
    {
      "name": "",
      "type": "",
      "description": "",
      "evidence": [
        {
          "source_id": "",
          "location": "",
          "quote": ""
        }
      ]
    }
  ],
  "concepts": [
    {
      "name": "",
      "definition": "",
      "related_entities": [],
      "evidence": [
        {
          "source_id": "",
          "location": ""
        }
      ]
    }
  ],
  "topics": [
    {
      "name": "",
      "description": "",
      "related_concepts": []
    }
  ],
  "claims": [
    {
      "claim": "",
      "confidence": "high | medium | low",
      "source_id": "",
      "location": "",
      "citation_status": "model_reported | user_verified | verified | missing"
    }
  ],
  "relationships": [
    {
      "source": "",
      "source_type": "entity | concept | topic | source | wiki_page",
      "target": "",
      "target_type": "entity | concept | topic | source | wiki_page",
      "relation": "",
      "description": "",
      "evidence": {
        "source_id": "",
        "location": ""
      },
      "confidence": "high | medium | low"
    }
  ],
  "proposed_wiki_updates": [
    {
      "operation": "create | update | merge | skip",
      "page_type": "",
      "path": "",
      "title": "",
      "reason": "",
      "risk_level": "low | medium | high",
      "requires_review": true
    }
  ],
  "conflicts": [
    {
      "description": "",
      "new_claim": "",
      "existing_claim": "",
      "risk_level": "medium",
      "suggested_action": ""
    }
  ],
  "questions_for_user": []
}

Resolution Task 返回格式：

{
  "resolutions": [
    {
      "input_name": "",
      "input_type": "entity | concept | topic",
      "decision": "create_new | update_existing | append_to_existing | add_alias | merge_suggestion | skip | needs_user_review",
      "target_page": "",
      "new_page_path": "",
      "alias_to_add": "",
      "reason": "",
      "confidence": "high | medium | low",
      "requires_review": true
    }
  ],
  "relationships": [
    {
      "source_name": "",
      "source_page": "",
      "target_name": "",
      "target_page": "",
      "relation": "",
      "description": "",
      "evidence_source_id": "",
      "evidence_location": "",
      "confidence": "high | medium | low",
      "requires_review": true
    }
  ],
  "review_items": []
}

Wiki Update Task 返回格式：

{
  "wiki_update_plan": [
    {
      "path": "",
      "operation": "create | update | merge | delete_suggestion | skip",
      "title": "",
      "summary": "",
      "content_blocks": {},
      "new_markdown": "",
      "patch_mode": "full_replace | replace_section | append_section | create_new",
      "target_section": "",
      "citations": [
        {
          "source_id": "",
          "location": "",
          "citation_status": "model_reported"
        }
      ],
      "risk_level": "low | medium | high",
      "requires_review": true,
      "reason": ""
    }
  ],
  "index_updates": [],
  "log_entry": "",
  "graph_updates": [],
  "review_notes": []
}

Query Task 返回格式：

{
  "answer": "",
  "citations": [
    {
      "source_type": "wiki | source",
      "path": "",
      "location": "",
      "quote": "",
      "citation_status": "model_reported | user_verified | verified | missing"
    }
  ],
  "related_pages": [],
  "suggested_follow_up_questions": [],
  "save_as_wiki_page": {
    "recommended": true,
    "suggested_title": "",
    "suggested_path": "",
    "reason": ""
  }
}

============================================================
十六、Prompt 设计要求
============================================================

所有模型任务都必须使用标准化 Prompt。

Prompt 必须告诉大模型以下定义：

Source：
用户上传的原始资料，是事实来源，不允许修改。

Entity：
具有明确指代对象的名词，例如人物、组织、论文、项目、产品、数据集、方法名称、地点、软件库。

Concept：
抽象知识点、理论、机制、原则、术语、方法类别。

Topic：
更高层级的主题集合。

Claim：
可以被验证的事实性陈述，需要绑定来源。

Citation：
Claim 对应的来源位置，例如文件名、页码、章节、段落、图表编号。由于本系统不做本地 PDF 深度解析，模型报告的引用位置应标记为 model_reported。

Conflict：
新资料中的 Claim 与已有 Wiki 中的 Claim 不一致或存在张力。

Wiki Page：
系统需要创建或更新的 Markdown 页面。

Wiki Update Plan：
大模型建议本地系统执行的一组页面创建、修改、合并或跳过操作。

Alias：
同一实体或概念的等价名称、翻译、缩写或大小写变体。相关概念、上下位概念、实现方式、同主题概念不能作为 alias。

Relationship：
实体、概念、主题或 source 之间的标准化关系，必须使用给定 relation enum。

Prompt 分层：

System Prompt：不可随便修改，定义协议和输出要求。
Template Prompt：由知识库模板决定。
User Custom Instruction：用户偏好、语言、关注重点。
Task Prompt：运行时拼接上下文、候选页面、任务目标。

用户自定义 Prompt 不能破坏 JSON 输出协议。

============================================================
十七、数据库设计
============================================================

使用 SQLite。

必须包含以下表或等价结构：

knowledge_bases
- id
- name
- path
- template_name
- created_at
- updated_at

sources
- id
- kb_id
- file_name
- file_path
- file_type
- file_size
- file_hash
- status
- created_at
- updated_at

assets
- id
- kb_id
- file_name
- file_path
- file_type
- file_size
- file_hash
- created_at

wiki_pages
- id
- kb_id
- title
- path
- page_type
- canonical_name
- tags
- content_hash
- created_at
- updated_at

knowledge_items
- id
- kb_id
- canonical_name
- item_type
- page_path
- summary
- created_at
- updated_at

aliases
- id
- item_id
- alias
- normalized_alias
- language
- created_at

relationships
- id
- kb_id
- source_item_id
- target_item_id
- relation
- evidence_source_id
- evidence_location
- confidence
- status
- created_at

tasks
- id
- kb_id
- task_type
- status
- current_agent
- model_profile_id
- input_ref
- output_ref
- error_message
- retry_count
- created_at
- updated_at
- locked_at
- completed_at

task_events
- id
- task_id
- event_type
- agent_name
- message
- created_at

reviews
- id
- kb_id
- task_id
- status
- summary
- risk_level
- created_at
- updated_at

review_items
- id
- review_id
- operation
- target_path
- base_version_hash
- old_content
- new_content
- status
- risk_level
- created_at
- updated_at

versions
- id
- kb_id
- page_path
- content_hash
- snapshot_path
- task_id
- operation_id
- created_at

operations
- id
- kb_id
- task_id
- operation_hash
- target_path
- status
- applied_at

graph_nodes
- id
- kb_id
- node_type
- label
- path
- metadata

graph_edges
- id
- kb_id
- source_node_id
- target_node_id
- edge_type
- metadata

model_profiles
- id
- provider
- name
- base_url
- model_name
- encrypted_api_key_ref
- role
- temperature
- max_tokens
- timeout
- retry_count
- created_at
- updated_at

schema_migrations
- version
- applied_at

数据库必须支持 migration system，并在迁移前备份。

============================================================
十八、审阅与风险等级
============================================================

支持三种维护模式：

Strict Mode：
所有修改都需要审阅。

Balanced Mode：
默认推荐。低风险新增和 alias / related link 可自动应用；已有页面正文修改、冲突、合并需要审阅。

Auto Mode：
低风险和中风险自动应用；高风险必须审阅；所有修改保留版本快照，可回滚。

风险等级：

低风险：
- 新增 source summary
- 添加 alias
- 添加 related link
- 追加 log
- 新建无冲突页面
- 新增标签

中风险：
- 修改已有页面 section
- 调整页面结构
- 添加上位/下位关系
- 将问答保存为页面

高风险：
- 合并页面
- 删除页面
- 覆盖核心定义
- 修改 canonical_name
- 标记 contradiction
- 重命名页面
- 覆盖旧结论

审阅中心必须支持：

1. 按任务分组
2. 按页面分组
3. 按风险等级分组
4. 高风险置顶
5. 默认折叠低风险
6. 一键接受低风险
7. 逐条接受
8. 全部拒绝
9. 编辑后接受
10. 要求重新生成
11. 标记为以后处理
12. 查看原始 Prompt
13. 查看模型原始输出
14. 查看引用来源
15. 查看版本快照
16. 回滚

每个 AI 修改必须回答四个问题：

改了什么？
为什么改？
基于哪个 source？
能否撤销？

============================================================
十九、UI 设计基准
============================================================

UI 必须严格参考我们确定的“LLM Knowledge Wiki · 全页面打开状态总览”设计图。

整体风格：

1. 简洁
2. 浅色
3. 蓝白灰
4. 低饱和
5. 桌面端工作台
6. 信息密度适中
7. 卡片化
8. 专业但不冰冷
9. 接近 Chatbox / Cherry Studio / Notion / Obsidian / Linear 的结合体

不要做成：

1. 炫酷科技风
2. 游戏风
3. 深色赛博风
4. 复杂大屏仪表盘
5. 强渐变
6. 过大阴影
7. 复杂背景纹理
8. 夸张动画

主界面固定采用：

左侧导航栏 + 中间主工作区 + 右侧上下文面板 + 底部状态栏

需要实现的页面：

1. 主界面 / Wiki 工作台
2. Chat 问答页面
3. 导入任务页面
4. 审阅 / Diff 页面
5. 知识图谱页面
6. 搜索页面
7. 模型配置 / 设置页面
8. 新建知识库 / 选择模板页面
9. Source 原始文件阅读页面
10. 页面历史 / 版本记录页面
11. 首页 Dashboard
12. 初始化向导
13. Health Check 页面
14. 任务详情页面
15. Prompt / Agent 设置页面

主界面必须以 Wiki 页面阅读和维护为中心，而不是以 Chat 为中心。

Chat 是功能模块之一，不是软件全部。

左侧导航栏包含：

- 当前知识库名称
- 首页
- Wiki 页面
- Chat 问答
- 文件 / Sources
- 导入队列
- 审阅中心
- 搜索
- 知识图谱
- 模板
- 健康检查
- 设置

底部状态栏显示：

- 当前模型
- 当前任务状态
- 当前知识库路径
- 最近保存时间
- Token 使用估算
- DeepSeek 连接状态
- 本地存储状态

右侧上下文面板显示：

- 来源引用
- 相关页面
- 反向链接
- 页面元数据
- AI 建议
- 待审阅修改
- 冲突提示
- 当前页面操作按钮

============================================================
二十、核心页面功能
============================================================

1. 首页 Dashboard

显示：
- 当前知识库概览
- Wiki 页面数量
- Source 数量
- 待审阅修改数量
- 最近导入文件
- 最近更新页面
- 最近问答
- AI 建议
- 快捷操作按钮

快捷操作：
- 上传文档
- 新建 Wiki 页面
- 开始问答
- 运行健康检查
- 打开设置

2. Wiki 工作台

功能：
- Markdown 渲染
- 编辑模式
- 预览模式
- 源码模式
- 双链跳转
- 标签显示
- YAML frontmatter 显示
- 页面元数据
- 来源引用
- 相关页面
- 反向链接
- AI 页面操作

AI 页面操作：
- 总结当前页面
- 优化结构
- 查找缺失引用
- 查找冲突
- 生成相关问题
- 基于当前页面提问
- 将当前页面扩展为长文
- 生成图谱关系

3. Chat 问答页面

功能：
- 用户输入问题
- 选择问答范围：
  - 整个知识库
  - 当前页面
  - 指定文件
  - 指定标签
  - 指定目录
- 回答带引用
- 展示本次使用的上下文
- 支持流式输出
- 显示当前使用模型
- 显示 token usage
- 回答后支持：
  - 保存为 Wiki 页面
  - 追加到当前页面
  - 生成更新建议
  - 生成对比表
  - 生成综述
  - 生成时间线
  - 加入 log.md

4. Source 页面

显示：
- 文件名
- 类型
- 大小
- 上传时间
- source_id
- 文件 hash
- 导入状态
- 关联 Wiki 页面数量
- 操作按钮

操作：
- 上传文件
- 批量上传
- 打开原始文件
- 重新导入
- 查看 Source Summary
- 查看关联页面
- 删除 Source
- 复制引用路径

5. 导入任务页面

不是传统百分比进度条，而是 Agent Activity Timeline。

文档导入事件示例：
- 已保存原始文件
- 已生成 source_id
- 已构建知识抽取 Prompt
- 已上传附件到 DeepSeek
- 等待模型返回结构化 JSON
- 已收到模型结果
- 正在校验 JSON Schema
- 正在执行 Candidate Search
- ResolutionAgent 正在消歧
- RelationshipAgent 正在建立关系
- WikiUpdateAgent 正在生成更新计划
- ReviewAgent 已生成审阅项
- 等待用户审阅

图片导入事件示例：
- 已保存图片资产
- 已生成 asset_id
- 当前系统不进行图片理解
- 当前系统不进行 OCR
- 图片可在 Wiki 页面中手动引用

6. 审阅 / Diff 页面

左右对比：
左侧：当前 Wiki 页面
右侧：AI 建议修改后页面

高亮：
- 新增内容
- 删除内容
- 修改内容
- 引用变化
- 可能冲突内容

操作：
- 全部接受
- 全部拒绝
- 逐条接受
- 编辑后接受
- 要求重新生成
- 标记以后处理
- 查看 Prompt
- 查看模型原始输出
- 查看来源
- 回滚

7. 搜索页面

第一阶段以本地全文搜索为主，不强制向量数据库。

搜索范围：
- Wiki 页面
- Source Summary
- log.md
- index.md
- 页面标题
- 标签
- frontmatter
- aliases

搜索结果显示：
- 标题
- 路径
- 摘要片段
- 页面类型
- 更新时间
- 标签
- 匹配字段

8. 知识图谱页面

节点类型：
- source
- concept
- entity
- topic
- question
- wiki_page

边类型：
- links_to
- cites
- mentions
- related_to
- updates
- conflicts_with
- uses
- is_a
- part_of

功能：
- 查看节点关系
- 点击节点打开页面
- 筛选节点类型
- 筛选边类型
- 显示孤立页面
- 显示高连接页面
- 显示关系 evidence
- 控制 related_to 噪声

9. 设置页面

包含：

A. 模型设置
- DeepSeek API Key
- Base URL
- 模型名称
- 默认聊天模型
- 默认文档分析模型
- 默认 Wiki 写作模型
- 默认冲突检测模型
- Temperature
- Max Tokens
- Timeout
- Retry
- 流式输出
- 测试连接
- 附件能力测试

B. 知识库设置
- 当前知识库路径
- 默认语言
- 当前模板
- 审阅模式
- 自动保存
- 数据导出
- 缓存清理

C. Agent / Prompt 设置
- 查看系统 Prompt
- 查看模板 Prompt
- 编辑用户自定义 Prompt
- 恢复默认 Prompt
- 导入 Prompt
- 导出 Prompt

D. 外观设置
- 浅色 / 深色
- 字体大小
- 编辑器字号
- 紧凑模式
- 侧边栏宽度

E. 关于
- 版本号
- 本地数据路径
- 开源许可
- 日志目录

设置页分为：
基础模式
高级模式
开发者模式

基础模式只显示必要配置，避免吓到用户。

10. 初始化向导

第一次启动显示：

Step 1：欢迎页面
说明这是 LLM 驱动的 Wiki 维护工作台。

Step 2：选择数据目录
默认使用用户文档目录下的 LLM Knowledge Wiki。

Step 3：配置 DeepSeek
输入 API Key、Base URL、测试连接、测试附件能力。

Step 4：创建第一个知识库
输入知识库名称。

Step 5：选择知识库模板
通用知识库 / 科研论文知识库 / 课程学习知识库 / 项目管理知识库 / 读书笔记知识库。

Step 6：完成
进入工作台首页。

============================================================
二十一、ModelGateway 要求
============================================================

所有模型调用必须经过统一 ModelGateway。

ModelGateway 负责：

1. API Key 注入
2. Base URL 管理
3. 模型选择
4. 任务类型到模型路由
5. 超时
6. 自动重试
7. 指数退避
8. 限流处理
9. 错误归一化
10. 流式输出
11. 附件上传
12. 原始响应保存
13. JSON 修复
14. Token 统计或估算
15. 用户可读错误提示

必须处理错误：

timeout
429
5xx
余额不足
API Key 无效
附件过大
上下文过长
模型暂不可用
网络断开
JSON 非法
文件上传失败

错误提示必须明确，不要只显示“请求失败”。

============================================================
二十二、成本与 token usage
============================================================

任务、Agent 和模型调用需要记录 token usage 或预估 usage。

UI 中显示：

- 本次任务预估 token
- 本次任务实际 token
- 当前 Agent 使用量
- 当前任务总使用量
- 今日使用量
- 当前知识库累计使用量

如果 DeepSeek API 返回 usage 信息，优先使用 API 返回值。
如果没有，使用本地估算。

============================================================
二十三、Workspace Reconcile 与 Health Check
============================================================

必须实现 Workspace Reconcile Service。

用于检查：

- SQLite 中记录的页面是否存在
- Markdown 文件是否有数据库记录
- index.md 是否包含所有页面
- log.md 是否缺失关键操作
- versions 是否完整
- raw sources 是否存在
- source hash 是否匹配
- aliases 是否重复
- relationships 是否指向不存在页面
- orphan pages
- missing citations
- unresolved conflicts

HealthCheckAgent 生成报告，不直接修改。

用户确认后再生成维护任务。

============================================================
二十四、安装与 Windows 兼容性
============================================================

必须考虑：

1. 中文路径
2. 空格路径
3. OneDrive 同步路径
4. 无管理员权限
5. 文件被占用
6. 路径过长
7. 杀毒软件拦截
8. 安装目录无写入权限
9. AppData 权限问题
10. 用户文档目录不存在或不可写

用户数据不能存安装目录。

如果路径不可写，应提示用户选择其他目录。

============================================================
二十五、日志与隐私安全
============================================================

必须有：

1. 应用日志
2. 任务日志
3. 模型调用日志
4. 错误日志
5. 审阅日志
6. 版本日志

日志必须脱敏：

1. API Key
2. Authorization Header
3. 用户敏感路径可部分隐藏
4. 不在日志中记录完整密钥
5. Prompt Snapshot 中不得包含 API Key

API Key 必须使用系统凭据存储。

============================================================
二十六、代码结构建议
============================================================

项目结构建议：

project/
  src/
    app/
    components/
      layout/
      sidebar/
      editor/
      chat/
      review/
      settings/
      graph/
      source/
      task/
      onboarding/
    pages/
      DashboardPage.tsx
      ChatPage.tsx
      WikiPage.tsx
      SourcesPage.tsx
      ImportTasksPage.tsx
      ReviewPage.tsx
      SearchPage.tsx
      GraphPage.tsx
      SettingsPage.tsx
      OnboardingPage.tsx
      HealthCheckPage.tsx
      TaskDetailPage.tsx
    stores/
      useAppStore.ts
      useKnowledgeBaseStore.ts
      useTaskStore.ts
      useModelStore.ts
      useReviewStore.ts
    services/
      api/
      events/
      ui/
    types/
      model.ts
      wiki.ts
      task.ts
      review.ts
      source.ts
      template.ts
      graph.ts
    utils/
  src-tauri/
    src/
      main.rs
      app_kernel.rs
      commands/
        config.rs
        workspace.rs
        task.rs
        model.rs
        wiki.rs
        review.rs
        graph.rs
        search.rs
      core/
        config_service.rs
        secret_service.rs
        workspace_service.rs
        database_service.rs
        task_queue.rs
        event_bus.rs
      agents/
        mod.rs
        coordinator.rs
        source_ingest.rs
        resolution.rs
        relationship.rs
        wiki_update.rs
        review.rs
        query.rs
        health_check.rs
      model/
        model_gateway.rs
        deepseek_client.rs
      prompts/
        prompt_registry.rs
        prompt_builder.rs
      wiki/
        wiki_writer.rs
        version_manager.rs
        index_service.rs
        log_service.rs
        markdown_template.rs
      review/
        review_engine.rs
        diff_engine.rs
      search/
        candidate_search.rs
        full_text_search.rs
      graph/
        graph_service.rs
      schema/
        json_schema_validator.rs
      recovery/
        recovery_check.rs
        workspace_reconcile.rs
      db/
        schema.rs
        migrations.rs
    tauri.conf.json

============================================================
二十七、开发实施顺序
============================================================

请不要用“第一版/第二版/MVP”的话术开发。  
请采用“正式架构优先，功能逐步落地”的方式。

开发顺序：

1. 搭建 Tauri 2.x + React + TypeScript + Rust 正式工程。
2. 搭建 UI 主题、布局、路由和基础组件。
3. 实现 Rust AppKernel。
4. 实现 SQLite 数据库与 migration。
5. 实现 WorkspaceService 和本地目录初始化。
6. 实现 SecretService，安全保存 DeepSeek API Key。
7. 实现 ModelGateway 和 DeepSeekClient。
8. 实现模型连接测试和附件能力测试。
9. 实现 TaskQueue、task_events、EventBus。
10. 实现 AgentRuntime 和 CoordinatorAgent。
11. 实现 PromptRegistry 和 PromptBuilder。
12. 实现 JSON Schema Validator。
13. 实现 Source 文件上传、hash、source_id、asset_id。
14. 实现 SourceIngestAgent。
15. 实现 CandidateSearchEngine。
16. 实现 ResolutionAgent。
17. 实现 RelationshipAgent。
18. 实现 WikiUpdateAgent。
19. 实现 ReviewEngine 和 DiffEngine。
20. 实现 Review 页面。
21. 实现 WikiWriter。
22. 实现 VersionManager。
23. 实现 IndexService 和 LogService。
24. 实现 Wiki 页面浏览、编辑、预览。
25. 实现 Chat / QueryAgent。
26. 实现回答保存为 Wiki 页面。
27. 实现 Search 页面。
28. 实现 Graph 页面。
29. 实现 HealthCheckAgent 和 Workspace Reconcile。
30. 实现初始化向导。
31. 实现设置页面。
32. 完成 Windows 安装包。
33. 在干净 Windows 环境测试安装运行。
34. 输出 README、用户手册和开发文档。

============================================================
二十八、最终验收标准
============================================================

软件完成后必须满足：

1. 用户可以下载 Windows 安装包。
2. 双击安装后可以直接打开软件。
3. 不需要安装 Node.js、Python、Rust、SQLite。
4. 第一次启动进入初始化向导。
5. 用户可以配置 DeepSeek API Key。
6. 软件可以测试 DeepSeek 连接。
7. 软件可以测试文档附件能力。
8. 用户可以创建本地知识库。
9. 用户可以选择知识库模板。
10. 软件自动初始化 workspace 目录结构。
11. 用户可以上传 PDF / DOCX / Markdown / TXT / HTML。
12. 文档被保存到 raw/sources/documents/。
13. 软件生成 source_id 和 file_hash。
14. 软件构建 Prompt 并将文档附件提交给 DeepSeek。
15. DeepSeek 返回结构化 JSON。
16. 软件校验 JSON Schema。
17. 非法 JSON 可以进入 repair 流程。
18. SourceIngestAgent 输出 source_summary、entities、concepts、claims、relationships。
19. ResolutionAgent 执行去重与消歧。
20. RelationshipAgent 建立标准化关系。
21. WikiUpdateAgent 生成 wiki_update_plan。
22. ReviewAgent 生成审阅任务和 Diff。
23. 用户可以在审阅中心接受、拒绝、编辑后接受。
24. WikiWriter 写入 Markdown Wiki。
25. VersionManager 保存版本快照。
26. index.md 自动更新。
27. log.md 自动追加。
28. 用户可以浏览、编辑、预览 Wiki 页面。
29. 用户可以基于 Wiki 问答。
30. 回答可以保存为 Wiki 页面。
31. 图片上传后只保存为 asset，不 OCR、不识别、不参与知识抽取。
32. 软件显示 Agent Activity Timeline。
33. 软件支持任务失败重试。
34. 软件关闭后可以恢复 interrupted 任务。
35. 软件防止重复应用 operation。
36. 软件防止覆盖用户手动编辑。
37. 软件支持页面版本查看和回滚。
38. 软件支持本地搜索。
39. 软件支持基础知识图谱。
40. 软件支持 Health Check 和 Workspace Reconcile。
41. 软件 UI 与已确认基准图风格一致。
42. 软件日志脱敏 API Key。
43. 软件可在普通 Windows 电脑上安装运行。
44. 软件数据保存在本地用户目录。
45. 卸载软件不默认删除用户知识库。

============================================================
二十九、参赛展示重点
============================================================

软件需要能够支持后续项目介绍、参赛展示和路演。

请在产品文档和 README 中突出以下创新点：

1. 从“文档问答”升级为“Wiki 自动维护”。
2. 从“一次性回答”升级为“长期知识资产沉淀”。
3. 从“单 Agent 聊天”升级为“本地多 Agent 协作式知识维护”。
4. 从“黑箱 AI 改写”升级为“结构化 JSON + Diff 审阅 + 版本回滚”。
5. 从“普通 RAG 检索”升级为“文档导入阶段的知识编译与 Wiki 更新”。
6. 从“聊天记录”升级为“可编辑、可追踪、可复用的 Markdown Wiki”。
7. 本地优先，用户数据保存在本地。
8. LLM 只生成计划，本地系统执行计划，保证稳定性和安全性。
9. 支持知识库模板，适配科研、课程、项目、读书、通用知识管理。
10. 适合学习、科研、项目管理、个人知识管理、团队文档整理等场景。

============================================================
三十、最终开发要求
============================================================

请你按照以上需求，直接构建完整可交付工程。

不要只给概念。
不要只给伪代码。
不要实现一个临时 Demo。
不要把已否决功能重新加入。
不要把 OCR、多模态、本地 PDF 深度解析作为开发内容。
不要做自由 Agent 群聊。
不要让 LLM 直接改 Wiki。
不要让前端承担核心状态机和写入逻辑。
不要忽略 Windows 安装包。

请优先确保：

1. 架构稳定
2. 本地运行可靠
3. 安装体验完整
4. Agent 调度可恢复
5. 模型输出可校验
6. Wiki 写入可回滚
7. 用户审阅可解释
8. UI 专业统一
9. 代码模块清晰
10. 后续可持续扩展

最终请交付：

1. 完整源代码
2. Windows 安装包
3. README
4. 用户使用说明
5. 开发者文档
6. 数据库 schema 文档
7. Agent 说明文档
8. Prompt 模板文档
9. JSON Schema 文档
10. 打包发布说明