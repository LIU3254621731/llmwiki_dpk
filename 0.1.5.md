你是一名资深 Tauri + React + Rust 桌面软件工程师，正在继续开发“智维 Wiki - LLM 知识维护工作台”。当前软件已经完成基础 UI、知识库切换、Source 文件上传、导入任务、Agent Timeline、审阅中心、健康检查、知识图谱入口、文件管理 Source 视图等功能。

根据当前运行截图，系统已经可以上传文档、创建 source_ingest 任务、调用 SourceIngestAgent、生成部分审阅任务，并且健康检查可以发现知识项、图谱节点和数据一致性问题。但系统仍存在核心闭环问题：Wiki 页面数量仍为 0，knowledge_items 已存在但未关联到 wiki_pages，graph_nodes 有统计但画布仍未显示节点，Source 文件状态与任务状态不一致，旧失败任务污染首页健康状态，Source Preview / Markdown Preview / Workspace 文件树尚未真正完善。

本阶段目标是 v0.1.5-dev：修复任务状态、配置读取、审阅写入、Wiki 页面生成、知识项关联、图谱渲染、Source Preview 和文件树显示，使系统从“能生成任务和审阅”进入“能稳定生成本地 Wiki 知识资产”。

不要推倒重写，不要改变当前浅色蓝白灰桌面工作台 UI 风格。不要实现 OCR、多模态图片理解、本地 PDF 深度解析、云同步、多人协作、Web 端、移动端。继续保持本地优先、LLM 驱动、审阅后写入 Markdown Wiki 的产品边界。

============================================================
一、P0：修复 DeepSeek 配置读取与任务执行一致性
============================================================

当前任务中仍出现“DeepSeek API Key 未配置，请在设置中配置”，即使设置页已存在 API Key 配置入口。请修复配置读取链路。

要求：

1. 统一模型配置存储位置。
2. 设置页保存配置后，后端任务执行必须读取同一份配置。
3. 创建任务前检查：
   - API Key 是否存在；
   - Base URL 是否存在；
   - Chat 模型是否存在；
   - Reasoner 模型是否存在。
4. 如果 API Key 缺失，不允许创建 source_ingest 任务，应在上传后提示“请先配置模型”。
5. 如果任务创建后配置改变，重试任务时必须重新加载最新配置。
6. 任务详情中显示本次任务使用的：
   - provider；
   - base_url；
   - chat_model；
   - reasoner_model；
   - config_status；
   - created_with_config_id。
7. 设置页增加：
   - 测试连接结果；
   - 测试 JSON 输出结果；
   - 测试文档附件调用；
   - 最近一次成功时间。
8. 修复旧任务：对于因 API Key 缺失失败的任务，配置完成后可点击“重新运行”。

验收：
配置 API Key 后，新任务不再出现 API Key 未配置；旧失败任务点击重试后能使用新配置执行。

============================================================
二、P0：修复任务状态机和任务污染问题
============================================================

当前导入任务出现 queued、interrupted、failed、cancelled、review_generating 等状态，但状态解释不清晰，旧失败任务长期污染首页。

请实现清晰的任务状态机。

任务状态建议：

queued
running
sent_to_model
model_returned
json_validating
review_generating
review_pending
partially_applied
applied
applied_with_warnings
failed
cancelled
interrupted
archived

每个任务增加字段：

failure_reason
recoverable
resume_from_stage
last_success_stage
next_action
archived_at
handled_at

要求：

1. interrupted 必须说明中断原因。
2. failed 必须说明失败原因。
3. cancelled 不应算作失败任务。
4. 已处理的 failed 可以标记 handled，不再影响首页“失败任务”统计。
5. 支持归档任务。
6. 任务列表支持筛选：
   - 全部；
   - 运行中；
   - 待审阅；
   - 失败；
   - 已取消；
   - 已归档。
7. 首页失败任务只统计未处理 failed。
8. 首页推荐下一步应优先显示：
   - 待审阅；
   - 未处理失败任务；
   - 运行中任务。
9. 对 PDF 扫描件失败，显示建议：
   - 上传可复制文本 PDF；
   - 上传 DOCX/MD/TXT；
   - 使用外部 OCR 后再导入；
   - 可作为 asset 保存但不进入知识抽取。

验收：
旧 cancelled 任务不再影响失败任务数量；失败任务可标记已处理或归档；首页推荐下一步更准确。

============================================================
三、P0：修复 Review 接受后生成正式 Wiki 页面
============================================================

当前系统有 Review Items，但首页 Wiki 页面仍为 0。请确保审阅接受后真正生成 Wiki 页面。

接受 Review Item 的流程必须为：

1. 读取 review_item。
2. 判断 operation_type：
   - create_page；
   - update_page；
   - append_section；
   - add_alias；
   - add_relation；
   - skip；
   - merge_suggestion。
3. 如果 operation_type 是 skip，不应显示普通“接受写入”，应显示“确认跳过”。
4. 如果是 merge_suggestion，应进入人工合并流程，不直接写入。
5. 如果是 create/update/append，调用 WikiWriter。
6. WikiWriter 必须：
   - 生成 Markdown；
   - 创建父目录；
   - 写入 workspace/wiki/.../*.md；
   - 更新 wiki_pages；
   - 更新 knowledge_items.page_id；
   - 更新 aliases；
   - 更新 relationships；
   - 更新 graph_nodes；
   - 创建 version snapshot；
   - 更新 index.md；
   - 追加 log.md。
7. 任一步失败，review_item.status = apply_failed，并显示错误。
8. 成功后 review_item.status = applied。
9. 刷新：
   - Dashboard；
   - Wiki 页面；
   - Source 关联页面数；
   - Graph；
   - Health Check。

验收：
用户接受一个 create_page / update_page 审阅项后，首页 Wiki 页面数量增加，Wiki 页面列表出现页面，点击可打开。

============================================================
四、P0：修复 knowledge_items 未关联 wiki_pages 的问题
============================================================

健康检查显示“发现 27 个未关联到页面的知识项”。请实现知识项到 Wiki 页面关联流程。

要求：

1. knowledge_items 必须有 page_id 或 linked_page_path 字段。
2. 当 Review 生成页面时，应提前记录 candidate_page_path。
3. 当 Review 被接受并写入页面后：
   - knowledge_items.page_id 更新为对应 wiki_pages.id；
   - graph_nodes.page_id 更新；
   - Source 关联页面数更新。
4. 健康检查提供操作：
   - 从未关联知识项生成 Review 草稿；
   - 关联到已有 Wiki 页面；
   - 忽略该知识项；
   - 删除孤立知识项。
5. 不允许直接把所有 knowledge_items 自动变成正式页面，必须走 Review。
6. 如果用户选择“一键生成草稿”，生成 pending Review Items，而不是直接写入 Wiki。

验收：
接受审阅生成页面后，对应 knowledge_item 不再显示为“未关联页面”。

============================================================
五、P0：修复 Wiki 页面为 0 与 Markdown 索引同步问题
============================================================

当前“从 Markdown 重建 Wiki 索引”显示新增 0、更新 0、跳过 3，但 Wiki 页面仍为 0。请增强该功能。

要求：

1. 扫描 workspace/wiki/**/*.md。
2. 明确列出：
   - 扫描总数；
   - 新增数量；
   - 更新数量；
   - 跳过数量；
   - 跳过原因。
3. 对 index.md、log.md、overview.md 这类系统文件，显示为 system_file_skipped。
4. 对缺少 frontmatter 但有 H1 的文件，也应能创建 wiki_pages。
5. 对缺少 frontmatter 和 H1 的文件，标记为 invalid_markdown。
6. 从 Markdown 建立 wiki_pages 时：
   - title 优先 frontmatter.title；
   - 其次第一个 H1；
   - 其次文件名；
   - page_type 优先 frontmatter.type；
   - 其次根据路径推断。
7. 如果扫描到 0 个有效页面，应明确提示：
   “当前 wiki 目录没有可索引的普通 Wiki 页面。”
8. 提供“打开 wiki 文件夹”按钮，方便用户验证。

验收：
如果 wiki 目录中存在普通 md 页面，重建索引后 Wiki 页面列表能显示；如果没有普通页面，提示原因清楚。

============================================================
六、P0：修复知识图谱有节点统计但画布空白的问题
============================================================

当前知识图谱显示 27 节点、0 边、27 警告，但画布仍为空白。请修复图谱渲染。

要求：

1. graph_nodes > 0 时，画布必须显示节点。
2. graph_edges = 0 时，仍显示孤立节点。
3. 思维导图模式下，如果没有边：
   - 以当前知识库为中心节点；
   - 按 node_type 分支；
   - 将孤立节点挂到对应类型分支下。
4. 支持节点类型：
   - entity；
   - concept；
   - topic；
   - person；
   - source；
   - wikipage；
   - question；
   - dataset；
   - method。
5. 画布右上角统计：
   - 节点数；
   - 边数；
   - 孤立节点；
   - 警告数。
6. 点击节点显示详情：
   - title；
   - type；
   - confidence；
   - source_id；
   - page_id；
   - 打开 Wiki；
   - 设为中心；
   - 在 Chat 中询问。
7. 如果节点没有 page_id，显示“未关联页面”，并提供“生成 Wiki 草稿”操作。
8. “从知识项重建图谱”必须真实写入 graph_nodes，并刷新画布。

验收：
当前 27 个节点应至少以思维导图分类节点或孤立节点形式显示出来，不能只显示空白提示。

============================================================
七、P1：完善 Source 文件管理为真正的 Source 管理中心
============================================================

当前文件管理已有 Source 视图和文件树入口，但 Source 视图信息仍过少。请完善。

Source 列表显示：

1. 文件名；
2. 类型；
3. 大小；
4. 状态；
5. 关联页面数；
6. 关联知识项数；
7. 关联 Review 数；
8. 最近任务；
9. 上传时间；
10. source_id；
11. file_hash。

Source 状态使用：

saved
queued
analyzing
analysis_failed
review_pending
partially_applied
applied
applied_with_warnings
asset_only

操作：

1. 查看 Source Preview；
2. 查看任务；
3. 查看审阅；
4. 查看关联知识项；
5. 查看关联 Wiki 页面；
6. 重新分析；
7. 作为新版本导入；
8. 打开所在文件夹；
9. 删除记录。

验收：
用户能从文件管理页看出一个文档当前处于“待处理 / 分析中 / 待审阅 / 已生成页面 / 失败”的哪一步。

============================================================
八、P1：实现 Source Preview 和 Markdown Preview
============================================================

当前截图中还没有看到 Source Preview。请实现或完善 Source Preview 页面。

要求：

1. 点击 Source 视图中的眼睛按钮进入 Source Preview。
2. Source Preview 顶部显示：
   - 文件名；
   - source_id；
   - 文件类型；
   - 状态；
   - 最近任务；
   - 重新分析按钮。
3. Tabs：
   - 原始信息；
   - Markdown 预览；
   - AI 摘要；
   - 抽取实体；
   - 抽取概念；
   - 关系；
   - 关联 Wiki 页面；
   - 任务日志。
4. DOCX：
   - 尽量提取普通文本生成 Markdown Preview；
   - 不追求复杂格式还原。
5. TXT / MD / HTML：
   - 支持 Markdown 渲染预览。
6. PDF：
   - 不做 OCR；
   - 不做深度解析；
   - 如果无文本层，显示明确提示和建议。
7. 图片：
   - asset_only；
   - 显示缩略图；
   - 不做 OCR，不做理解。
8. 预览文件保存到：
   .runtime/source_previews/{source_id}.md
9. Source Preview 不允许直接写入正式 Wiki，只用于预览和溯源。

验收：
点击 DOCX Source 可以看到 Markdown Preview 或至少看到提取失败原因；点击 PDF 扫描件能看到“不执行 OCR”的友好提示。

============================================================
九、P1：完善文件树视图
============================================================

当前文件管理已有“文件树”入口，但需要确保真实可用。

要求：

1. 文件树展示当前 workspace：
   - raw/
   - wiki/
   - versions/
   - .runtime/
   - schema/
   - config/
   - db/
2. 支持展开/折叠。
3. 点击文件显示预览：
   - md/json/txt 直接预览；
   - docx/pdf 显示基本信息和 Source Preview 入口；
   - image 显示缩略图。
4. 文件节点显示：
   - 是否被数据库记录；
   - 是否为 Source；
   - 是否为 Wiki Page；
   - 是否为 runtime artifact；
   - 是否异常。
5. 提供“重新扫描 workspace”。
6. 提供“复制相对路径”“打开所在文件夹”。

验收：
用户能在文件树中看到 raw/sources、wiki、runtime/tasks 下的真实文件。

============================================================
十、P1：优化 Dashboard 状态与推荐下一步
============================================================

当前首页已经能显示推荐下一步，但还需要更精确。

要求：

1. 首页健康状态必须综合：
   - 未处理失败任务；
   - 待审阅项；
   - 数据一致性问题；
   - graph_nodes 是否为空；
   - wiki_pages 是否为 0；
   - source 是否有未处理。
2. 如果有待审阅，推荐：
   “你有 X 个待审阅修改，建议前往审阅中心处理。”
3. 如果有失败任务，推荐：
   “你有 X 个失败任务，建议查看任务详情并重试。”
4. 如果 Wiki 页面为 0 但有知识项，推荐：
   “已有知识项但尚未生成 Wiki 页面，请处理审阅或生成页面草稿。”
5. 如果图谱节点有但边为 0，推荐：
   “当前图谱关系尚未建立，可运行关系重建。”
6. 首页统计卡片点击跳转对应页面。
7. 旧归档任务不影响首页问题数。

验收：
首页显示的问题数量与导入任务、审阅中心、健康检查一致。

============================================================
十一、P1：提升审阅中心可解释性
============================================================

当前审阅中心已经有列表，但需要优化：

1. 显示全部状态分布：
   - pending；
   - accepted；
   - rejected；
   - applied；
   - apply_failed；
   - skipped；
   - cancelled。
2. 对 operation_type=skip 的 item：
   - 显示“确认跳过”；
   - 不显示普通接受写入。
3. 对 merge_suggestion：
   - 显示“人工合并”；
   - 不允许自动接受低风险。
4. Review Item 显示：
   - target title；
   - target path；
   - operation type；
   - page type；
   - source_id；
   - evidence；
   - confidence；
   - risk；
   - AI 修改原因。
5. 查看 Diff 时显示：
   - 将创建/修改的 Markdown；
   - 接受后影响；
   - 是否会生成 Wiki 页面；
   - 是否会关联 knowledge_item。

验收：
用户可以明确知道每个审阅项接受后会发生什么。

============================================================
十二、保持产品边界
============================================================

不要实现：
1. OCR；
2. 多模态图片理解；
3. 图片 caption；
4. 本地 PDF 深度解析；
5. PDF 页面切片；
6. 表格视觉识别；
7. 公式 OCR；
8. 自由 Agent 群聊；
9. 云同步；
10. Web 端；
11. 移动端；
12. 多人协作。

扫描件 PDF 只能提示用户使用外部 OCR 或上传文本型文档。

============================================================
十三、开发顺序建议
============================================================

请按以下顺序实现：

1. 修复模型配置读取与任务执行一致性。
2. 修复任务状态机和失败任务归档。
3. 修复 Review Apply → WikiWriter → wiki_pages。
4. 修复 knowledge_items 与 wiki_pages 关联。
5. 修复 Markdown 重建 Wiki 索引的跳过原因和索引逻辑。
6. 修复图谱节点渲染，显示孤立节点和思维导图分类。
7. 完善 Source 状态和 Source 管理字段。
8. 实现 Source Preview / Markdown Preview。
9. 实现 Workspace 文件树真实展示。
10. 优化 Dashboard 推荐下一步。
11. 优化审阅中心解释能力。
12. 全链路测试。

============================================================
十四、完整验收流程
============================================================

实现后请按以下流程测试：

1. 打开软件。
2. 配置 DeepSeek API Key。
3. 点击测试连接，确认成功。
4. 上传一个 DOCX。
5. Source 状态从 saved/queued 变为 analyzing。
6. 任务进入 running。
7. DeepSeek 调用成功。
8. 生成 Review Items。
9. 首页显示待审阅数量。
10. 审阅中心接受一个 create_page item。
11. WikiWriter 写入 Markdown。
12. 首页 Wiki 页面数量增加。
13. Wiki 页面列表显示新页面。
14. Source 关联页面数变为 1 或更多。
15. knowledge_item 不再是孤立未关联。
16. 重建图谱后，图谱画布显示节点。
17. 图谱节点可点击查看详情。
18. Source Preview 可打开并显示 Markdown 预览或清晰错误。
19. 文件树能看到 raw/wiki/runtime 文件。
20. 健康检查问题数减少。
21. 失败任务可归档，不再污染首页。

============================================================
十五、输出要求
============================================================

完成后请输出：

1. 修复的问题清单；
2. 修改的文件列表；
3. 数据库 migration；
4. 任务状态机说明；
5. Review Apply 流程说明；
6. WikiWriter 写入流程说明；
7. Source Preview 实现说明；
8. Graph 渲染说明；
9. 手动测试流程；
10. 当前仍存在的风险。

你是一名资深 Tauri + React + Rust 桌面软件工程师。当前项目“智维 Wiki - LLM 知识维护工作台”已经实现文件管理页面的“文件树”视图，可以扫描 workspace 并展示 .runtime、tasks、config、db、drafts、raw 等目录，也可以选中文件并显示文件名、类型、大小、Hash、相对路径、创建时间、修改时间等基础元信息。

但是当前存在一个明显缺陷：点击 .md 文件，例如 .runtime/tasks/task_xxx/prompt_0.md 后，页面只显示基本信息和时间，没有显示 Markdown 正文内容，也没有 Markdown 渲染预览、源码预览、JSON 格式化预览等能力。

本轮目标：补齐 Workspace File Preview 能力。文件树中点击任意可预览文件时，中间主区域应显示文件内容预览，而不是只显示基本信息。

注意：
1. Workspace File Preview 用于预览 workspace 内部任意文件，例如 prompt_0.md、ingest_result.json、update_plan.json、relationship_result.json、log.md、wiki/*.md。
2. Source Preview 用于用户上传的原始文件，例如 docx、pdf、txt、md、html、图片。
3. 两者可以共用底层读取能力，但 UI 入口和语义要区分。
4. 不要实现 OCR、多模态图片理解、本地 PDF 深度解析。
5. 不要把预览内容直接写入正式 Wiki 页面。

============================================================
一、后端新增 WorkspaceFilePreviewService
============================================================

请新增或完善后端服务：

WorkspaceFilePreviewService

核心接口建议：

1. get_workspace_file_preview(kb_id, relative_path) -> FilePreviewResponse

输入：
- kb_id
- relative_path，必须是 workspace-relative path，例如：
  .runtime/tasks/task_20260507165645760/prompt_0.md
  .runtime/tasks/task_xxx/ingest_result.json
  wiki/concepts/rppg.md
  config/kb.config.json

输出 FilePreviewResponse：

{
  "relative_path": "...",
  "file_name": "...",
  "extension": "md",
  "size": 37120,
  "hash": "...",
  "modified_at": "...",
  "preview_type": "markdown | json | text | image | binary | unsupported | source_redirect",
  "content": "...",
  "render_hint": {
    "can_render_markdown": true,
    "can_show_source": true,
    "can_format_json": false,
    "is_large_file": false,
    "truncated": false
  },
  "error": null
}

2. 安全路径校验

必须防止路径穿越：

- 不允许 absolute path 直接传入；
- 不允许 ../；
- 不允许跳出 workspace_root；
- 所有路径通过 PathService.resolve_workspace_path(kb_id, relative_path)。

3. 文件大小限制

为了避免大文件卡死：
- 默认最大直接预览大小 2 MB；
- 2 MB 以上显示“文件过大，是否打开原始文件或外部编辑器”；
- 对 JSON/Markdown/TXT 可允许用户点击“强制加载前 N KB”。

4. 支持预览类型

.md / .markdown：
- preview_type = markdown
- content = 原始 Markdown 文本
- 前端负责渲染 Markdown 和源码切换

.json：
- preview_type = json
- content = 原始 JSON 字符串
- 后端可校验是否 valid JSON
- 前端格式化展示

.txt / .log：
- preview_type = text

.yaml / .yml / .toml / .rs / .ts / .tsx / .js / .css / .html：
- preview_type = text 或 code
- 保留源码显示

.png / .jpg / .jpeg / .webp / .gif：
- preview_type = image
- 返回可供前端加载的安全文件 URL 或 base64 thumbnail
- 不做图片理解

.docx / .pdf：
- 如果该文件是 Source 文件，返回 preview_type = source_redirect，并提供 source_id；
- UI 提示“请在 Source Preview 中查看原始文件预览和 AI 摘要”；
- 不在普通文件预览中做复杂解析

其他文件：
- preview_type = unsupported
- 仅显示元信息和操作按钮

验收：
后端能够读取 prompt_0.md 并返回 markdown content。

============================================================
二、前端文件树点击后加载内容预览
============================================================

当前文件树点击文件后只显示“基本信息”和“时间”。请改为：

左侧：文件树
中间：文件内容预览主区域
右侧：文件详情面板

中间主区域结构：

顶部：
- 文件名
- 相对路径
- 文件类型 badge
- 大小
- 操作按钮：
  - 刷新预览
  - 复制内容
  - 打开原始文件
  - 打开所在目录

Tabs：
1. 预览
2. 源码
3. 元信息

不同 preview_type 的渲染规则：

1. markdown
   - 默认显示“预览”Tab：渲染后的 Markdown
   - “源码”Tab：原始 Markdown
   - 支持标题、列表、表格、代码块、引用、链接
   - prompt_0.md 应能正常显示完整 Prompt 内容

2. json
   - 默认显示格式化 JSON
   - 支持折叠/展开
   - 提供“复制 JSON”
   - 如果 JSON 无效，显示原始文本和错误信息

3. text / code
   - 使用等宽字体显示
   - 保留换行
   - 支持复制

4. image
   - 显示图片预览
   - 右侧显示图片元信息
   - 不做 OCR，不做理解

5. source_redirect
   - 显示提示：
     “该文件是原始 Source 文件，请在 Source Preview 中查看。”
   - 提供按钮：
     “打开 Source Preview”

6. unsupported / binary
   - 显示：
     “该文件类型暂不支持内置预览”
   - 提供：
     打开原始文件
     打开所在目录
     复制相对路径

右侧文件详情面板保留：
- 文件名
- 类型
- 大小
- Hash
- 相对路径
- 记录类型
- 创建时间
- 修改时间
- 操作

验收：
点击 .runtime/tasks/.../prompt_0.md 后，中间区域不再只显示基本信息，而是显示 Markdown 渲染内容和源码 Tab。

============================================================
三、Markdown 渲染组件
============================================================

请新增或复用 MarkdownRenderer 组件。

要求：

1. 支持 GitHub Flavored Markdown：
   - 标题
   - 粗体/斜体
   - 列表
   - 表格
   - 代码块
   - 引用
   - 链接
   - 分割线

2. 支持代码块样式，但不要引入过重依赖。

3. 对 prompt_0.md 这种长文档：
   - 支持滚动
   - 不要撑爆页面
   - 中间预览区域高度自适应

4. 对 Wiki 页面 Markdown：
   - 以后可以复用该组件
   - frontmatter 是否隐藏由调用方决定

5. 对任务中间文件 prompt_0.md：
   - 不隐藏 frontmatter
   - 按普通 Markdown 显示

验收：
prompt_0.md 能以 Markdown 格式阅读，并可切换源码。

============================================================
四、JSON 预览组件
============================================================

任务中间文件中有：
- ingest_result.json
- resolution_result.json
- relationship_result.json
- update_plan.json

请新增 JsonPreview 组件。

要求：

1. 格式化 JSON。
2. 支持复制。
3. 支持显示 key 层级。
4. JSON 无效时显示错误和原始文本。
5. 大 JSON 支持滚动。
6. 不要让页面卡死。

验收：
点击 update_plan.json 能看到格式化 JSON，而不是只看到文件元信息。

============================================================
五、文件详情页状态优化
============================================================

当前选中文件时中间区域只有“基本信息”和“时间”，右侧也显示类似信息，重复且缺少核心内容。

请调整布局：

中间主区域：
- 负责内容预览。

右侧详情面板：
- 负责文件元信息和操作。

避免中间和右侧重复显示大量相同信息。

当没有选择文件时，显示空状态：

“请选择左侧文件查看预览。”

当选择目录时，显示目录内文件列表。

当选择文件时，显示对应预览。

============================================================
六、与 Source Preview 的关系
============================================================

如果文件树中点击的是 raw/sources/documents 下的原始上传文件：

1. 如果能找到 source_id，显示 Source 文件卡片。
2. 提供按钮“打开 Source Preview”。
3. 不要在 Workspace File Preview 中强行解析 docx/pdf。
4. Source Preview 仍按原计划实现：
   - 原始信息
   - Markdown 预览
   - AI 摘要
   - 抽取实体
   - 抽取概念
   - 关系
   - 关联 Wiki 页面
   - 任务日志

本轮至少要让文件树中 md/json/txt 文件可预览。

============================================================
七、错误处理
============================================================

需要处理：

1. 文件不存在。
2. 路径非法。
3. 文件过大。
4. 文件读取失败。
5. 编码错误。
6. JSON 解析失败。
7. Markdown 渲染失败。
8. 图片加载失败。

错误提示要显示：
- 错误类型；
- 文件路径；
- 建议操作；
- 打开所在目录按钮。

============================================================
八、验收流程
============================================================

请按以下流程测试：

1. 进入文件管理。
2. 切换到“文件树”。
3. 展开 .runtime/tasks。
4. 选择 prompt_0.md。
5. 中间区域显示 Markdown 渲染内容。
6. 切换到“源码”，显示原始 Markdown。
7. 选择 ingest_result.json。
8. 中间区域显示格式化 JSON。
9. 选择 update_plan.json。
10. 中间区域显示格式化 JSON。
11. 选择 config/kb.config.json。
12. 中间区域显示格式化 JSON。
13. 选择普通目录。
14. 中间区域显示目录文件列表。
15. 选择 raw 中的 docx。
16. 显示“打开 Source Preview”按钮。
17. 点击“复制相对路径”可复制。
18. 点击“打开所在目录”可打开系统文件夹。

============================================================
九、输出要求
============================================================

完成后请输出：

1. 新增的后端接口；
2. 新增的前端组件；
3. 修改的页面；
4. 支持的预览类型；
5. 安全路径校验逻辑；
6. 文件大小限制；
7. 测试步骤；
8. 尚未支持的类型和原因。