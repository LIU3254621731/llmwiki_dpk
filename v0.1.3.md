你是一名资深 Tauri + React + Rust 桌面软件工程师。当前项目版本为 v0.1.2，已经完成基础 UI、知识库切换、文件上传、导入任务、Agent 时间线、任务中间文件、Wiki 页面列表、文件管理、审阅中心、搜索、知识图谱和健康检查等基础模块。

但是根据当前运行截图，软件仍存在核心闭环问题：AI 分析结果已经产生，但没有稳定、可靠、完整地转化为本地 Markdown Wiki 知识库。当前最重要目标不是继续新增大功能，而是修复知识库持久化闭环、数据一致性、审阅写入、图谱同步、问答搜索可用性和 Source 管理。

本次开发版本号为 v0.1.3。请在现有代码基础上修复和完善，不要推倒重写，不要改变现有浅色蓝白灰工作台 UI 风格，不要加入 OCR、多模态图片理解、本地 PDF 深度解析、图片 caption、自由 Agent 群聊、云同步、Web 端、移动端等已否决方向。

============================================================
一、v0.1.3 核心目标
============================================================

本版本必须打通并稳定以下主链路：

上传文档
→ 保存 Source
→ 创建 source_ingest 任务
→ Agent 生成 ingest_result / resolution_result / relationship_result / update_plan
→ Review 显示完整审阅信息和 Diff
→ 用户接受 Review Item
→ WikiWriter 真正写入 Markdown 文件
→ 更新 wiki_pages / knowledge_items / aliases / relationships / graph_nodes / graph_edges
→ 更新 index.md / log.md
→ 创建 version 快照
→ Wiki 页面可正常打开
→ Search 可搜索到页面
→ Graph 可显示节点，至少能显示孤立节点
→ Chat 可基于当前知识库查询，不再 SQL 参数错误
→ Health Check 一致性通过或提供修复动作

============================================================
二、P0：修复 Wiki 页面文件缺失与路径不一致
============================================================

当前健康检查显示大量“数据库记录的页面文件不存在”，但 Dashboard 显示 Wiki 页面 16，说明 wiki_pages 表与真实 Markdown 文件不一致。请优先修复。

要求：

1. 统一 Wiki 页面路径规范。
2. 数据库 wiki_pages.path 必须保存相对 workspace 的规范路径，例如：
   wiki/concepts/remote-photoplethysmography-rppg.md
   wiki/entities/openface.md
   wiki/datasets/pv3d.md
   wiki/sources/video-deception-detection-report.md
3. 不要保存 UI 展示字符串或混乱路径，例如：
   concept · wiki\xxx.md
   wiki\中文标题_(xxx).md
4. 内部统一使用正斜杠 `/` 作为相对路径分隔符。
5. 文件系统读写时再通过 PathService 转换为平台路径。
6. 实现统一 PathService / SlugService：
   - title → safe slug；
   - 去除 Windows 非法字符；
   - 处理空格、括号、斜杠、冒号；
   - 中文标题可使用拼音、转写或短 hash；
   - 文件重名时追加短 hash；
   - 根据 page_type 自动选择目录。
7. WikiWriter 创建页面前必须确保父目录存在。
8. 读取页面时必须使用 workspace_root + wiki_pages.path，不得直接使用显示路径。
9. 页面列表显示 title 和 relative_path，读取使用 normalized path。
10. 修复已有错误数据：实现 WorkspaceReconcile 修复流程，扫描 wiki_pages 表中缺失文件的记录，优先从 versions 快照恢复；如果无法恢复，标记为 broken，不要静默失败。
11. Wiki 页面缺失时，在 UI 显示“页面文件缺失”，并提供：
    - 从版本快照恢复；
    - 从 review item 重新应用；
    - 删除失效记录；
    - 标记为 broken。

验收：
点击 Wiki 页面列表中任意页面，不再出现 os error 3；如果文件缺失，UI 给出可操作修复入口。

============================================================
三、P0：修复 Review 接受后没有可靠写入 Markdown
============================================================

当前 Review 状态可能被标记 applied，但正式 Markdown 文件不存在。请修复审阅应用逻辑。

要求：

1. Review Item 点击接受后必须调用 WikiWriter。
2. WikiWriter 成功完成以下所有步骤后，才能把 review_item.status 设置为 applied：
   - 写入 Markdown 文件；
   - 更新 wiki_pages；
   - 更新 knowledge_items；
   - 更新 aliases；
   - 更新 relationships；
   - 更新 graph_nodes / graph_edges；
   - 更新 index.md；
   - 追加 log.md；
   - 创建 version 快照；
   - 记录 operation_id / operation_hash。
3. 如果任意一步失败，Review Item 不得标记 applied，应标记 apply_failed，并显示具体错误。
4. task 状态不要轻易设为 applied。应支持：
   - applied；
   - applied_with_warnings；
   - partially_applied；
   - failed_to_apply。
5. 每个 operation 必须幂等：
   - operation_id；
   - operation_hash；
   - target_path；
   - source_task_id；
   - applied_at。
6. 应用前检查：
   - operation_hash 是否已应用；
   - target_path 是否被锁定；
   - 当前页面 hash 是否等于 base_version_hash；
   - alias 是否已存在；
   - relationship 是否已存在。
7. 如果用户手动改过页面，不能直接应用旧 Diff，应提示重新生成 Diff。
8. 接受成功后自动刷新：
   - Dashboard；
   - Wiki 页面列表；
   - Review 页面；
   - Graph；
   - Health Check；
   - Sources 关联页面数量。

验收：
接受一个 create/update Review Item 后，真实 Markdown 文件出现在 workspace/wiki/... 下，Wiki 页面能打开，数据库与文件一致。

============================================================
四、P0：修复 Chat 问答 SQL 参数错误
============================================================

当前问答页面显示：
“映射失败: Wrong number of parameters passed to query. Got 0, needed 1”

请修复 QueryAgent / SearchService / Tauri command 中所有 SQL 参数绑定错误。

要求：

1. 所有按知识库查询的 SQL 必须传 current_kb_id。
2. 用户选择“整个知识库”时，也必须传 kb_id。
3. 搜索词为空时，使用 list_all 或空查询逻辑，不要调用需要参数的 SQL。
4. 修复 Rust command 参数映射，确保前端传参与后端函数签名一致。
5. 错误提示必须指出模块，例如：
   QueryAgent 检索上下文失败：缺少 kb_id。
6. Chat 流程应支持：
   - 选择范围：整个知识库 / 当前页面 / 指定文件 / 指定标签；
   - 读取相关 Wiki 页面；
   - 构造 Query Prompt；
   - 调用 DeepSeek；
   - 返回 answer + citations；
   - 显示使用的上下文；
   - 支持保存为 Wiki 页面。
7. 保存回答为 Wiki 页面必须走 Review → WikiWriter → VersionManager → index/log，不允许直接绕过。

验收：
在 Chat 输入“什么是 BOL？”不再报 SQL 参数错误，至少能基于本地 Wiki 页面返回答案或明确提示没有找到上下文。

============================================================
五、P0：修复 Search 搜索无结果
============================================================

当前搜索 bol 没有返回结果，但 Wiki 页面中存在 BOL。请实现真实本地搜索。

要求：

1. 搜索范围包括：
   - wiki_pages.title；
   - wiki_pages.path；
   - wiki_pages.canonical_name；
   - aliases.alias；
   - Markdown 正文；
   - frontmatter；
   - source_summary；
   - tags。
2. 至少实现标题、路径、正文、alias 的本地全文搜索。
3. 搜索结果显示：
   - 标题；
   - 页面类型；
   - 路径；
   - 摘要片段；
   - 匹配字段；
   - 更新时间；
   - 打开按钮。
4. 搜索应按当前知识库 kb_id 隔离。
5. 支持空状态：
   - 没有结果；
   - 数据索引为空；
   - 页面文件缺失。
6. 搜索到 broken 页面时要显示警告。

验收：
搜索 BOL 能返回 BOL 页面；搜索中文概念也能返回对应 Wiki 页面。

============================================================
六、P0：修复知识图谱无数据问题
============================================================

当前健康检查显示图谱节点数 16、图谱边数 0，但图谱页面仍显示暂无图谱数据。请修复图谱数据读取和显示逻辑。

要求：

1. graph_nodes > 0 且 graph_edges = 0 时，图谱页面必须显示孤立节点，而不是“暂无数据”。
2. graph_nodes = 0 但 knowledge_items > 0 时，显示“可从知识项重建图谱”，并提供按钮。
3. relationship_result.json 生成后必须写入 relationships 表。
4. relationships 写入后必须同步 graph_edges。
5. knowledge_items / wiki_pages / sources 应同步 graph_nodes。
6. 提供“重建图谱索引”按钮：
   - 从 sources 生成 source 节点；
   - 从 wiki_pages 生成 wiki_page 节点；
   - 从 knowledge_items 生成 concept/entity/topic 节点；
   - 从 relationships 生成 graph_edges。
7. 图谱页面至少显示：
   - 孤立节点；
   - 节点类型；
   - 节点标签；
   - 节点详情。
8. 如果边为 0，显示提示“当前有知识节点，但尚无关系边，可运行关系同步或重新分析文档”。

验收：
当前 16 个知识项至少应在图谱页面显示为 16 个孤立节点；不应显示完全空白。

============================================================
七、P1：完善文件管理为 Source 管理中心
============================================================

当前文件管理只是文件列表，需要升级为 Source 管理页面。

要求：

1. Source 列表显示：
   - 文件名；
   - 文件类型；
   - 文件大小；
   - source_id；
   - file_hash；
   - 上传时间；
   - 状态；
   - 关联 Wiki 页面数量；
   - 关联关系数量；
   - 最近任务 ID；
   - 分析状态。
2. 文件状态使用：
   - saved；
   - analyzing；
   - analyzed；
   - review_pending；
   - applied；
   - applied_with_warnings；
   - failed；
   - asset_only。
3. 操作按钮：
   - 打开原始文件；
   - 打开所在文件夹；
   - 查看 Source Preview；
   - 查看分析结果；
   - 查看关联 Wiki 页面；
   - 重新分析；
   - 作为新版本导入；
   - 复制 source_id；
   - 删除记录。
4. 相同 file_hash 上传时提示：
   - 跳过；
   - 重新分析；
   - 作为新版本导入。
5. 图片文件状态为 asset_only，不触发 AI 分析。
6. 删除 source 默认不删除原始文件，除非用户明确勾选。

验收：
用户能从文件管理页面看出一个文件贡献了哪些 Wiki 页面、任务和关系。

============================================================
八、P1：实现 Source Preview 统一预览页面
============================================================

需要一个统一 Source Preview 页面。注意：这不是 OCR，不是本地 PDF 深度解析，只是预览层。

允许实现：

1. Markdown：直接读取显示。
2. TXT：包装成 Markdown 显示。
3. HTML：轻量转 Markdown，提取正文。
4. DOCX：如果已有轻量库则提取普通文本；否则显示“暂不支持本地预览，可查看 AI 摘要”。
5. PDF：不做本地深度解析；显示文件元信息、AI source_summary、coverage_report、模型摘要。
6. 图片：显示资产信息和缩略图，不 OCR、不视觉理解。

Source Preview 页面 Tabs：

1. 原始信息；
2. Markdown 预览；
3. AI 摘要；
4. 抽取实体；
5. 抽取概念；
6. 关系；
7. 关联 Wiki 页面；
8. 任务日志。

缓存：

.runtime/source_previews/{source_id}.md

preview_status：

generated
unavailable
failed
ai_summary_only
asset_only

验收：
点击文件管理中的“查看预览”，能打开 Source Preview；PDF 至少显示 AI 摘要和元信息。

============================================================
九、P1：完善审阅中心为 Diff 工作台
============================================================

审阅中心必须展示完整信息，而不是简单文件名列表。

Review 任务顶部显示：

1. task_id；
2. source 文件名；
3. 风险等级；
4. 总项数；
5. 低/中/高风险数量；
6. 待审阅/已接受/已拒绝/应用失败数量；
7. coverage_report；
8. 操作：
   - 接受低风险；
   - 全部拒绝；
   - 重新生成；
   - 查看任务详情；
   - 打开 Source Preview。

Review Item 显示：

1. 操作类型；
2. 目标页面标题；
3. 目标路径；
4. 页面类型；
5. 风险等级；
6. 修改摘要；
7. 修改原因；
8. source_id / location / citation_status；
9. confidence；
10. requires_review；
11. Diff 折叠面板；
12. 操作：
    - 接受；
    - 拒绝；
    - 编辑后接受；
    - 查看源文件；
    - 查看模型原始输出；
    - 发送到 Chat 解释。

Diff 规则：

1. create：显示将创建的完整 Markdown。
2. update：显示旧内容与新内容对比。
3. append：显示追加 section。
4. add_alias：显示 alias 添加前后。
5. add_relation：显示 source → relation → target 和 evidence。
6. merge_suggestion：高风险，不自动应用。

验收：
用户能清楚看到 AI 要改什么、为什么改、基于什么来源、接受后会生成什么页面。

============================================================
十、P1：完善 Wiki 页面工作台
============================================================

Wiki 页面列表：

1. 按 page_type 分组：
   - source；
   - concept；
   - entity；
   - dataset；
   - method；
   - topic；
   - question；
   - review。
2. 支持搜索标题。
3. 显示：
   - title；
   - page_type；
   - relative_path；
   - tags；
   - updated_at；
   - broken 状态。
4. Broken 页面显示红色标记，并提供修复。

Wiki 阅读区：

1. Markdown 渲染模式。
2. 源码模式。
3. 编辑模式。
4. 保存前创建版本快照。
5. 保存后更新 content_hash。
6. 右侧元信息面板：
   - frontmatter；
   - sources；
   - aliases；
   - relationships；
   - backlinks；
   - versions；
   - health status。
7. 页面缺失时提供：
   - 从快照恢复；
   - 从 review item 创建；
   - 标记 broken；
   - 删除数据库记录。

新建页面：

1. 用户手动新建页面。
2. 选择 page_type。
3. 使用规范路径和模板。
4. 更新 wiki_pages 和 index.md/log.md。

验收：
Wiki 页面系统能稳定增删改查，且能展示元信息和修复 broken 页面。

============================================================
十一、P1：增强健康检查为可修复工作台
============================================================

当前健康检查能发现问题，但不能修复。请补齐修复动作。

健康检查问题分类：

1. 严重错误；
2. 警告；
3. 可自动修复；
4. 需人工确认；
5. 已通过。

每个问题显示：

1. 问题描述；
2. 影响范围；
3. 可能原因；
4. 推荐操作；
5. 查看详情；
6. 修复按钮。

必须实现修复动作：

1. 从版本快照恢复缺失页面；
2. 删除失效 wiki_pages 记录；
3. 从 review item 重新应用；
4. 重建 graph_nodes；
5. 重建 graph_edges；
6. 重新生成 index.md；
7. 重新生成 log.md 索引摘要；
8. 修复 knowledge_items 与 wiki_pages 关联。

验收：
对“数据库记录的页面文件不存在”可以一键从版本快照恢复，恢复后健康检查不再报该项。

============================================================
十二、P1：Dashboard 改造
============================================================

首页需要成为知识库管理入口。

要求：

1. 当前知识库卡片：
   - 名称；
   - 模板；
   - 路径；
   - 健康状态；
   - 最近更新时间。
2. 健康状态“需关注”必须可点击并展示原因：
   - 页面缺失数量；
   - 失败任务数量；
   - 图谱未同步；
   - 问答异常；
   - 数据不一致。
3. 统计卡片：
   - Wiki 页面；
   - Source；
   - 待审阅；
   - 知识关系；
   - broken 页面；
   - 失败任务。
4. 快捷操作：
   - 上传文档；
   - 新建页面；
   - 开始问答；
   - 审阅中心；
   - 知识图谱；
   - 健康检查；
   - 创建知识库；
   - 打开知识库。
5. 最近活动：
   - 最近导入；
   - 最近生成页面；
   - 最近审阅；
   - 最近失败任务；
   - 最近健康问题。

验收：
首页能直接指导用户下一步应该做什么，而不是只展示数字。

============================================================
十三、P1：知识库管理完善
============================================================

当前已有知识库切换入口，但需要完善。

要求：

1. 支持创建多个知识库。
2. 支持切换当前知识库。
3. 支持打开已有本地知识库。
4. 支持删除知识库记录，不默认删除本地文件。
5. 支持重命名知识库。
6. 支持显示模板类型。
7. 切换知识库后刷新所有页面数据。
8. 数据必须按 kb_id 隔离。
9. 每个知识库有独立：
   - workspace；
   - db；
   - raw；
   - wiki；
   - versions；
   - .runtime；
   - schema。

新建知识库流程：

1. 输入名称；
2. 选择路径；
3. 选择模板；
4. 初始化目录结构；
5. 创建 index.md/log.md/overview.md；
6. 写入 knowledge_bases；
7. 切换到新知识库。

验收：
创建“科研学习”和“我的知识库”后，两个知识库数据互不混淆。

============================================================
十四、必须保持的边界
============================================================

不要实现：

1. OCR；
2. 多模态图片理解；
3. 图片 caption；
4. 本地 PDF 深度解析；
5. PDF 页面切片；
6. 表格视觉识别；
7. 公式 OCR；
8. 自由 Agent 群聊；
9. 云同步；
10. Web 端；
11. 移动端；
12. 多人协作。

Source Preview 只是预览层，不是知识抽取主流程。

============================================================
十五、开发顺序
============================================================

请按以下顺序执行：

1. 修复 PathService / SlugService / wiki_pages.path。
2. 修复 WikiWriter 正式写入 Markdown。
3. 修复 Review 接受后的 applied 状态。
4. 实现从版本快照恢复缺失页面。
5. 修复 Chat SQL 参数错误。
6. 修复 Search 搜索无结果。
7. 修复 Graph 显示孤立节点。
8. 实现 graph 重建/同步。
9. 完善 Source 管理页面。
10. 实现 Source Preview 页面。
11. 完善 Review Diff 工作台。
12. 完善 Wiki 右侧元信息面板。
13. 增强 Health Check 修复功能。
14. 改造 Dashboard。
15. 完善知识库管理。
16. 全链路自测。

============================================================
十六、完整验收流程
============================================================

请实现后按以下流程自测：

1. 启动软件。
2. 创建一个“科研论文知识库”。
3. 上传一个 DOCX 或 PDF。
4. 文件进入 Sources，显示 source_id、hash、状态。
5. 创建导入任务。
6. Agent Timeline 正常显示。
7. 生成中间文件。
8. Review 页面显示完整审阅项。
9. 接受一个 create/update 项。
10. WikiWriter 创建真实 Markdown 文件。
11. Wiki 页面列表显示新页面。
12. 点击页面可正常打开。
13. Health Check 不再报该页面文件缺失。
14. Search 搜索页面标题能返回结果。
15. Graph 页面至少显示节点。
16. Chat 提问不再 SQL 报错。
17. index.md 和 log.md 更新。
18. versions 中有快照。
19. Dashboard 统计正确。
20. 删除或恢复 broken 页面功能可用。
21. 切换知识库后数据隔离。
22. 图片上传只保存 asset，不触发 AI 分析。

============================================================
十七、输出要求
============================================================

请基于现有项目修改，不要删除重写。

完成后请输出：

1. 修复的问题清单。
2. 修改的文件列表。
3. 新增的模块。
4. 数据库 migration。
5. 关键逻辑说明。
6. 需要手动测试的流程。
7. 当前仍存在的风险。
8. 运行和打包说明。

请优先保证真实闭环可用，而不是只在 UI 上显示假数据。