你是一名资深 Tauri + React + Rust 桌面软件工程师。当前项目版本为 v0.1.2，已经完成基础 UI、知识库切换、文件上传、导入任务、Agent Pipeline、审阅中心、Wiki 页面列表、问答、搜索、知识图谱和健康检查等模块。

根据当前运行截图，软件已经完成了部分核心流程：
1. 可以创建和切换知识库；
2. 可以上传 docx 文件并创建 source_ingest 任务；
3. PipelineCoordinator 可以生成审阅任务；
4. 审阅中心能显示 13 个 Review Items；
5. Wiki 页面列表能显示 13 个页面；
6. Chat 已不再出现 SQL 参数错误，能检索到页面索引；
7. 健康检查能发现数据一致性问题；
8. 文件管理能显示上传文件；
9. 图谱页面已有重建入口。

但是目前仍存在核心闭环问题：AI 分析结果虽然产生，但没有稳定、可靠、完整地转化为本地 Markdown Wiki。请在现有代码基础上继续开发，不要推倒重写，不要改变现有浅色蓝白灰 UI 风格，不要加入 OCR、多模态图片理解、本地 PDF 深度解析、图片 caption、自由 Agent 群聊、云同步、Web 端、移动端等已否决方向。

本次开发版本号为 v0.1.3。核心目标是：修复本地知识库持久化闭环，让 Review 接受后真正生成可读 Markdown 页面，并让 Wiki、Search、Chat、Graph、Health、Source 管理全部基于一致的数据工作。

============================================================
一、P0：彻底修复 wiki/wiki 路径重复问题
============================================================

当前健康检查显示大量错误：

数据库记录的页面文件不存在：wiki/wiki/concepts/xxx.md

这是最高优先级 bug。请统一路径规范。

要求：

1. 明确唯一路径规范：
   数据库 wiki_pages.path 永远保存相对于 workspace 根目录的路径，例如：
   wiki/concepts/mediapipe-pose.md
   wiki/entities/liu-rulin.md
   wiki/datasets/pv3d.md

2. 禁止保存：
   wiki/wiki/concepts/xxx.md
   concept · wiki\xxx.md
   绝对路径
   UI 展示路径

3. 文件读写统一规则：
   full_path = workspace_root + wiki_pages.path
   不允许在读取时再次拼接 wiki/ 前缀。

4. 如果某些内部函数需要相对于 wiki 目录的路径，则必须显式命名为 wiki_relative_path，例如：
   concepts/mediapipe-pose.md
   不得和 workspace_relative_path 混用。

5. 实现或修复 PathService：
   - normalize_workspace_path()
   - resolve_workspace_path()
   - normalize_wiki_page_path()
   - is_valid_workspace_relative_path()
   - strip_duplicate_wiki_prefix()
   - ensure_parent_dir()

6. 对已有数据库中的错误 path 做 migration / reconcile：
   - wiki/wiki/concepts/xxx.md → wiki/concepts/xxx.md
   - wiki\wiki\concepts\xxx.md → wiki/concepts/xxx.md
   - concept · wiki\xxx.md → wiki/concepts/xxx.md 或按 page_type 归类

7. 健康检查应在修复后重新扫描，不能继续报 wiki/wiki。

验收：
健康检查中不再出现 wiki/wiki/concepts 这种重复路径。

============================================================
二、P0：修复 Review Apply 与 WikiWriter 落盘闭环
============================================================

当前审阅中心显示待审 13 项，但页面和健康检查说明写入闭环不稳定。请修复 Review 接受后真正写入 Markdown 的逻辑。

要求：

1. Review Item 点击接受后必须调用 WikiWriter。
2. WikiWriter 必须完成以下步骤后，才能将 review_item.status 设为 applied：
   - 解析目标 path；
   - 创建父目录；
   - 生成 Markdown；
   - 写入正式 wiki 文件；
   - 更新 wiki_pages；
   - 更新 knowledge_items；
   - 更新 aliases；
   - 更新 relationships；
   - 更新 graph_nodes / graph_edges；
   - 更新 index.md；
   - 追加 log.md；
   - 创建 version 快照；
   - 写入 operation_id 和 operation_hash。

3. 如果任意一步失败：
   - review_item.status = apply_failed；
   - task.status = applied_with_warnings 或 partially_applied；
   - UI 显示具体错误；
   - 不允许静默标记成功。

4. 应用前检查 base_version_hash：
   - 如果目标页面不存在，且 operation 是 create/update，可以允许创建；
   - 如果页面存在且 hash 不一致，应提示“页面已变更，需要重新生成 Diff”；
   - 但不能因为路径错误误判为用户手动修改。

5. 修复当前错误提示：
   “页面已被手动修改，无法应用旧 Diff”
   需要区分：
   - 真实用户修改；
   - 目标文件缺失；
   - 路径错误；
   - base_version_hash 为空；
   - 版本快照缺失。

6. 新增 applied_with_warnings 状态。
7. Review 顶部显示：
   - 待审；
   - 已接受；
   - 已拒绝；
   - 应用失败；
   - 部分应用；
   - 路径错误数量。

验收：
接受一个 Review Item 后，workspace/wiki/concepts/... 下真实出现 md 文件，Wiki 页面可打开，健康检查不报该页面缺失。

============================================================
三、P0：修复页面标题、slug 和 page_type 质量
============================================================

当前页面列表出现大量 page-xxx、rppg-8d8126b44e2f、roi-xxx，显示名不友好。请修复页面标题和文件名生成逻辑。

要求：

1. Wiki 页面显示名称必须优先使用：
   - update_plan.title；
   - content_blocks.title；
   - Markdown 第一个 H1；
   - frontmatter.title；
   - canonical_name；
   - 最后才使用 page-id。

2. 文件名可以包含短 hash，但显示名不应是 page-id。
   例如：
   title: 刘汝林
   path: wiki/entities/liu-rulin-63f45d.md
   UI 显示：刘汝林

3. page_type 应根据知识类型分类：
   - person / organization → wiki/entities/
   - dataset → wiki/datasets/
   - method / framework → wiki/methods/
   - concept → wiki/concepts/
   - source → wiki/sources/
   - question → wiki/questions/

4. 修复 frontmatter：
   - title
   - type
   - canonical_name
   - aliases
   - sources
   - tags
   - confidence
   - status
   - created
   - updated
   - last_updated_by_task

5. 避免 Markdown 重复标题：
   当前出现 # 刘汝林 和 # 刘汝林 两次。页面模板只能生成一个 H1。

6. Evidence 不能总是“待补充”。如果 source_id/location 存在，应写入：
   - source_id；
   - location；
   - citation_status；
   - quote 或 evidence summary。
   如果确实没有 evidence，标记 citation_status: missing，并将页面风险设为 medium 或 high。

验收：
新生成页面列表显示真实中文/英文标题，不再大量显示 page-xxx；Markdown 不再重复 H1。

============================================================
四、P0：修复 Chat 只检索索引、不读取正文的问题
============================================================

当前 Chat 能回答“存在 mediapipe-pose 页面，但未提供具体内容”，说明 QueryAgent 只读到索引，没有读取 Markdown 正文。

要求：

1. QueryAgent 检索到 wiki_pages 后，必须读取对应 Markdown 文件正文。
2. 如果读取失败，返回明确原因：
   - 文件不存在；
   - path 错误；
   - 页面为空；
   - 权限错误。
3. 构造 Chat 上下文时包括：
   - title；
   - path；
   - page_type；
   - summary section；
   - definition section；
   - key_points section；
   - evidence section。
4. 回答时给出引用：
   - wiki page path；
   - section；
   - citation_status。
5. 如果页面只有占位内容，应提示“该页面内容较少”，但仍给出已有内容。
6. 保存回答为 Wiki 页面必须走 Review → WikiWriter → VersionManager，不允许直接绕过。

验收：
问“什么是 mediapipe-pose？”时，系统能读取 mediapipe-pose.md 的正文，而不是只说索引存在。

============================================================
五、P0：修复 Search 搜索能力
============================================================

当前搜索 mesiapip 没有结果可以理解为拼写错误，但搜索 mediapipe、BOL、rPPG 等应返回结果。请实现真实搜索。

要求：

1. 搜索范围：
   - wiki_pages.title；
   - wiki_pages.path；
   - wiki_pages.canonical_name；
   - aliases；
   - frontmatter；
   - Markdown 正文；
   - tags；
   - source_summary。

2. 支持：
   - 精确搜索；
   - 大小写不敏感；
   - 中英文搜索；
   - 子串搜索；
   - 简单模糊搜索，例如 mediapipe 和 mediapipe-pose 可匹配。

3. 搜索结果显示：
   - 标题；
   - 页面类型；
   - 路径；
   - 匹配片段；
   - 匹配字段；
   - 更新时间；
   - 打开按钮。

4. 搜索不到时提供建议：
   - 检查拼写；
   - 尝试搜索相近词；
   - 是否只搜索当前知识库。

验收：
搜索 mediapipe 能返回 mediapipe-pose 页面；搜索 rPPG 能返回相关页面。

============================================================
六、P0：修复知识图谱空白问题
============================================================

当前 Wiki 页面有 13 个，但知识图谱仍显示暂无图谱数据。请修复 GraphService 和图谱页面。

要求：

1. 如果 wiki_pages > 0，即使没有 relationships，也要能生成 graph_nodes。
2. 如果 graph_edges = 0，显示孤立节点，不要显示“暂无图谱数据”。
3. “从知识项重建图谱”按钮应执行：
   - 从 wiki_pages 生成 wiki_page 节点；
   - 从 knowledge_items 生成 concept/entity/topic 节点；
   - 从 sources 生成 source 节点；
   - 从 relationships 生成 graph_edges。
4. 如果 relationships 表为空，但页面中有 related pages 或 source contributions，可生成弱关系候选，进入审阅或 pending 状态。
5. 图谱节点至少显示：
   - title；
   - type；
   - path；
   - confidence；
   - source_count。
6. 点击节点显示详情面板：
   - 标题；
   - 类型；
   - 路径；
   - 摘要；
   - 打开页面；
   - 在 Chat 中询问。
7. graph_nodes / graph_edges 必须按当前 kb_id 隔离。

验收：
即使知识关系为 0，图谱页面也应显示 13 个孤立节点；不应继续显示完全空白。

============================================================
七、P1：完善 Source 文件管理
============================================================

当前文件管理只是文件名、类型、大小、状态。请升级为 Source 管理中心。

要求：

1. Source 列表显示：
   - 文件名；
   - 类型；
   - 大小；
   - 状态；
   - source_id；
   - file_hash；
   - 上传时间；
   - 最近任务；
   - 关联 Wiki 页面数；
   - 关联 Review 数；
   - 关联关系数。

2. 状态枚举：
   - saved；
   - analyzing；
   - analyzed；
   - review_pending；
   - applied；
   - applied_with_warnings；
   - failed；
   - asset_only。

3. 操作：
   - 打开原始文件；
   - 打开所在文件夹；
   - 查看 Source Preview；
   - 查看分析结果；
   - 查看关联 Wiki 页面；
   - 重新分析；
   - 作为新版本导入；
   - 复制 source_id；
   - 删除记录。

4. Source Preview 至少显示：
   - 原始信息；
   - AI 摘要；
   - coverage_report；
   - 抽取实体；
   - 抽取概念；
   - 关系；
   - 关联页面；
   - 任务日志。

注意：
Source Preview 不是本地 PDF 深度解析，不做 OCR，不做图片理解。

验收：
用户能从文件管理页面清楚看到一个文件对知识库贡献了什么。

============================================================
八、P1：完善审阅中心信息和 Diff
============================================================

当前审阅中心已有基本信息，但仍需增强为真正的 Diff 工作台。

要求：

1. Review 任务顶部显示：
   - task_id；
   - source 文件名；
   - 总项数；
   - 低/中/高风险数量；
   - 待审/已接受/已拒绝/应用失败数量；
   - coverage_report；
   - 查看任务详情；
   - 打开 Source Preview。

2. Review Item 显示：
   - 操作类型；
   - 目标页面标题；
   - 目标路径；
   - 页面类型；
   - 风险等级；
   - 修改摘要；
   - AI 修改原因；
   - source_id；
   - location；
   - citation_status；
   - confidence；
   - 接受后会发生什么。

3. “查看 Diff”展开后：
   - create：显示完整将创建的 Markdown；
   - update：显示旧内容 / 新内容；
   - append：显示追加到哪个 section；
   - add_relation：显示 source → relation → target；
   - add_alias：显示 alias 添加前后。

4. 接受失败要显示具体原因，不要只提示无法应用旧 Diff。

验收：
用户可以根据审阅中心判断每个 AI 修改是否可信。

============================================================
九、P1：增强健康检查修复能力
============================================================

当前健康检查发现 56 个一致性问题，但“已修复 0 个问题”。请让修复动作真正可用。

要求：

1. 对每个问题分类：
   - 可自动修复；
   - 需人工确认；
   - 不可修复；
   - 已修复。

2. 对“数据库记录页面文件不存在”执行：
   - 先检查 path 是否有 wiki/wiki 重复并修正；
   - 再检查 versions 快照；
   - 如果有快照，恢复正式页面；
   - 如果无快照但有 review item，提示重新应用；
   - 如果都没有，提供删除数据库记录按钮。

3. 一键修复所有可修复问题应真实执行修复，并刷新报告。

4. 修复完成后更新：
   - wiki_pages；
   - content_hash；
   - health status；
   - Dashboard 统计。

验收：
点击“尝试一键修复”后，如果有 version 快照，页面文件应被恢复，问题数量减少。

============================================================
十、P1：完善 Dashboard 状态解释
============================================================

首页当前显示健康，但底部和审阅仍有待处理。请让 Dashboard 更可解释。

要求：

1. 健康状态旁边显示原因：
   - 待审 9；
   - 关系 0；
   - 数据一致性问题 56；
   - 失败任务数；
   - broken pages 数。

2. 统计卡片点击后跳转：
   - 页面 → Wiki 页面；
   - Source → 文件管理；
   - 待审 → 审阅中心；
   - 知识关系 → 知识图谱；
   - 失败任务 → 导入任务；
   - 健康问题 → 健康检查。

3. 首页新增最近活动：
   - 最近导入；
   - 最近生成页面；
   - 最近审阅；
   - 最近失败任务；
   - 最近健康问题。

验收：
用户从首页能知道下一步该处理什么。

============================================================
十一、必须保持产品边界
============================================================

不要实现：
1. OCR；
2. 多模态图片理解；
3. 图片 caption；
4. 本地 PDF 深度解析；
5. PDF 页面切片；
6. 表格视觉识别；
7. 公式 OCR；
8. 自由 Agent 群聊；
9. 云同步；
10. Web 端；
11. 移动端；
12. 多人协作。

本版本重点不是新增大功能，而是修复本地知识库闭环。

============================================================
十二、建议开发顺序
============================================================

请按以下顺序执行：

1. 修复 PathService，解决 wiki/wiki 重复路径。
2. 对已有数据库路径做 migration/reconcile。
3. 修复 WikiWriter 写入正式 Markdown。
4. 修复 Review Apply 状态。
5. 修复 page title / slug / page_type。
6. 修复 Chat 读取 Markdown 正文。
7. 修复 Search 标题/正文/alias 搜索。
8. 修复 Graph 从 wiki_pages/knowledge_items 生成孤立节点。
9. 增强 Health Check 一键修复。
10. 完善 Source 管理。
11. 完善 Review Diff 展示。
12. 完善 Dashboard 状态解释。

============================================================
十三、完整验收流程
============================================================

实现后按以下流程自测：

1. 创建或打开“我的知识库”。
2. 上传一个 docx。
3. 等待生成审阅。
4. 审阅中心显示 13 个 Review Items。
5. 接受一个 Review Item。
6. 检查 workspace/wiki/concepts/xxx.md 是否真实存在。
7. 打开 Wiki 页面，能正常显示。
8. 健康检查不再报该页面文件缺失。
9. 搜索该页面标题能返回结果。
10. Chat 询问该页面相关问题，能读取正文回答。
11. 图谱页面至少显示该页面节点。
12. Source 页面显示该文件关联了该页面。
13. index.md 和 log.md 更新。
14. 版本快照可查看和恢复。
15. 一键修复能减少健康检查问题数。

============================================================
十四、输出要求
============================================================

请在完成后输出：

1. 修复了哪些问题；
2. 修改了哪些文件；
3. 新增了哪些模块；
4. 数据库 migration 内容；
5. 路径规范说明；
6. WikiWriter 写入流程说明；
7. Review Apply 状态机说明；
8. 仍然存在的风险；
9. 手动测试步骤；
10. 如何运行和打包。

请优先保证真实闭环可用，不要只做 UI 展示。