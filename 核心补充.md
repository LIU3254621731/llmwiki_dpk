你是一名资深 Tauri + React + Rust 桌面软件工程师、AI 知识库产品架构师和本地文件系统应用开发专家。当前项目是“智维 Wiki - LLM 知识维护工作台”，已有版本 v0.1.2/v0.1.3 已经完成基础 UI、知识库切换、文件上传、导入任务、Agent Pipeline、审阅中心、Wiki 页面、问答、搜索、知识图谱、健康检查等模块。

现在进入 v0.1.4 开发阶段。本阶段的目标不是继续新增复杂 Agent，也不是改变产品方向，而是在现有本地知识库闭环基础上，补齐“本地文件资产管理 + Markdown 阅读体验 + Source Preview + 思维导图式知识结构视图 + 右侧上下文面板”，使软件从“能生成 Wiki 页面”升级为“能管理、阅读、追踪、理解整个本地知识库的工作台”。

请基于现有代码继续开发，不要推倒重写，不要改变当前浅色蓝白灰、简洁、桌面工作台式 UI 风格。保持现有导航结构，但可以增强页面布局、右侧面板、预览区域和交互体验。

============================================================
一、v0.1.4 总体目标
============================================================

本阶段需要实现以下核心能力：

1. 本地知识库文件管理系统
   - 展示当前知识库 workspace 的真实文件结构；
   - 支持 raw、wiki、versions、schema、runtime 等目录浏览；
   - 支持原始文件、Wiki 文件、版本文件、任务中间文件的预览和定位；
   - 让用户能清楚知道本地知识库到底保存了哪些文件。

2. Source 原始文件管理与预览
   - 文件管理页面不再只是上传文件列表；
   - 要成为 Source 管理中心；
   - 支持查看 source_id、hash、任务、AI 分析结果、关联 Wiki 页面、关联图谱节点；
   - 支持打开统一 Source Preview 页面。

3. Markdown 格式预览界面
   - Wiki 页面默认使用 Markdown 渲染阅读模式；
   - 支持阅读模式、编辑模式、源码模式；
   - frontmatter 不应直接作为正文展示；
   - frontmatter 应进入右侧信息面板；
   - Source 文件也应支持统一 Markdown Preview。

4. 本地文件转换成 Markdown 并预览
   - DOCX / TXT / MD / HTML 等文件可以转换为 Markdown 预览；
   - PDF 不做本地深度解析，不做 OCR，只显示元信息 + AI 摘要 + 可选基础文本预览；
   - 图片只显示资产信息和缩略图，不 OCR、不视觉理解；
   - 转换结果用于预览，不作为正式 Wiki 页面；
   - 正式 Wiki 页面仍必须经过 Agent 分析、Review、WikiWriter 写入。

5. 知识图谱改造为思维导图样式
   - 默认图谱视图改为 Mind Map Knowledge Graph；
   - 支持以当前页面、Source、Topic、Concept、Entity 或整个知识库为中心；
   - 支持按多个信息维度展开；
   - 支持展开/折叠、设为中心、打开页面、在 Chat 中询问、导出大纲。

6. 多维度知识展开
   - 按页面类型展开；
   - 按关系类型展开；
   - 按来源文件展开；
   - 按时间展开；
   - 按可信度展开；
   - 按维护状态展开。

7. 右侧上下文面板
   - Wiki、Source、Review、Graph、Chat、Search 页面都应有可复用右侧上下文面板；
   - 用于展示元信息、来源、关系、版本、引用、AI 建议和操作按钮。

8. UI 体验优化
   - 保持现有风格；
   - 增强信息密度；
   - 减少大面积空白；
   - 增加最近活动、状态解释、下一步建议；
   - 让用户清楚知道当前系统状态和下一步操作。

============================================================
二、必须保持的产品边界
============================================================

本项目仍然是：

本地优先的、LLM 驱动的、多 Agent 协作式 Markdown Wiki 维护工作台。

请不要把项目做偏。

不要实现以下功能：


2. 不做多模态图片理解。
11. 不做 Web 端。
12. 不做移动端。
13. 不做多人协作。
14. 不允许 Source Preview Markdown 直接替代正式 Wiki 页面。
15. 不允许文件转换 Markdown 后绕过 Review 直接写入正式 Wiki。

重要原则：

Source Preview Markdown ≠ Wiki Page Markdown。

Source Preview Markdown 是原始文件的可读预览副本。
Wiki Page Markdown 是经过 LLM 分析、消歧、审阅、WikiWriter 写入后的长期知识资产。

============================================================
三、本地知识库文件管理系统
============================================================

请将“文件管理”页面升级为真正的本地知识库文件管理系统。

当前文件管理页面只显示上传文件列表，本阶段需要增加 Workspace 文件树视图。

页面建议采用三栏布局：

左侧：文件树
中间：文件列表 / 文件预览
右侧：文件元信息 / AI 分析结果 / 关联 Wiki 页面

左侧文件树显示当前知识库 workspace 的真实目录结构：

workspace/
  raw/
    sources/
      documents/
    assets/
      images/
      attachments/
  wiki/
    index.md
    log.md
    overview.md
    concepts/
    entities/
    datasets/
    methods/
    topics/
    questions/
    sources/
    reviews/
  versions/
    pages/
    snapshots/
  .runtime/
    tasks/
    logs/
    source_previews/
  schema/
    TEMPLATE.yaml
    PROMPTS.md
    AGENTS.md
    WIKI_SCHEMA.md
  config/
    kb.config.json
  db/
    app.sqlite

功能要求：

1. 文件树支持展开/折叠目录。
2. 点击目录，中间显示该目录下文件列表。
3. 点击文件，中间或右侧显示文件预览。
4. 支持过滤：
   - 只看原始文件；
   - 只看 Wiki 页面；
   - 只看版本快照；
   - 只看任务中间文件；
   - 只看图片资产；
   - 只看缺失/异常文件。
5. 支持搜索文件名。
6. 支持打开文件所在目录。
7. 支持复制相对路径。
8. 支持打开原始文件。
9. 支持安全删除记录，不默认删除真实文件。
10. 支持刷新文件树。
11. 支持“重新扫描 workspace”。

文件节点需要显示：

1. 文件名；
2. 文件类型；
3. 文件大小；
4. 修改时间；
5. 是否被数据库记录；
6. 是否为 source；
7. 是否为 wiki page；
8. 是否为 version snapshot；
9. 是否为 runtime artifact；
10. 是否存在异常。

右侧文件详情面板显示：

1. 文件名；
2. 相对路径；
3. 绝对路径；
4. 文件类型；
5. 文件大小；
6. SHA256 hash；
7. 创建时间；
8. 修改时间；
9. 数据库关联状态；
10. source_id / page_id / task_id / version_id；
11. 关联 Wiki 页面；
12. 关联任务；
13. 关联图谱节点；
14. 可执行操作。

验收标准：

1. 用户可以在文件管理页面看到整个知识库真实文件结构。
2. 用户可以从 raw/sources/documents 找到上传的 docx/pdf。
3. 用户可以从 wiki/concepts 打开生成的 Markdown 页面。
4. 用户可以从 .runtime/tasks 打开任务中间文件。
5. 用户可以从 versions/pages 查看页面快照。
6. 文件树和数据库状态不一致时能显示警告。

============================================================
四、Source 管理中心
============================================================

在文件管理页面中增加 Source 视图，或将文件管理页面拆分为：

1. Source 文件视图；
2. Workspace 文件树视图。

Source 文件视图显示用户上传的原始文件，而不是所有 workspace 文件。

Source 列表必须显示：

1. 文件名；
2. 文件类型；
3. 文件大小；
4. 状态；
5. source_id；
6. file_hash；
7. 上传时间；
8. 最近分析时间；
9. 最近任务 ID；
10. 关联 Wiki 页面数量；
11. 关联 Review 数量；
12. 关联关系数量；
13. AI 摘要状态；
14. 预览状态。

Source 状态枚举：

saved
analyzing
analyzed
review_pending
applied
applied_with_warnings
failed
asset_only

操作按钮：

1. 打开原始文件；
2. 打开所在文件夹；
3. 查看 Source Preview；
4. 查看 AI 摘要；
5. 查看抽取结果；
6. 查看关联 Wiki 页面；
7. 查看关联任务；
8. 重新分析；
9. 作为新版本导入；
10. 复制 source_id；
11. 删除记录。

Source 详情页 / 右侧面板显示：

1. 基本信息；
2. source_id；
3. file_hash；
4. 文件路径；
5. 文件状态；
6. AI source_summary；
7. coverage_report；
8. 抽取实体数量；
9. 抽取概念数量；
10. 抽取关系数量；
11. 生成 Review 数量；
12. 已应用 Wiki 页面数量；
13. 失败项；
14. 关联 Wiki 页面列表；
15. 关联图谱节点；
16. 相关任务日志。

验收标准：

用户点击一个 DOCX Source，可以看到该文件贡献了哪些知识项、哪些 Wiki 页面、哪些任务、哪些审阅项。

============================================================
五、统一 Source Preview 页面
============================================================

请实现 Source Preview 页面，用于预览上传的原始文件和 AI 分析结果。

Source Preview 不是知识抽取主流程，不做 OCR、不做 PDF 深度解析、不做图片理解。

Source Preview 页面建议布局：

顶部：
- 文件名；
- 文件类型；
- source_id；
- 状态；
- 操作按钮：打开原始文件、重新分析、打开所在文件夹、复制引用路径。

左侧：
- 文件元信息；
- 关联任务；
- 关联 Wiki 页面；
- 关联图谱节点。

中间：
- Markdown 预览；
- 或图片缩略图；
- 或 PDF 元信息 + AI 摘要。

右侧：
- AI 摘要；
- coverage_report；
- 抽取实体；
- 抽取概念；
- 关系；
- 任务日志。

Tabs：

1. 原始信息
2. Markdown 预览
3. AI 摘要
4. 抽取实体
5. 抽取概念
6. 抽取关系
7. 关联 Wiki 页面
8. 任务日志

不同文件的预览规则：

1. Markdown 文件
   - 直接读取并渲染 Markdown。

2. TXT 文件
   - 读取纯文本；
   - 包装成 Markdown；
   - 支持标题、段落、代码块的基础显示。

3. HTML 文件
   - 使用轻量 HTML-to-Markdown 转换；
   - 保留标题、段落、列表、链接；
   - 不做复杂网页清洗。

4. DOCX 文件
   - 如果项目中已有轻量依赖，可以提取普通文本并转换为 Markdown；
   - 保留标题、段落、列表；
   - 表格可以简化为 Markdown table 或纯文本；
   - 不追求版面还原；
   - 如果实现复杂，先提供基础文本提取或显示“本地预览有限，请查看 AI 摘要”。

5. PDF 文件
   - 不做本地深度解析；
   - 不做 OCR；
   - 不做页面级版面还原；
   - 显示文件元信息；
   - 显示 DeepSeek 分析得到的 source_summary；
   - 显示 coverage_report；
   - 如果已有简单文本提取能力，可作为“基础文本预览”，但必须标注“不保证完整”。

6. 图片文件
   - 显示缩略图；
   - 显示文件信息；
   - 状态 asset_only；
   - 明确提示“不进行 OCR、不进行图片理解、不参与 AI 知识抽取”。

Preview 缓存：

转换后的 Markdown 预览保存到：

.runtime/source_previews/{source_id}.md

同时记录 preview_status：

generated
ai_summary_only
unavailable
failed
asset_only

数据库可新增 source_previews 表，或在 sources 表中增加：

preview_path
preview_status
preview_generated_at
preview_error

验收标准：

1. DOCX 可以打开 Source Preview。
2. TXT/MD/HTML 可以渲染为 Markdown。
3. PDF 至少能显示文件元信息 + AI 摘要。
4. 图片能显示缩略图和 asset 信息。
5. Source Preview 不影响正式 Wiki 页面生成流程。

============================================================
六、Wiki Markdown 阅读/编辑/源码三模式
============================================================

当前 Wiki 页面阅读区仍然偏原始文本。请升级为正式 Markdown 阅读体验。

Wiki 页面需要三种模式：

1. 阅读模式
   - 默认模式；
   - 渲染 Markdown；
   - 不直接显示 YAML frontmatter；
   - 适合普通用户阅读。

2. 编辑模式
   - 可编辑 Markdown 正文；
   - 保存前创建版本快照；
   - 保存后更新 content_hash；
   - 保存后刷新页面和健康状态。

3. 源码模式
   - 显示完整原始 Markdown；
   - 包括 YAML frontmatter；
   - 适合开发调试。

阅读模式要求：

1. frontmatter 不作为正文显示。
2. frontmatter 解析后显示到右侧元信息面板。
3. 正文区域渲染：
   - H1/H2/H3；
   - 列表；
   - 表格；
   - 代码块；
   - 引用；
   - 链接；
   - 双链；
   - 分割线。
4. 支持目录导航。
5. 支持复制标题链接。
6. 支持点击内部 Wiki 链接跳转。
7. 支持 evidence / source 链接跳转到 Source Preview。
8. 支持 Related Pages 跳转。
9. 支持页面缺失时显示修复操作。

右侧 Wiki 上下文面板显示：

1. title；
2. page_type；
3. canonical_name；
4. aliases；
5. sources；
6. tags；
7. confidence；
8. status；
9. created；
10. updated；
11. last_updated_by_task；
12. related pages；
13. backlinks；
14. relationships；
15. source contributions；
16. versions；
17. health status；
18. AI 建议操作。

AI 建议操作：

1. 总结当前页面；
2. 查找缺失引用；
3. 生成相关问题；
4. 基于当前页面问答；
5. 生成思维导图；
6. 重新审阅页面；
7. 运行局部健康检查。

验收标准：

1. Wiki 页面默认显示渲染后的 Markdown。
2. frontmatter 不再直接显示在正文。
3. 用户可以切换阅读/编辑/源码模式。
4. 保存编辑会创建版本快照。
5. 右侧面板能显示页面元信息。

============================================================
七、Markdown 转换服务
============================================================

请实现本地 Markdown Preview 转换服务，注意它只服务于预览，不负责知识抽取。

建议后端模块：

MarkdownPreviewService / SourcePreviewService

职责：

1. 根据 source_id 找到原始文件；
2. 判断文件类型；
3. 生成预览 Markdown；
4. 保存到 .runtime/source_previews/{source_id}.md；
5. 更新 preview_status；
6. 返回预览内容给前端；
7. 如果失败，返回明确错误和 fallback 信息。

支持转换：

MD：
直接读取。

TXT：
转义特殊字符后包装为 Markdown。

HTML：
轻量转换为 Markdown。

DOCX：
提取普通文本，尽量保留标题和列表。
不要追求完整版式。

PDF：
默认 ai_summary_only。
可以显示 AI 摘要和 coverage_report。
如果已有简单文本抽取工具，可作为 optional basic_text_preview，但 UI 必须提示“不保证完整”。

图片：
asset_only。
不 OCR，不理解。

必须避免：

1. 不要把 PDF 转换做成复杂解析系统。
2. 不要引入大型 OCR/版面分析依赖。
3. 不要把 Preview Markdown 写入 wiki/ 目录。
4. 不要绕过 Review 创建 Wiki 页面。

验收标准：

Source Preview 生成的 Markdown 文件只存在于 .runtime/source_previews，不污染 wiki 目录。

============================================================
八、知识图谱改造为思维导图样式
============================================================

当前知识图谱页面采用普通图谱视图但数据为空或难以理解。本阶段请将默认知识图谱改造成“思维导图式知识结构视图”。

名称可以显示为：

知识图谱 / 思维导图

核心定义：

Mind Map Knowledge Graph 是以一个中心节点为核心，根据用户选择的信息维度展开分支的知识结构视图。

默认视图：

如果用户没有选择中心节点，则以当前知识库为中心：

我的知识库
  ├── 概念
  ├── 实体
  ├── 数据集
  ├── 方法
  ├── 来源文件
  ├── 问题
  ├── 待审阅
  └── 健康问题

如果用户选择某个页面为中心，例如 rPPG：

rPPG
  ├── 定义
  ├── 相关方法
  ├── 相关数据集
  ├── 相关论文/来源
  ├── 应用场景
  ├── 相关人物/机构
  ├── 相关问题
  ├── 缺失引用
  └── 待补充内容

支持中心节点类型：

1. 当前 Wiki 页面；
2. Source 文件；
3. Topic；
4. Concept；
5. Entity；
6. Dataset；
7. Method；
8. Question；
9. 整个知识库；
10. 搜索结果节点。

支持展开维度：

1. 按页面类型展开：
   - concepts；
   - entities；
   - datasets；
   - methods；
   - topics；
   - sources；
   - questions。

2. 按关系类型展开：
   - is_a；
   - part_of；
   - uses；
   - depends_on；
   - cites；
   - mentions；
   - related_to；
   - contradicts；
   - derived_from；
   - applies_to。

3. 按来源展开：
   - 来自哪个 Source；
   - 哪个 Source 贡献了该页面；
   - 哪个任务生成了该页面；
   - 哪个文档支持该 claim。

4. 按时间展开：
   - 最近创建；
   - 最近更新；
   - 导入批次；
   - 版本变化。

5. 按可信度展开：
   - high；
   - medium；
   - low；
   - missing citation；
   - model_reported；
   - user_verified。

6. 按维护状态展开：
   - 待审阅；
   - 缺引用；
   - 孤立节点；
   - 冲突；
   - 内容过短；
   - 过期；
   - broken page。

顶部工具栏：

1. 中心节点选择器；
2. 展开维度选择；
3. 展开深度选择；
4. 视图模式：
   - 思维导图；
   - 关系网络；
   - 健康视图；
5. 搜索节点；
6. 重新生成；
7. 导出；
8. 刷新。

左侧筛选栏：

1. 节点类型；
2. 关系类型；
3. 置信度；
4. 来源；
5. 时间；
6. 健康状态；
7. 是否显示孤立节点；
8. 是否显示待审阅；
9. 是否显示低置信度。

中间画布：

1. 思维导图节点；
2. 中心节点突出显示；
3. 分支可折叠；
4. 支持缩放和平移；
5. 支持拖拽节点；
6. 支持框选节点；
7. 支持小地图或定位按钮；
8. 支持重新布局。

右侧详情面板：

选中节点时显示：

1. 标题；
2. 类型；
3. 路径；
4. 摘要；
5. aliases；
6. source_count；
7. 入边/出边数量；
8. 相关页面；
9. 维护状态；
10. evidence；
11. 操作：
    - 打开 Wiki 页面；
    - 打开 Source Preview；
    - 在 Chat 中询问；
    - 设为中心；
    - 展开一层；
    - 生成子图摘要；
    - 保存为 Wiki 页面。

选中分支或关系时显示：

1. source；
2. target；
3. relation；
4. confidence；
5. evidence；
6. citation_status；
7. created_by_task；
8. 操作：
   - 查看证据；
   - 解释关系；
   - 确认关系；
   - 删除关系；
   - 提交审阅。

交互要求：

1. 单击节点：右侧显示详情。
2. 双击节点：打开 Wiki 页面或 Source Preview。
3. 右键节点：显示上下文菜单。
4. 点击分支标题：展开/折叠。
5. 点击“设为中心”：以该节点重新生成思维导图。
6. 搜索节点后：定位并高亮。
7. 框选节点：可生成子图摘要。
8. 导出：
   - PNG；
   - SVG；
   - Markdown 大纲；
   - JSON。

如果当前没有关系边：

1. 仍然从 wiki_pages / knowledge_items 生成节点；
2. 按 page_type 组织为思维导图分支；
3. 不要显示完全空白；
4. 提示“当前有知识节点，但关系较少，可运行关系分析或从页面重建关系”。

验收标准：

1. 知识图谱页面默认显示思维导图，而不是空白。
2. 即使关系为 0，也能以知识库为中心显示页面类型分支和页面节点。
3. 用户可以选择 rPPG / mediapipe-pose / Source 作为中心节点。
4. 用户可以按来源、类型、关系、时间、可信度、健康状态展开。
5. 节点可点击、可打开页面、可在 Chat 中询问。

============================================================
九、右侧上下文面板组件
============================================================

请抽象一个可复用的 RightContextPanel 组件，在多个页面使用。

适用页面：

1. Wiki 页面；
2. 文件管理 / Source Preview；
3. 审阅中心；
4. 知识图谱；
5. Chat；
6. Search；
7. Health Check。

RightContextPanel 根据页面上下文显示不同内容。

Wiki 页面右侧：

1. 页面元信息；
2. aliases；
3. sources；
4. tags；
5. relationships；
6. backlinks；
7. versions；
8. health；
9. AI actions。

Source 页面右侧：

1. source metadata；
2. AI summary；
3. coverage_report；
4. extracted entities；
5. extracted concepts；
6. linked wiki pages；
7. tasks；
8. actions。

Review 页面右侧：

1. 当前 Review Item 摘要；
2. 修改原因；
3. 风险解释；
4. evidence；
5. 应用后影响；
6. 操作历史。

Graph 页面右侧：

1. 节点详情；
2. 关系详情；
3. evidence；
4. related pages；
5. actions。

Chat 页面右侧：

1. 本次使用上下文；
2. 引用页面；
3. 引用 source；
4. 可保存到 Wiki 的建议；
5. 相关问题。

Search 页面右侧：

1. 当前选中结果详情；
2. 匹配字段；
3. 匹配片段；
4. 打开页面；
5. 在 Chat 中询问。

Health 页面右侧：

1. 当前问题详情；
2. 原因；
3. 影响；
4. 修复动作；
5. 修复日志。

验收标准：

至少 Wiki、Source Preview、Graph、Chat 页面使用右侧上下文面板。

============================================================
十、UI 优化要求
============================================================

保持现有 UI 风格：

1. 浅色；
2. 蓝白灰；
3. 简洁；
4. 工作台；
5. 不做炫酷科技风；
6. 不做复杂渐变；
7. 不做重阴影；
8. 不做游戏风。

需要优化：

1. 减少大面积空白。
2. 增加用户下一步建议。
3. 增强状态解释。
4. 增加最近活动。
5. 增加右侧上下文信息。
6. 页面标题与操作按钮对齐。
7. 保持统一卡片样式。
8. 保持统一 icon 风格。
9. 保持底部状态栏。
10. 状态文案要清楚。

状态文案优化：

processed 不够明确，应改为：
- 已保存；
- 分析中；
- 已分析；
- 等待审阅；
- 已应用；
- 已应用但有警告；
- 失败；
- 仅资产。

生成审阅 应显示为：
- 流水线完成，等待审阅 X 项。

健康 应显示为：
- 健康；
- 需关注；
- 有严重问题。

图谱为空 应显示为：
- 当前有 X 个知识页面，但关系尚未建立；
- 可从页面生成思维导图；
- 可运行关系重建。

验收标准：

页面不再大面积空白，用户能明确知道当前页面有哪些可操作内容。

============================================================
十一、Dashboard 优化
============================================================

首页需要增强为知识库管理和状态总览。

新增区域：

1. 当前知识库卡片；
2. 最近导入文件；
3. 最近生成页面；
4. 最近审阅任务；
5. 最近健康问题；
6. 推荐下一步操作；
7. 最近打开页面；
8. 当前知识库文件结构入口；
9. 快速打开 Source 管理；
10. 快速打开思维导图。

Dashboard 统计卡片应可点击跳转：

1. Wiki 页面 → Wiki 页面；
2. Source 文件 → 文件管理；
3. 待审阅 → 审阅中心；
4. 知识关系 → 知识图谱；
5. 健康问题 → 健康检查；
6. 失败任务 → 导入任务。

推荐下一步示例：

1. 有待审阅：你有 9 个待审阅修改，建议前往审阅中心。
2. 有 source 但无关系：当前知识关系为 0，建议重建思维导图或运行关系分析。
3. 有页面但内容短：部分页面内容较少，建议补充来源或重新分析。
4. 有健康问题：检测到数据一致性问题，建议一键修复。

验收标准：

首页能指导用户下一步操作，而不是只展示数字。

============================================================
十二、Search 页面优化
============================================================

Search 页面需要配合 Markdown 和知识结构增强。

功能要求：

1. 搜索 Wiki 页面标题；
2. 搜索正文；
3. 搜索 aliases；
4. 搜索 source 文件；
5. 搜索 source_summary；
6. 搜索 tags；
7. 搜索 frontmatter；
8. 支持模糊搜索；
9. 支持中英文；
10. 支持按类型筛选。

搜索结果显示：

1. 标题；
2. 类型；
3. 路径；
4. 匹配片段；
5. 匹配字段；
6. source；
7. 更新时间；
8. 打开按钮；
9. 在 Chat 中询问；
10. 在思维导图中定位。

右侧面板显示选中搜索结果详情。

验收标准：

搜索 mediapipe 能返回 mediapipe-pose 页面；搜索 rPPG 能返回相关页面；搜索 source 文件名能返回文件。

============================================================
十三、Chat 页面优化
============================================================

Chat 页面需要显示“使用了哪些知识库上下文”。

要求：

1. 用户可选择范围：
   - 整个知识库；
   - 当前页面；
   - 指定 Source；
   - 指定文件夹；
   - 指定标签；
   - 当前思维导图子图。

2. 回答区显示引用：
   - Wiki 页面；
   - Source 文件；
   - section；
   - citation_status。

3. 右侧上下文面板显示：
   - 本次检索到的页面；
   - 使用的 source；
   - 置信度；
   - 可打开页面；
   - 可查看 Source Preview。

4. 支持保存回答：
   - 保存为 Wiki 页面；
   - 追加到当前页面；
   - 生成 Review Item；
   - 保存为问题页面。

5. 保存仍必须走 Review → WikiWriter → VersionManager。

验收标准：

Chat 不只回答，还能显示“基于哪些页面回答”。

============================================================
十四、Health Check 优化
============================================================

健康检查页面需要继续增强为维护工作台。

增加检查项：

1. 文件结构异常；
2. Source 缺失；
3. Preview 缺失；
4. Wiki 页面缺失；
5. Markdown frontmatter 异常；
6. 页面内容过短；
7. Evidence 缺失；
8. relationships 为空；
9. graph_nodes 缺失；
10. graph_edges 缺失；
11. Search index 过期；
12. Source 与 Wiki 未关联；
13. 版本快照缺失；
14. runtime task 文件缺失。

每个问题显示：

1. 问题描述；
2. 严重程度；
3. 影响范围；
4. 推荐修复；
5. 一键修复；
6. 查看详情；
7. 忽略。

支持修复：

1. 重建文件树索引；
2. 重建 Source Preview；
3. 重建 Wiki 页面索引；
4. 重建图谱节点；
5. 重建 Search 索引；
6. 从快照恢复页面；
7. 修复重复 wiki/wiki 路径；
8. 删除失效数据库记录；
9. 重新生成 index.md；
10. 重新生成 log.md 摘要。

验收标准：

Health Check 不只是报告，而能执行修复动作。

============================================================
十五、数据库与后端建议
============================================================

可以根据现有结构增加或调整以下表/字段。

sources 表可增加：

preview_path
preview_status
preview_generated_at
preview_error
summary_json_path
coverage_json_path
linked_pages_count
linked_relations_count

新增 source_previews 表可选：

id
source_id
kb_id
preview_path
preview_status
content_hash
generated_at
error_message

新增 file_index 表可选：

id
kb_id
relative_path
file_type
file_size
content_hash
modified_at
record_type
linked_record_id
status

graph_nodes 应支持：

id
kb_id
node_type
label
path
source_id
page_id
confidence
status
metadata

mind_map_cache 可选：

id
kb_id
center_node_id
dimension_config
depth
cache_json_path
created_at
updated_at

务必保证：

1. 所有数据按 kb_id 隔离。
2. 所有文件路径为 workspace-relative path。
3. 不保存混乱路径。
4. 不保存重复 wiki/wiki。
5. UI 显示路径和内部读写路径分离。

============================================================
十六、开发顺序建议
============================================================

请按以下顺序开发：

1. 实现 Workspace 文件树扫描服务。
2. 改造文件管理页面为 Source 视图 + 文件树视图。
3. 实现 Source Preview Service。
4. 实现 Source Preview 页面。
5. 实现 Markdown 渲染组件。
6. 改造 Wiki 页面为阅读/编辑/源码三模式。
7. 实现 RightContextPanel。
8. 改造知识图谱为思维导图默认视图。
9. 实现多维度展开逻辑。
10. 实现 Graph 节点详情和操作。
11. 优化 Search 页面。
12. 优化 Chat 上下文面板。
13. 优化 Health Check 修复项。
14. 优化 Dashboard 最近活动和推荐操作。
15. 全链路测试。

============================================================
十七、完整验收流程
============================================================

实现后按以下流程自测：

1. 打开已有知识库。
2. 进入文件管理。
3. 能看到 workspace 文件树。
4. 展开 raw/sources/documents，能看到上传的 docx。
5. 点击 docx，能看到 source 详情。
6. 打开 Source Preview，能看到 Markdown 预览或 AI 摘要。
7. 展开 wiki/concepts，能看到生成的 md 文件。
8. 点击 md 文件，能渲染 Markdown。
9. 进入 Wiki 页面，默认阅读模式隐藏 frontmatter。
10. 切换源码模式，能看到完整 Markdown。
11. 切换编辑模式，保存后生成版本快照。
12. 进入知识图谱，默认显示思维导图。
13. 即使没有关系边，也能按页面类型显示节点。
14. 选择某个页面作为中心，能展开相关维度。
15. 点击图谱节点，右侧显示详情。
16. 双击节点，打开 Wiki 页面。
17. 搜索 mediapipe / rPPG 能返回结果。
18. Chat 回答时显示使用了哪些上下文。
19. Health Check 能检查 preview、file index、graph、wiki 一致性。
20. Dashboard 显示最近活动和推荐下一步。

============================================================
十八、输出要求
============================================================

完成后请输出：

1. 修改了哪些页面；
2. 新增了哪些后端服务；
3. 新增了哪些数据库字段或 migration；
4. Source Preview 的实现边界；
5. Markdown 渲染组件说明；
6. 思维导图数据结构说明；
7. RightContextPanel 组件说明；
8. 仍然存在的风险；
9. 手动测试步骤；
10. 如何运行和打包。

请优先保证功能真实可用，不要只做 UI 假数据。
本版本重点是：本地文件资产管理、Markdown 预览、Source Preview、思维导图式知识结构视图和工作台信息体验。