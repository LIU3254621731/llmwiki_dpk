<br />

````markdown
# LLM Knowledge Wiki 项目开发文档

> 项目暂定名：LLM Knowledge Wiki  
> 中文暂定名：智维 Wiki / LLM 知识维护工作台  
> 项目定位：本地优先的 LLM 驱动多 Agent 协作式 Markdown Wiki 维护工作台  
> 文档用途：项目开发、功能落地、参赛介绍、后续迭代、Codex/Claude Code 开发约束

---

## 1. 项目核心定义

LLM Knowledge Wiki 不是普通 RAG 问答工具，不是普通 Markdown 编辑器，不是 OCR 软件，不是本地 PDF 深度解析工具，也不是多模态图片理解系统。

本项目的核心定义是：

**一个本地优先的、由大模型驱动的、多 Agent 协作式 Markdown Wiki 维护工作台。**

软件的核心目标不是让用户“上传文档后临时问答”，而是让用户通过上传文档、提问、审阅和编辑，驱动一个本地 Markdown Wiki 持续演化。大模型负责理解文档、抽取知识、判断实体概念关系、生成 Wiki 更新建议；本地多 Agent Runtime 负责任务调度、结构化通信、状态管理、审阅、版本快照、文件写入和稳定性控制；用户通过专业、简洁的交互界面维护自己的长期知识资产。

本软件的四个核心支柱是：

```text
交互是入口；
本地多 Agent Runtime 是流程；
DeepSeek 等大模型是能力；
Markdown Wiki 是长期知识资产。
````

换句话说：

```text
用户负责意图、上传、提问、审阅和确认；
Agent 负责任务拆解、流程编排和结构化协作；
DeepSeek 负责文档理解、知识抽取、关系判断和内容生成；
本地系统负责存储、校验、审阅、写入、版本控制和安全。
```

***

## 2. 项目一句话介绍

**LLM Knowledge Wiki 是一个本地运行的 AI 知识库维护工作台，它通过 DeepSeek 等大模型读取用户上传的文档附件，并由本地多 Agent 协作系统将文档内容转化为可审阅、可回滚、可持续演化的 Markdown Wiki。**

更简短的宣传语：

```text
从文档到 Wiki，从问答到知识资产。
```

或者：

```text
上传、理解、审阅、沉淀。
```

***

## 3. 与传统 RAG 的区别

传统 RAG 的典型逻辑是：

```text
上传文件 → 向量化 → 提问时检索片段 → 临时生成答案
```

本软件的逻辑是：

```text
上传文档 → LLM 抽取结构化知识 → 多 Agent 消歧与关联 → 生成 Wiki 更新计划 → 审阅后写入 Markdown Wiki → 后续基于 Wiki 继续问答和维护
```

传统 RAG 的问题：

1. 每次提问都重新检索和拼接知识。
2. 知识没有长期沉淀。
3. 有价值回答容易留在聊天记录中，无法变成可维护资产。
4. 文档之间的概念关系、实体关系、冲突关系不容易长期积累。
5. 用户难以审阅 AI 对知识的处理过程。

本软件的优势：

1. 把一次性问答转化为长期 Wiki 资产。
2. 把 LLM 从聊天助手升级为知识库维护者。
3. 通过多 Agent 流程完成抽取、消歧、关联、更新、审阅、沉淀。
4. 所有修改可审阅、可追踪、可回滚。
5. 用户保留最终控制权。

***

## 4. 已确认的产品边界

本项目必须严格遵守以下边界。

### 4.1 明确要做的内容

本软件只做：

1. LLM 驱动的 Wiki 维护工作台。
2. 本地运行的多 Agent Runtime。
3. DeepSeek 文档附件输入。
4. Prompt + 文档附件 → DeepSeek → JSON → 本地审阅 → Markdown Wiki。
5. Markdown Wiki 的自动生成、更新、索引、日志和版本管理。
6. 文档导入后的实体、概念、主题、关系、事实主张抽取。
7. 本地去重、消歧、关系建立、知识图谱维护。
8. 审阅 Diff、版本快照、回滚。
9. 基于 Wiki 的问答。
10. 高价值回答沉淀为 Wiki 页面。
11. 本地搜索、基础图谱、健康检查和数据一致性检查。
12. Windows 一键安装、本地运行。

### 4.2 明确不做的内容

以下方向已经被否决，不允许重新加入：



3. 不做多模态图片理解。
4. 不做图片 caption。
9. 不以传统向量 RAG 作为核心架构。
10. 不做自由 Agent 群聊。
11. 不允许 Agent 直接修改本地 Wiki 文件。
12. 不允许 LLM 直接写入正式知识库。
13. 不做 Web 端部署。
14. 不做移动端。
15. 不做多人协作云端系统。
16. 不做复杂云同步。
17. 不要求用户安装 Node.js、Python、Rust、SQLite 或其他开发环境。
18. 不要求用户通过命令行启动。

***

## 5. 核心处理闭环

完整闭环如下：

```text
用户上传文档
    ↓
本地保存 Raw Source
    ↓
生成 source_id、file_hash
    ↓
根据知识库模板生成 Prompt
    ↓
Prompt + 文档附件提交给 DeepSeek
    ↓
DeepSeek 返回严格 JSON
    ↓
本地 JSON Schema 校验
    ↓
Candidate Search / Resolution 去重消歧
    ↓
Relationship 建立实体概念关系
    ↓
生成 Wiki Update Plan
    ↓
ReviewEngine 生成 Diff 审阅项
    ↓
用户确认或低风险自动应用
    ↓
WikiWriter 写入 Markdown Wiki
    ↓
VersionManager 保存快照
    ↓
IndexService / LogService 更新 index.md 和 log.md
    ↓
用户基于 Wiki 问答
    ↓
高价值回答沉淀为新 Wiki 页面
    ↓
Wiki 持续演化
```

***

## 6. 软件总体架构

### 6.1 推荐技术栈

```text
桌面框架：Tauri 2.x
前端：React + TypeScript
UI：Tailwind CSS + shadcn/ui
状态管理：Zustand
数据请求与缓存：TanStack Query
本地核心：Rust Core Runtime
数据库：SQLite
任务队列：Rust 本地任务队列 + SQLite 持久化状态
模型调用：Rust reqwest HTTP Client
JSON Schema 校验：Rust JSON Schema Validator
Markdown 编辑/预览：Milkdown / TipTap / Monaco + Markdown Preview
Diff 展示：文本 Diff 组件
知识图谱：React Flow / Cytoscape.js
密钥存储：Windows Credential Manager / 系统 Keychain
安装包：Tauri Bundler + NSIS / MSI
```

### 6.2 职责划分

React 前端只负责：

1. 页面渲染。
2. 用户交互。
3. 表单输入。
4. 状态展示。
5. Chat 输入输出。
6. 审阅 Diff 展示。
7. 图谱展示。
8. 设置界面。

Rust Core Runtime 负责：

1. AppKernel。
2. Workspace 管理。
3. SQLite 数据库。
4. TaskQueue。
5. AgentRuntime。
6. Coordinator 调度。
7. ModelGateway。
8. DeepSeekClient。
9. PromptBuilder。
10. PromptRegistry。
11. JSON Schema 校验。
12. Candidate Search。
13. Resolution Engine。
14. Relationship Engine。
15. Review Engine。
16. Diff Engine。
17. WikiWriter。
18. VersionManager。
19. IndexService。
20. LogService。
21. GraphService。
22. SearchService。
23. EventBus。
24. Recovery Check。
25. Workspace Reconcile。
26. 文件安全读写。
27. API Key 安全存储。

### 6.3 总体架构图

```text
React UI
  ↓ Tauri Commands / Events
Rust AppKernel
  ↓
TaskQueue
  ↓
CoordinatorAgent
  ↓
Specialized Agents
  ↓
ModelGateway
  ↓
DeepSeek API
  ↓
JsonSchemaValidator
  ↓
CandidateSearchEngine
  ↓
ResolutionEngine
  ↓
RelationshipEngine
  ↓
ReviewEngine
  ↓
WikiWriter
  ↓
VersionManager
  ↓
Markdown Wiki + SQLite
```

***

## 7. 本地多 Agent Runtime

### 7.1 Agent 的定义

本软件中的 Agent 不是自由聊天机器人，也不是独立运行的大模型实例。

Agent 的定义是：

```text
Agent = Role + Prompt + Tools + Context + Input Schema + Output Schema + State
```

它是一个本地任务处理模块，拥有明确职责、固定输入输出协议和受控工具权限。

### 7.2 本地运行的含义

“本地多 Agent”不是指本地运行多个大模型，而是指本地运行：

1. Agent Runtime。
2. TaskQueue。
3. PromptBuilder。
4. JSON Validator。
5. WikiWriter。
6. VersionManager。
7. EventBus。
8. Agent 调度逻辑。
9. SQLite 状态持久化。
10. 本地文件系统写入。

DeepSeek 通过 API 提供大模型能力。

### 7.3 Agent 运行规则

1. Agent 不允许自由互相调用。
2. Agent 不允许直接修改本地 Wiki。
3. Agent 不允许绕过 Coordinator。
4. Agent 只返回结构化 JSON 和 next\_actions。
5. Coordinator 根据 next\_actions 调度下一个 Agent。
6. 所有文件写入只能通过 WikiWriter。
7. 所有模型调用只能通过 ModelGateway。
8. 所有任务状态必须写入 SQLite。
9. 所有 Agent 输入输出必须保存到 `.runtime/tasks/{task_id}/`。

***

## 8. Agent 角色设计

### 8.1 CoordinatorAgent

职责：

1. 判断任务类型。
2. 拆解任务。
3. 调度专职 Agent。
4. 传递结构化输入。
5. 收集结构化输出。
6. 决定下一步流程。
7. 写入 task\_events。
8. 处理失败和重试。

### 8.2 SourceIngestAgent

职责：

1. 根据知识库模板构建 Ingest Prompt。
2. 将 Prompt + 文档附件提交给 DeepSeek。
3. 要求返回严格 JSON。
4. 抽取 source\_summary、entities、concepts、topics、claims、relationships、coverage\_report、proposed\_wiki\_updates。

### 8.3 ResolutionAgent

职责：

1. 判断新抽取实体、概念、主题是否与已有页面重复。
2. 基于候选页面、aliases、canonical\_name、index 片段进行消歧。
3. 输出 create\_new、update\_existing、add\_alias、append\_to\_existing、merge\_suggestion、skip、needs\_user\_review 等决策。

### 8.4 RelationshipAgent

职责：

1. 将实体、概念、主题之间的候选关系标准化。
2. 输出关系三元组。
3. 将 source 和 target 映射到 canonical Wiki page。
4. 控制图谱噪声。
5. 保留 evidence 和 confidence。

### 8.5 WikiUpdateAgent

职责：

1. 根据已消歧知识和关系生成 Wiki 更新计划。
2. 输出 wiki\_update\_plan。
3. 不直接写文件。
4. 标记 risk\_level 和 requires\_review。

### 8.6 ReviewAgent

职责：

1. 根据 Wiki Update Plan 生成 Review 和 Review Items。
2. 生成用户可读摘要。
3. 生成 Diff 数据。
4. 按风险等级分组。
5. 高风险修改必须进入审阅中心。

### 8.7 QueryAgent

职责：

1. 基于整个知识库、当前页面、指定文件、指定标签或指定目录回答问题。
2. 调用 DeepSeek 生成带引用的回答。
3. 支持将高价值回答保存为 Wiki 页面。

### 8.8 HealthCheckAgent

职责：

1. 检查重复页面。
2. 检查孤立页面。
3. 检查缺失引用。
4. 检查过时内容。
5. 检查冲突。
6. 检查图谱噪声。
7. 检查 index/log 不一致。
8. 只生成报告和建议，不直接修改 Wiki。

### 8.9 IndexService / LogService

IndexService 负责：

1. 更新 index.md。
2. 维护页面索引。
3. 维护摘要、aliases、related pages。

LogService 负责：

1. 追加 log.md。
2. 记录导入、问答、审阅、应用修改、健康检查等事件。

***

## 9. Agent 通信协议

Agent 之间不直接自由对话。通信方式如下：

1. Coordinator 统一调度。
2. Agent 读取结构化 input.json。
3. Agent 输出结构化 output.json。
4. 所有输入输出保存到 `.runtime/tasks/{task_id}/`。
5. 所有开始、完成、失败写入 task\_events。
6. 前端通过 EventBus 展示 Agent Activity Timeline。

### 9.1 AgentMessage 格式

```json
{
  "message_id": "",
  "task_id": "",
  "from_agent": "",
  "to_agent": "",
  "message_type": "",
  "payload": {},
  "created_at": ""
}
```

### 9.2 message\_type

```text
SOURCE_INGEST_REQUEST
SOURCE_INGEST_RESULT
RESOLUTION_REQUEST
RESOLUTION_RESULT
RELATIONSHIP_REQUEST
RELATIONSHIP_RESULT
WIKI_UPDATE_REQUEST
WIKI_UPDATE_PLAN
REVIEW_REQUEST
REVIEW_CREATED
QUERY_REQUEST
QUERY_RESULT
HEALTH_CHECK_REQUEST
HEALTH_CHECK_REPORT
ERROR
```

***

## 10. 文件处理策略

### 10.1 文档类文件

支持格式：

```text
PDF
DOCX
Markdown
TXT
HTML
```

处理方式：

1. 保存到 `raw/sources/documents/`。
2. 生成 source\_id。
3. 计算 SHA256 file\_hash。
4. 双路径文本提取：优先使用 lopdf 提取文本层，若 PDF 为扫描件/图片型（无文本层）则自动回退到 Windows 内置 OCR（通过提取页面内嵌图片 + Windows.Media.Ocr 识别）。
5. 创建 Source Ingest Task。
6. 作为附件提交给 DeepSeek。
7. 等待 DeepSeek 返回结构化 JSON。
8. 进入本地校验、消歧、审阅、写入流程。

### 10.2 图片类文件

支持格式：

```text
PNG
JPG
JPEG
WEBP
GIF
```

处理方式：

1. 保存到 `raw/assets/images/`。
2. 生成 asset\_id。
3. 不 OCR。
4. 不视觉理解。
5. 不参与知识抽取。
6. 可由用户手动插入 Wiki 页面或作为附件保存。
7. UI 必须明确提示：图片仅保存为资产，不参与 AI 知识抽取。

### 10.3 文件上传前检查

必须检查：

1. 文件是否存在。
2. 文件是否可读。
3. 文件大小是否超限。
4. 扩展名是否支持。
5. 是否重复导入。
6. 文件是否被占用。
7. 中文路径、空格路径是否兼容。
8. Windows 长路径问题。
9. OneDrive 路径冲突风险。

***

## 11. Workspace 目录结构

每个知识库目录结构如下：

```text
workspace/
  raw/
    sources/
      documents/
      webclips/
    assets/
      images/
      attachments/
  wiki/
    index.md
    log.md
    overview.md
    sources/
    concepts/
    entities/
    topics/
    questions/
    reviews/
  schema/
    AGENTS.md
    WIKI_SCHEMA.md
    PROMPTS.md
    TEMPLATE.yaml
  drafts/
    ingest/
    wiki_updates/
  versions/
    snapshots/
    pages/
  .runtime/
    tasks/
    logs/
  db/
    app.sqlite
  config/
    kb.config.json
```

说明：

- `raw/`：保存原始文件，是事实来源，不允许 AI 修改。
- `wiki/`：保存正式 Markdown Wiki，是长期知识资产。
- `schema/`：保存模板、Agent 规则、Prompt 规则、页面规范。
- `drafts/`：保存待审阅草稿。
- `versions/`：保存页面版本快照和回滚数据。
- `.runtime/`：保存任务运行时输入、Prompt、模型原始输出、结构化输出和错误日志。
- `db/`：保存 SQLite 数据库。
- `config/`：保存知识库配置。

***

## 12. SQLite 数据库设计

必须包含以下表或等价结构：

```text
knowledge_bases
sources
assets
wiki_pages
knowledge_items
aliases
relationships
tasks
task_events
reviews
review_items
versions
operations
graph_nodes
graph_edges
model_profiles
schema_migrations
```

### 12.1 关键表说明

#### knowledge\_bases

保存知识库基本信息。

字段：

```text
id
name
path
template_name
created_at
updated_at
```

#### sources

保存原始文档信息。

字段：

```text
id
kb_id
file_name
file_path
file_type
file_size
file_hash
status
created_at
updated_at
```

#### assets

保存图片等非抽取资产。

字段：

```text
id
kb_id
file_name
file_path
file_type
file_size
file_hash
created_at
```

#### wiki\_pages

保存 Wiki 页面索引。

字段：

```text
id
kb_id
title
path
page_type
canonical_name
tags
content_hash
created_at
updated_at
```

#### knowledge\_items

保存规范化实体、概念、主题等知识项。

字段：

```text
id
kb_id
canonical_name
item_type
page_path
summary
created_at
updated_at
```

#### aliases

保存别名映射，用于去重消歧。

字段：

```text
id
item_id
alias
normalized_alias
language
created_at
```

#### relationships

保存知识关系。

字段：

```text
id
kb_id
source_item_id
target_item_id
relation
evidence_source_id
evidence_location
confidence
status
created_at
```

#### tasks

保存后台任务状态。

字段：

```text
id
kb_id
task_type
status
current_agent
model_profile_id
input_ref
output_ref
error_message
retry_count
created_at
updated_at
locked_at
completed_at
```

#### task\_events

保存任务事件流。

字段：

```text
id
task_id
event_type
agent_name
message
created_at
```

#### reviews / review\_items

保存审阅任务和 Diff。

#### versions

保存页面版本快照。

#### operations

保存写入操作，用于防止重复应用。

#### model\_profiles

保存模型配置，但 API Key 不得明文存储，只保存 encrypted\_api\_key\_ref 或系统凭据引用。

***

## 13. 任务状态机

所有后台任务必须持久化，状态包括：

```text
created
queued
locked
prompt_built
sent_to_model
model_returned
json_validating
json_valid
json_invalid
json_repairing
json_repaired
candidate_searching
resolution_running
relationship_running
update_plan_generating
review_generating
review_pending
applying
applied
failed
cancelled
interrupted
```

每个任务目录：

```text
.runtime/tasks/{task_id}/
  input.json
  prompt.md
  model_raw_response.txt
  ingest_result.json
  resolution_result.json
  relationship_result.json
  update_plan.json
  review_items.json
  error.log
```

软件启动时必须执行 Recovery Check：

1. 扫描 interrupted / queued / locked / applying 状态任务。
2. 对模型调用中断任务标记 interrupted，可重试。
3. 对 review\_pending 任务恢复审阅。
4. 对 applying 中断任务检查 operation\_id 和版本快照，避免重复写入。
5. 允许用户继续、重试、取消或回滚。

***

## 14. ModelGateway 与 DeepSeek

所有模型调用必须经过统一 ModelGateway。

### 14.1 ModelGateway 职责

1. API Key 注入。
2. Base URL 管理。
3. 模型选择。
4. 任务类型到模型路由。
5. 超时处理。
6. 自动重试。
7. 指数退避。
8. 限流处理。
9. 错误归一化。
10. 流式输出。
11. 附件上传。
12. 原始响应保存。
13. JSON 修复。
14. Token usage 统计或估算。
15. 用户可读错误提示。

### 14.2 必须处理的错误

```text
timeout
429
5xx
余额不足
API Key 无效
附件过大
上下文过长
模型暂不可用
网络断开
JSON 非法
文件上传失败
```

错误提示必须具体，例如：

```text
DeepSeek 返回 429：请求过于频繁，请稍后重试。
```

而不是：

```text
请求失败
```

***

## 15. JSON 输出与校验机制

所有 LLM 输出必须优先返回严格 JSON。禁止只返回普通自然语言。

非法 JSON 处理流程：

```text
严格 JSON parse
  ↓
自动提取 JSON block
  ↓
调用 JSON Repair 一次
  ↓
仍失败则 task failed
  ↓
保存 model_response_raw
  ↓
允许用户查看原始输出并重试
```

### 15.1 Source Ingest Task 输出

必须包含：

```text
source_summary
coverage_report
entities
concepts
topics
claims
relationships
proposed_wiki_updates
conflicts
questions_for_user
```

coverage\_report 必须包含：

```text
document_sections_seen
possibly_missing_sections
confidence_in_coverage
notes
```

claims 必须包含：

```text
claim
confidence
source_id
location
citation_status
```

citation\_status：

```text
model_reported
user_verified
verified
missing
```

由于系统不做本地 PDF 深度解析，默认引用位置多为 `model_reported`，UI 可显示：

```text
来源位置由模型报告，未经过本地解析验证。
```

***

## 16. Prompt 设计原则

Prompt 必须定义以下概念：

### Source

用户上传原始资料，是事实来源，不允许修改。

### Entity

明确指代对象，如人物、组织、论文、项目、产品、数据集、方法、软件库。

### Concept

抽象知识点、理论、机制、原则、术语、方法类别。

### Topic

更高层主题集合。

### Claim

可验证事实性陈述，需要来源。

### Citation

来源位置，默认 `model_reported`。

### Conflict

新 claim 与已有 Wiki 不一致或存在张力。

### Wiki Page

要创建或更新的 Markdown 页面。

### Wiki Update Plan

页面创建、修改、合并或跳过计划。

### Alias

等价名称、翻译、缩写、大小写变体。相关概念、上下位概念、实现方式、同主题概念不能作为 alias。

### Relationship

标准化关系，必须使用 relation enum。

### Prompt 必须要求模型

1. 不要在未检查 existing\_candidates 前新建页面。
2. 不要把相关概念当 alias。
3. 不要把子概念当 alias。
4. 没有 evidence 的关系不要输出为可写入关系。
5. 优先返回 JSON，不要解释性文字。
6. 所有实体、概念、关系、Claim 尽量给出 evidence。
7. 模型如果不确定，应标记 confidence 为 low 或 requires\_review。

***

## 17. 去重、消歧与页面创建规则

### 17.1 为什么需要消歧

LLM 容易重复创建页面，例如：

```text
Self-Attention
self attention
自注意力
自注意力机制
Self Attention Mechanism
```

如果不处理，会造成知识库污染。

### 17.2 Candidate Search

创建页面前必须执行 Candidate Search。

候选依据：

```text
title
canonical_name
aliases
normalized_name
slug
page_type
source metadata
index.md 摘要
SQLite knowledge_items
字符串相似度
```

### 17.3 Resolution 决策

ResolutionAgent 输出：

```text
create_new
update_existing
append_to_existing
add_alias
merge_suggestion
skip
needs_user_review
```

### 17.4 Alias 规则

```text
完全同义：可自动添加
翻译同义：可低风险应用
缩写同义：需要确认
相近概念：不能作为 alias，只能 related_to
上下位概念：不能作为 alias，只能 is_a / part_of
同主题概念：不能作为 alias
无法判断：进入审阅中心
```

### 17.5 页面创建门槛

只有满足以下条件才创建独立页面：

1. 具有明确独立定义。
2. 未来可能被多次引用。
3. 不是泛泛词汇。
4. 不是普通章节标题。
5. 不是单次出现的边缘名词。
6. 不是已有页面中的普通段落内容。
7. 未发现语义等价页面。

***

## 18. 关系建立与知识图谱

### 18.1 关系类型

支持固定 relation enum：

```text
is_a
part_of
uses
depends_on
improves
compares_with
contradicts
cites
mentions
related_to
has_alias
belongs_to_topic
evaluated_on
proposed_by
applies_to
derived_from
```

### 18.2 关系写入规则

1. source/target 必须映射到 canonical Wiki page。
2. 没有 evidence 不写入。
3. 低置信度进入审阅。
4. 每次导入高置信关系最多 20 条。
5. 每次导入 related\_to 最多 5 条。
6. 关系写入 graph\_edges。
7. 同时更新 Markdown Related Pages。
8. 避免图谱变成无意义的“全连接图”。

***

## 19. Wiki 页面模板

不要让模型自由生成最终 Markdown。模型提供结构化内容块，本地模板渲染页面。

### 19.1 Frontmatter 示例

```yaml
---
title: Self-Attention
type: concept
canonical_name: Self-Attention
aliases:
  - self attention
  - 自注意力
sources:
  - src_xxx
tags:
  - attention
confidence: medium
status: active
created:
updated:
last_updated_by_task:
---
```

### 19.2 稳定 Section

```markdown
# Title

## Summary <!-- section:summary -->

## Definition <!-- section:definition -->

## Key Points <!-- section:key_points -->

## Evidence <!-- section:evidence -->

## Related Pages <!-- section:related_pages -->

## Source Contributions <!-- section:source_contributions -->

## Open Questions <!-- section:open_questions -->
```

WikiWriter patch 时必须使用 section id，避免标题变化导致 patch 失败。

***

## 20. 审阅与风险控制

### 20.1 维护模式

#### Strict Mode

所有修改都需要审阅。

#### Balanced Mode

默认推荐。低风险可自动；已有正文修改、冲突、合并需审阅。

#### Auto Mode

低中风险自动，高风险必须审阅，全部可回滚。

### 20.2 风险等级

低风险：

```text
新增 source summary
添加 alias
添加 related link
追加 log
新建无冲突页面
新增标签
```

中风险：

```text
修改已有 section
调整页面结构
添加上下位关系
保存问答为页面
```

高风险：

```text
合并页面
删除页面
覆盖核心定义
修改 canonical_name
标记 contradiction
重命名页面
覆盖旧结论
```

### 20.3 审阅中心功能

必须支持：

1. 按任务分组。
2. 按页面分组。
3. 按风险等级分组。
4. 高风险置顶。
5. 低风险折叠。
6. 一键接受低风险。
7. 逐条接受。
8. 全部拒绝。
9. 编辑后接受。
10. 要求重新生成。
11. 查看 Prompt。
12. 查看模型原始输出。
13. 查看来源。
14. 查看版本快照。
15. 回滚。

每个修改必须说明：

```text
改了什么？
为什么改？
基于哪个 source？
能否撤销？
```

***

## 21. WikiWriter 与版本安全

所有写入必须通过 WikiWriter。

### 21.1 WikiWriter 流程

```text
获取页面锁
  ↓
读取当前页面
  ↓
检查 base_version_hash
  ↓
创建版本快照
  ↓
校验 update_plan
  ↓
生成目标 Markdown
  ↓
写临时文件
  ↓
原子 rename 替换正式文件
  ↓
更新 SQLite
  ↓
更新 index.md
  ↓
追加 log.md
  ↓
释放锁
```

### 21.2 Operation 幂等性

每个 operation 必须有：

```text
operation_id
operation_hash
target_path
source_task_id
applied_at
```

应用前检查：

1. operation\_hash 是否已应用。
2. 页面是否锁定。
3. 当前页面 hash 是否等于生成 Diff 时的 base\_version\_hash。
4. alias 是否已存在。
5. relationship 是否已存在。
6. source\_id 是否已写入页面。

如果页面被用户手动修改，不能直接应用旧 Diff，必须提示重新生成 Diff。

***

## 22. Chat / QueryAgent

Chat 不是软件全部，只是入口之一。

### 22.1 问答范围

支持：

```text
整个知识库
当前页面
指定文件
指定标签
指定目录
```

### 22.2 QueryAgent 流程

```text
用户提问
  ↓
选择问答范围
  ↓
读取相关 Wiki 页面 / Source Summary / aliases / related pages
  ↓
构造 Query Prompt
  ↓
调用 DeepSeek
  ↓
返回带 citations 的 JSON
  ↓
显示本次使用上下文
  ↓
用户选择保存为 Wiki 页面或追加到页面
```

保存为 Wiki 页面也必须经过：

```text
Review
→ WikiWriter
→ VersionManager
→ index.md / log.md
```

***

## 23. Search / Health / Reconcile

### 23.1 搜索

先实现本地全文搜索，覆盖：

```text
Wiki 页面
Source Summary
index.md
log.md
标题
标签
frontmatter
aliases
```

### 23.2 HealthCheckAgent

检查：

1. 重复页面。
2. 孤立页面。
3. 缺引用。
4. 过时内容。
5. 冲突。
6. 图谱噪声。
7. index/log 不一致。

只生成报告，不直接修改。

### 23.3 WorkspaceReconcile

检查：

1. SQLite 中记录的页面是否存在。
2. Markdown 文件是否有数据库记录。
3. index.md 是否包含所有页面。
4. log.md 是否缺失关键操作。
5. versions 是否完整。
6. raw sources 是否存在。
7. source hash 是否匹配。
8. aliases 是否重复。
9. relationships 是否指向不存在页面。
10. orphan pages。
11. missing citations。
12. unresolved conflicts。

***

## 24. UI 页面与功能绑定

### 24.1 Dashboard

显示：

1. 知识库统计。
2. Source 数量。
3. Wiki 页面数量。
4. 待审阅任务。
5. 最近导入。
6. 最近更新页面。
7. 最近任务。
8. 快捷操作。

### 24.2 Sources

功能：

1. 上传文件。
2. 查看 source 列表。
3. 重新导入。
4. 查看 source summary。
5. 查看关联页面。
6. 删除 source。
7. 复制引用路径。

### 24.3 ImportTasks

展示 Agent Activity Timeline：

```text
已保存原始文件
已生成 source_id
已构建 Prompt
已上传附件到 DeepSeek
等待模型返回 JSON
正在校验 JSON
正在执行 Candidate Search
ResolutionAgent 正在消歧
RelationshipAgent 正在建立关系
WikiUpdateAgent 正在生成更新计划
ReviewAgent 已生成审阅项
等待用户审阅
```

### 24.4 Review

展示：

1. Diff。
2. 风险等级。
3. 来源。
4. 修改原因。
5. 接受/拒绝/重新生成。
6. Prompt 和模型原始输出。
7. 版本快照。
8. 回滚。

### 24.5 Wiki

功能：

1. 浏览。
2. 编辑。
3. 预览。
4. 查看 frontmatter。
5. 查看相关页面。
6. 查看反向链接。
7. 查看来源引用。
8. 调用 AI 页面操作。

### 24.6 Chat

功能：

1. 问答。
2. 选择上下文范围。
3. 显示引用。
4. 显示使用上下文。
5. 保存回答为 Wiki 页面。
6. 追加回答到已有页面。

### 24.7 Graph

显示：

1. graph\_nodes。
2. graph\_edges。
3. 节点筛选。
4. 边筛选。
5. evidence 查看。
6. 孤立节点。
7. 高连接页面。

### 24.8 Search

功能：

1. 本地全文搜索。
2. 标题搜索。
3. 标签搜索。
4. alias 搜索。
5. frontmatter 搜索。

### 24.9 Settings

包括：

1. DeepSeek 配置。
2. 连接测试。
3. JSON 输出测试。
4. 附件能力测试。
5. 模型角色分配。
6. 知识库路径。
7. 审阅模式。
8. Prompt 设置。
9. Agent 设置。
10. 外观设置。
11. 日志目录。

### 24.10 HealthCheck

功能：

1. 运行健康检查。
2. 查看报告。
3. 查看修复建议。
4. 生成审阅任务。

### 24.11 TaskDetail

显示：

1. input。
2. prompt。
3. raw output。
4. validated output。
5. task events。
6. error。
7. 重试/取消/恢复。

***

## 25. 初始化向导

第一次启动显示：

### Step 1：欢迎页面

说明这是 LLM 驱动的 Wiki 维护工作台。

### Step 2：选择数据目录

默认使用用户文档目录下的 LLM Knowledge Wiki。

### Step 3：配置 DeepSeek

输入 API Key、Base URL、测试连接、测试附件能力。

### Step 4：创建第一个知识库

输入知识库名称。

### Step 5：选择知识库模板

可选：

1. 通用知识库。
2. 科研论文知识库。
3. 课程学习知识库。
4. 项目管理知识库。
5. 读书笔记知识库。

### Step 6：完成

进入工作台首页。

***

## 26. 知识库模板系统

模板决定：

1. 默认目录结构。
2. 页面类型。
3. YAML frontmatter 字段。
4. Agent 抽取策略。
5. Prompt 内容。
6. 审阅规则。
7. 图谱节点类型。
8. 图谱边类型。
9. 默认问答风格。
10. 默认页面模板。

内置模板：

1. 通用知识库。
2. 科研论文知识库。
3. 课程学习知识库。
4. 项目管理知识库。
5. 读书笔记知识库。

### 26.1 科研论文模板示例

```yaml
name: research_paper
display_name: 科研论文知识库
description: 用于论文阅读、综述写作和研究方向管理
default_language: zh-CN

directories:
  - wiki/papers
  - wiki/concepts
  - wiki/methods
  - wiki/datasets
  - wiki/questions
  - wiki/reviews

page_types:
  paper:
    fields:
      - title
      - authors
      - year
      - venue
      - research_problem
      - method
      - dataset
      - metrics
      - results
      - limitations
      - sources
  concept:
    fields:
      - name
      - definition
      - related_papers
      - related_methods
      - sources

review_policy:
  require_review_for_new_page: false
  require_review_for_update: true
  require_review_for_conflict: true

graph:
  node_types:
    - paper
    - concept
    - method
    - dataset
    - source
  edge_types:
    - cites
    - uses_method
    - related_to
    - contradicts
```

知识库创建后如果要切换模板，必须作为迁移操作处理，需要生成迁移预览，不能直接强制修改已有页面结构。

***

## 27. 模型能力检测

设置页必须提供：

1. DeepSeek API 连接测试。
2. 普通聊天测试。
3. JSON 输出测试。
4. PDF 附件上传测试。
5. DOCX 附件上传测试。
6. 最大文件大小提示。
7. 超时与错误提示。
8. 如果当前模型不支持文档附件，则禁用文档导入任务并明确提示。

***

## 28. 长文档覆盖报告

由于不做本地 PDF 深度解析，模型可能无法保证完整阅读长文档。因此 SourceIngestAgent 的输出必须包含 coverage\_report。

示例：

```json
{
  "coverage_report": {
    "document_sections_seen": ["Abstract", "Introduction", "Method", "Experiments"],
    "possibly_missing_sections": ["Appendix"],
    "confidence_in_coverage": "medium",
    "notes": "模型认为主要章节已覆盖，但附录内容可能未充分分析。"
  }
}
```

UI 中应展示覆盖置信度，让用户知道模型是否可能遗漏内容。

***

## 29. 日志与隐私安全

必须有：

1. 应用日志。
2. 任务日志。
3. 模型调用日志。
4. 错误日志。
5. 审阅日志。
6. 版本日志。

日志必须脱敏：

1. API Key。
2. Authorization Header。
3. 用户敏感路径可部分隐藏。
4. 不记录完整密钥。
5. Prompt Snapshot 中不得包含 API Key。

API Key 必须使用系统凭据存储，不允许明文落盘。

***

## 30. Windows 安装与兼容性

目标平台：

```text
Windows 10 / Windows 11
```

必须支持：

1. 一键安装。
2. 桌面快捷方式。
3. 开始菜单快捷方式。
4. 无需安装开发环境。
5. 无需命令行启动。
6. 用户数据保存在用户目录。
7. 卸载软件不默认删除知识库。

必须考虑：

1. 中文路径。
2. 空格路径。
3. OneDrive 同步路径。
4. 无管理员权限。
5. 文件被占用。
6. 路径过长。
7. 杀毒软件拦截。
8. 安装目录无写入权限。
9. AppData 权限问题。
10. 用户文档目录不存在或不可写。

***

## 31. 已讨论问题与解决方案汇总

### 31.1 问题：模型返回内容与已有内容重复

解决方案：

1. 文件级 hash 去重。
2. source metadata 去重。
3. canonical\_name。
4. aliases。
5. normalized\_name。
6. Candidate Search。
7. ResolutionAgent。
8. 页面创建门槛。
9. merge 必须审阅。
10. 相关概念不能当 alias。

### 31.2 问题：实体概念之间无法建立有效联系

解决方案：

1. SourceIngestAgent 输出 relationships。
2. 使用固定 relation enum。
3. RelationshipAgent 标准化关系。
4. source/target 映射 canonical Wiki page。
5. 写入 graph\_edges。
6. 更新 Markdown Related Pages。
7. 没有 evidence 不写入。
8. 限制 related\_to 数量。

### 31.3 问题：多 Agent 后台维护不稳定

解决方案：

1. LLM 只生成维护计划。
2. 本地系统执行计划。
3. 所有任务状态持久化。
4. Agent 不自由互相调用。
5. Coordinator 统一调度。
6. JSON Schema 校验。
7. Review Diff。
8. WikiWriter 统一写入。
9. VersionManager 快照。
10. Recovery Check。

### 31.4 问题：模型 JSON 输出不稳定

解决方案：

1. 严格 JSON parse。
2. 自动提取 JSON block。
3. JSON Repair 一次。
4. 失败则 task failed。
5. 保存 raw output。
6. 允许用户重试。

### 31.5 问题：用户手动编辑被 AI 覆盖

解决方案：

1. 每个 Review Item 保存 base\_version\_hash。
2. 应用前检查当前页面 hash。
3. 如果页面已变更，拒绝直接应用。
4. 提示重新生成 Diff。

### 31.6 问题：多任务同时修改同一页面

解决方案：

1. 页面锁。
2. Apply 阶段串行。
3. operation\_id。
4. operation\_hash。
5. 幂等检查。

### 31.7 问题：图谱变成垃圾图谱

解决方案：

1. 固定关系类型。
2. 没有 evidence 不写入。
3. 低置信关系审阅。
4. 每次导入限制关系数量。
5. related\_to 限制最多 5 条。

### 31.8 问题：模型编造引用位置

解决方案：

1. citation\_status 分级。
2. 默认 model\_reported。
3. UI 明确显示未本地验证。
4. 无引用 claim 不自动写入。
5. 用户可标记 user\_verified。

### 31.9 问题：长文档遗漏

解决方案：

1. coverage\_report。
2. sections\_seen。
3. possibly\_missing\_sections。
4. confidence\_in\_coverage。
5. 用户可重新导入或补充说明。

### 31.10 问题：安装后普通用户无法运行

解决方案：

1. Tauri 一键安装包。
2. 打包所有运行依赖。
3. 不依赖 Node/Python/Rust。
4. 数据目录放用户文档。
5. API Key 存系统凭据。
6. Windows 路径兼容。

***

## 32. 开发落实清单

### 32.1 Core Runtime

- [ ] AppKernel
- [ ] WorkspaceService
- [ ] DatabaseService
- [ ] SecretService
- [ ] TaskQueue
- [ ] EventBus
- [ ] AgentRuntime
- [ ] RecoveryCheck
- [ ] WorkspaceReconcile

### 32.2 Model

- [ ] ModelGateway
- [ ] DeepSeekClient
- [ ] 连接测试
- [ ] JSON 输出测试
- [ ] 附件能力测试
- [ ] 错误归一化
- [ ] usage 统计
- [ ] JSON Repair

### 32.3 Agent

- [ ] CoordinatorAgent
- [ ] SourceIngestAgent
- [ ] ResolutionAgent
- [ ] RelationshipAgent
- [ ] WikiUpdateAgent
- [ ] ReviewAgent
- [ ] QueryAgent
- [ ] HealthCheckAgent

### 32.4 Wiki

- [ ] WikiWriter
- [ ] VersionManager
- [ ] IndexService
- [ ] LogService
- [ ] MarkdownTemplate
- [ ] 页面锁
- [ ] operation 幂等性

### 32.5 Review

- [ ] ReviewEngine
- [ ] DiffEngine
- [ ] 风险分级
- [ ] 审阅中心操作
- [ ] 回滚

### 32.6 Search / Graph

- [ ] CandidateSearchEngine
- [ ] 本地全文搜索
- [ ] GraphService
- [ ] graph\_nodes
- [ ] graph\_edges
- [ ] 关系 evidence 展示

### 32.7 UI 绑定

- [ ] Dashboard 数据绑定
- [ ] Sources 上传与状态
- [ ] ImportTasks 事件流
- [ ] Review Diff
- [ ] Wiki 浏览编辑
- [ ] Chat 问答
- [ ] Graph 展示
- [ ] Search 搜索
- [ ] Settings 配置
- [ ] HealthCheck 报告
- [ ] TaskDetail 调试信息

***

## 33. 最终验收标准

软件完成后必须满足：

1. 能启动桌面应用。
2. 能初始化知识库目录。
3. 能安全保存 DeepSeek API Key。
4. 能测试连接和 JSON 输出。
5. 能上传 PDF/DOCX/MD/TXT/HTML。
6. 能保存 source 并计算 hash。
7. 能创建任务并显示事件流。
8. 能调用 DeepSeek 附件分析。
9. 能校验 JSON，失败可 repair。
10. 能生成 source summary。
11. 能执行候选检索和消歧。
12. 能建立标准化关系。
13. 能生成 review diff。
14. 能接受审阅并写入 Markdown。
15. 能更新 index.md 和 log.md。
16. 能保存版本快照并回滚。
17. 能基于 Wiki 问答。
18. 能保存回答为 Wiki 页面。
19. 图片只保存 asset，不触发 OCR/识别。
20. 关闭重开可恢复任务。
21. 不会重复应用同一 operation。
22. 不会覆盖用户手动编辑。
23. 所有核心错误有明确提示。
24. API Key 不明文落盘。
25. 不改变既有 UI 风格。
26. Windows 普通用户可一键安装运行。
27. 用户无需安装开发环境。
28. 卸载软件不默认删除知识库。

***

## 34. 参赛展示重点

本项目在参赛或介绍时应突出以下价值：

1. 从“文档问答”升级为“Wiki 自动维护”。
2. 从“一次性回答”升级为“长期知识资产沉淀”。
3. 从“单 Agent 聊天”升级为“本地多 Agent 协作式知识维护”。
4. 从“黑箱 AI 改写”升级为“结构化 JSON + Diff 审阅 + 版本回滚”。
5. 从“普通 RAG 检索”升级为“文档导入阶段的知识编译与 Wiki 更新”。
6. 从“聊天记录”升级为“可编辑、可追踪、可复用的 Markdown Wiki”。
7. 本地优先，用户数据保存在本地。
8. LLM 只生成计划，本地系统执行计划，保证稳定性和安全性。
9. 支持知识库模板，适配科研、课程、项目、读书、通用知识管理。
10. 适合学习、科研、项目管理、个人知识管理、团队文档整理等场景。

***

## 35. 项目开发原则

1. 不做临时 Demo 架构。
2. 不把核心状态机放到前端。
3. 不让 LLM 直接改文件。
4. 不让 Agent 自由群聊。
5. 不加入多模态图片理解、通用视觉识别。PDF 扫描件 OCR 除外（v0.2.1 新增，用于提升扫描件 PDF 可处理性）。
6. 所有模型输出必须 Schema 校验。
7. 所有写入必须可审阅、可追踪、可回滚。
8. 所有任务必须可恢复、可重试、可取消。
9. 所有 API Key 必须安全存储。
10. UI 保持现有基准风格，不重新设计。

***

## 36. 总结

LLM Knowledge Wiki 的真正难点不是 UI，也不是简单调用 DeepSeek，而是如何把不稳定的大模型输出包进一个稳定的本地维护协议中。

因此本项目的核心工程目标是：

```text
用本地 Agent Runtime 组织流程；
用 DeepSeek 提供理解能力；
用 JSON Schema 约束输出；
用 Review Diff 保证可解释；
用 WikiWriter 控制写入；
用 VersionManager 保证回滚；
用 Markdown Wiki 沉淀长期知识资产。
```

最终软件应该成为一个真正可用的本地知识维护工作台，而不是一个普通的“上传文件问答”应用。

```
```

