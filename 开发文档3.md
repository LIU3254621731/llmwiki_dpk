你是一名资深 Tauri + React + Rust 桌面软件工程师。当前项目已经完成基础 UI、任务流程和部分 Agent Pipeline，但从当前运行截图看，软件仍存在较多核心逻辑缺口和 UI/功能不一致问题。请基于现有代码继续开发，不要推倒重写，不要改变整体浅色、蓝白灰、简洁工作台风格。本次开发版本号为 v0.1.2，目标是修复当前关键 bug，并补齐“本地知识库管理系统”的完整基础逻辑。

============================================================
一、本次 v0.1.2 的总体目标
============================================================

当前软件已经具备：
1. 首页统计卡片。
2. 文件上传后生成任务。
3. 任务中间文件展示。
4. Agent 事件时间线。
5. 审阅列表。
6. Wiki 页面列表。
7. 知识图谱空页面。
8. 部分 DeepSeek 分析流程。

但目前存在以下明显问题：

1. Wiki 页面没有真正成功创建或路径不正确。
2. 点击 Wiki 页面出现“读取页面失败：系统找不到指定的路径 os error 3”。
3. 审阅界面信息过少，只显示文件名和风险，没有显示 AI 修改原因、摘要、来源、Diff 内容、接受后的结果。
4. 审阅任务有 15 项，但首页显示待审阅 13 项，统计可能不一致。
5. 知识图谱页面显示暂无数据，说明 relationship_result.json 没有正确写入 graph_nodes / graph_edges，或图谱页面没有读取。
6. Wiki 页面列表只显示少量页面，且路径类似 concept · wiki\xxx.md，说明页面路径规范和读取逻辑存在问题。
7. 首页缺少“知识库管理 / 创建不同类型知识库 / 切换知识库”的完整界面。
8. 缺少原始文件管理页面和完整 source 管理逻辑。
9. 缺少“原始文件统一转成 Markdown 预览”的界面和缓存逻辑。
10. 当前软件还没有真正建立起本地知识库管理系统的完整闭环。
11. 当前 UI 与最初设想的“左侧导航 + 中间工作区 + 右侧上下文面板 + 可审阅 Wiki 维护工作台”仍有明显差距。
12. 当前任务中间文件虽然展示出来了，但没有把这些中间结果转化为用户可理解、可审阅、可应用的知识库内容。

本次 v0.1.2 必须完成：

1. 修复 Wiki 页面读取失败 bug。
2. 修复 Wiki 页面创建和路径规范问题。
3. 补齐本地知识库管理系统。
4. 首页新增知识库创建、切换、类型模板选择界面。
5. 新增或完善原始文件管理页面。
6. 新增“文件转 Markdown 预览 / Source Preview”页面。
7. 完善审阅中心，真正显示 AI 修改计划、Diff、来源、原因、风险和应用结果。
8. 审阅接受后必须真正写入 Markdown Wiki，并能在 Wiki 页面打开。
9. relationship_result.json 必须写入 graph 数据，知识图谱页面要能显示节点和关系。
10. 任务中间文件不只是下载/查看，而要成为任务详情页中的可解释结果。
11. 保持已确认的产品核心：LLM 驱动的 Wiki 维护工作台，本地多 Agent Runtime，DeepSeek 文档附件输入，Markdown Wiki 长期资产。
12. 不要加入 OCR、多模态图片理解、本地 PDF 深度解析、图片 caption、自由 Agent 群聊。

============================================================
二、必须修复的关键 Bug
============================================================

Bug 1：Wiki 页面路径读取失败

现象：
点击 Wiki 页面“远程光电容积脉搏波_(rPPG)”后，页面正文显示：

加载失败：读取页面失败：系统找不到指定的路径。 (os error 3)

说明：
当前 wiki_pages 表或前端页面列表中保存的 path 可能是相对路径、Windows 反斜杠路径、错误路径、未创建文件路径，或者 WikiWriter 实际没有写入对应 Markdown 文件。

修复要求：

1. 统一 Wiki 页面路径规范。
2. 数据库中的 wiki_pages.path 必须保存相对于 workspace 的规范路径，例如：
   wiki/concepts/remote-photoplethysmography-rppg.md
3. 不要保存混乱路径，例如：
   wiki\远程光电容积脉搏波_(rPPG).md
   concept · wiki\远程光电容积脉搏波_(rPPG).md
4. UI 可以显示中文标题，但文件路径必须安全规范。
5. 实现 slugify 函数：
   - 英文转小写 kebab-case；
   - 中文标题可以使用拼音、hash 后缀或安全转义；
   - 去除非法字符；
   - 避免 Windows 路径非法字符；
   - 避免重名，重名时追加短 hash。
6. WikiWriter 应确保创建父目录。
7. 创建页面时必须真正写入 Markdown 文件。
8. 读取页面时必须基于 workspace root + relative path 拼接，不允许直接用 UI 显示路径。
9. 所有路径处理必须统一走 PathService / WorkspaceService。
10. 修复已有错误数据：启动时或 Reconcile 时扫描 wiki_pages 表，如果 path 指向文件不存在，尝试根据 title 和 versions/drafts/update_plan 重建页面；不能重建则标记为 broken，并在 UI 中提示“页面文件缺失”。

验收：
点击 Wiki 页面列表中的任意页面，不再出现 os error 3；页面能正常显示 Markdown 内容。

------------------------------------------------------------

Bug 2：审阅接受后没有真正创建 Wiki 页面

现象：
审阅中心有多个 update 项，但 Wiki 页面只显示少量页面，且页面无法读取。

修复要求：

1. Review Item 点击接受后必须调用 WikiWriter。
2. WikiWriter 必须根据 update_plan 生成或更新 Markdown 页面。
3. 写入成功后必须：
   - 更新 wiki_pages 表；
   - 更新 knowledge_items 表；
   - 更新 aliases 表；
   - 更新 relationships 表；
   - 更新 graph_nodes / graph_edges；
   - 更新 index.md；
   - 追加 log.md；
   - 生成 versions 快照；
   - 更新 review_item.status = accepted / applied；
   - 更新 task 状态；
   - 刷新前端列表。
4. 接受低风险按钮必须批量执行低风险项，并显示执行结果。
5. 全部拒绝必须更新状态，不得写入 Wiki。
6. 单项拒绝必须保留任务记录。
7. 如果写入失败，必须显示具体错误和目标路径。

验收：
在审阅界面接受一个 update 项后，Wiki 页面列表立即出现该页面，点击可正常阅读。

------------------------------------------------------------

Bug 3：统计数据不一致

现象：
首页显示待审阅 13，但审阅页显示共 15 项。

修复要求：

1. 统一 Dashboard 统计来源。
2. 待审阅数量应来自 review_items 表中 status = pending 的数量，或 reviews 中 pending item 汇总。
3. Wiki 页面数量来自 wiki_pages 表中存在且文件可读的页面数量。
4. Source 文件数量来自 sources 表。
5. 知识关系数量来自 relationships 或 graph_edges。
6. 如果存在 broken page，应在 Dashboard 显示健康警告。

验收：
首页统计与对应页面列表数量一致。

------------------------------------------------------------

Bug 4：知识图谱无数据

现象：
任务中已有 relationship_result.json，但知识图谱页面显示“暂无图谱数据”。

修复要求：

1. RelationshipAgent 输出的 relationships 必须写入 relationships 表。
2. 同时生成 graph_nodes 和 graph_edges。
3. 如果关系 source/target 对应页面不存在，应先创建 knowledge_items 或进入 pending 状态。
4. Graph 页面从 graph_nodes / graph_edges 读取数据。
5. Graph 页面应支持空状态说明：
   - 如果没有关系：提示用户先导入文档或接受审阅；
   - 如果有 relationship_result 但未入库：提示“发现未同步关系，可点击同步”。
6. 添加“重建图谱索引”按钮，从 wiki_pages、knowledge_items、relationships 重新生成 graph_nodes / graph_edges。

验收：
导入并接受审阅后，知识图谱页面至少显示 source、concept/entity/wiki_page 节点及关系边。

============================================================
三、补齐本地知识库管理系统
============================================================

当前首页只有“我的知识库”概览，不足以支撑多个知识库管理。需要新增完整知识库管理逻辑。

功能要求：

1. 支持创建多个知识库。
2. 支持切换当前知识库。
3. 支持不同类型知识库模板。
4. 支持打开已有本地知识库路径。
5. 支持删除知识库记录，但默认不删除本地文件。
6. 支持显示每个知识库的：
   - 名称；
   - 类型模板；
   - 本地路径；
   - Wiki 页面数；
   - Source 数；
   - 待审阅数；
   - 最近更新时间；
   - 健康状态。
7. 当前知识库必须全局生效：
   - Dashboard；
   - Sources；
   - Wiki；
   - Review；
   - Graph；
   - Chat；
   - Search；
   - Tasks；
   - Settings。
8. 切换知识库后，所有页面自动刷新数据。
9. 当前知识库路径显示在底部状态栏或 Dashboard。

首页新增区域：

1. 当前知识库卡片。
2. 知识库切换下拉框。
3. 新建知识库按钮。
4. 打开本地知识库按钮。
5. 知识库模板卡片。

新建知识库流程：

Step 1：输入名称。
Step 2：选择本地路径。
Step 3：选择模板：
   - 通用知识库；
   - 科研论文知识库；
   - 课程学习知识库；
   - 项目管理知识库；
   - 读书笔记知识库。
Step 4：初始化目录结构。
Step 5：创建 index.md、log.md、overview.md、schema/TEMPLATE.yaml。
Step 6：写入 knowledge_bases 表。
Step 7：切换到新知识库。

模板要影响：
1. wiki 子目录；
2. 页面类型；
3. Prompt 模板；
4. frontmatter 字段；
5. graph node/edge 类型；
6. 审阅规则。

验收：
用户可以创建两个不同模板知识库，并在首页切换，切换后各页面数据隔离。

============================================================
四、补齐原始文件管理页面和逻辑
============================================================

当前缺少真正的 Source 管理系统。需要新增或完善 Sources 页面。

Sources 页面应包括：

1. Source 文件列表。
2. 文件上传按钮。
3. 批量上传按钮。
4. 文件状态：
   - saved；
   - ingesting；
   - analyzed；
   - review_pending；
   - applied；
   - failed；
   - asset_only。
5. 文件类型：
   - document；
   - markdown；
   - html；
   - image_asset；
   - unknown。
6. 文件信息：
   - file_name；
   - file_path；
   - file_size；
   - file_hash；
   - source_id；
   - created_at；
   - updated_at；
   - related_pages_count；
   - task_id；
   - analysis_status。
7. 操作按钮：
   - 打开原始文件；
   - 查看 Markdown 预览；
   - 查看分析结果；
   - 重新分析；
   - 查看关联 Wiki 页面；
   - 删除记录；
   - 从知识库移除；
   - 复制 source_id；
   - 打开所在文件夹。

文件处理规则：

1. PDF/DOCX/MD/TXT/HTML 保存到 raw/sources/documents/。
2. PNG/JPG/JPEG/WEBP/GIF 保存到 raw/assets/images/。
3. 图片不触发 AI 分析，状态为 asset_only。
4. 文档可以创建 SourceIngestTask。
5. 相同 hash 文件提示已存在：
   - 跳过；
   - 重新分析；
   - 作为新版本导入。
6. 删除 source 时不得默认删除原始文件，除非用户明确选择。

验收：
用户可以在 Sources 页面看到所有上传文件、状态、分析任务和关联页面。

============================================================
五、实现文件统一 Markdown 预览界面
============================================================

当前系统缺少“不同文件统一转成 Markdown 并预览”的界面。注意：这里不是本地 PDF 深度解析，也不是 OCR，而是用于 UI 预览和 source 管理的轻量文本/Markdown 表示。

功能目标：
给用户一个统一的 Source Preview 页面，让用户可以查看上传文件在知识库中的可读表示。

允许实现的轻量转换：

1. Markdown 文件：直接读取显示。
2. TXT 文件：包装成 Markdown 显示。
3. HTML 文件：使用轻量 HTML-to-Markdown 转换，提取正文即可，不做复杂语义解析。
4. DOCX 文件：如果已有轻量库可用，可提取普通文本转 Markdown；如果实现复杂，先显示“暂不支持本地预览，可由模型附件分析”。
5. PDF 文件：不做本地深度解析；可以显示文件元信息、模型返回的 source_summary、coverage_report 和 AI 摘要作为 Markdown Preview。
6. 图片：显示资产信息和缩略图，不做 OCR、不做图片理解。

重要边界：
这个 Source Preview 只是给用户预览，不作为知识抽取主流程，不做 OCR，不替代 DeepSeek 附件分析。

Source Preview 页面应显示：

1. 左侧：原始文件信息。
2. 中间：Markdown 预览。
3. 右侧：AI 分析结果。
4. Tabs：
   - 原始信息；
   - Markdown 预览；
   - 模型摘要；
   - 抽取实体；
   - 抽取概念；
   - 关系；
   - 关联 Wiki 页面；
   - 任务日志。

缓存逻辑：

1. 转换结果保存到：
   .runtime/source_previews/{source_id}.md
2. 记录 preview_status：
   - generated；
   - unavailable；
   - failed；
   - ai_summary_only。
3. PDF 默认 preview_status = ai_summary_only。
4. 图片默认 preview_status = asset_only。

验收：
Sources 页面点击“查看 Markdown 预览”，可以打开统一 Source Preview 页面；PDF 至少显示 AI 摘要和元信息，不报错。

============================================================
六、完善审阅中心
============================================================

当前审阅界面只显示 medium、update、文件名、查看 Diff，信息不足。需要重做审阅项展示逻辑。

Review 页面每个任务顶部应显示：

1. task_id。
2. source 文件名。
3. 当前风险等级。
4. 共多少项。
5. 低/中/高风险数量。
6. 待审阅/已接受/已拒绝数量。
7. 模型覆盖报告。
8. 入口按钮：
   - 接受低风险；
   - 全部拒绝；
   - 重新生成；
   - 查看任务详情；
   - 打开 Source Preview。

每个 Review Item 应展示：

1. 操作类型：create/update/append/add_alias/add_relation/merge_suggestion。
2. 目标页面标题。
3. 目标路径。
4. 页面类型。
5. 风险等级。
6. 修改摘要。
7. 修改原因。
8. 依据来源 source_id / location / citation_status。
9. confidence。
10. 是否需要用户审阅。
11. Diff 折叠面板。
12. 操作：
   - 接受；
   - 拒绝；
   - 编辑后接受；
   - 查看源文件；
   - 查看模型原始输出；
   - 发送到 Chat 解释。

Diff 内容要求：

1. create：显示将要创建的完整 Markdown。
2. update：显示旧内容和新内容对比。
3. append：显示追加到哪个 section。
4. add_alias：显示 alias 添加前后。
5. add_relation：显示关系 source → relation → target 和 evidence。
6. merge_suggestion：必须高风险，不能自动应用。

审阅接受后：

1. 调用 WikiWriter。
2. 更新状态。
3. 刷新 Wiki 页面列表。
4. 刷新图谱。
5. 刷新 Dashboard。
6. 给出成功/失败提示。

验收：
用户能通过审阅中心清楚知道 AI 要改什么、为什么改、基于什么来源、接受后会生成什么页面。

============================================================
七、完善 Wiki 页面系统
============================================================

当前 Wiki 页面列表和阅读区功能不足，且路径错误。需要完善。

Wiki 页面列表：

1. 按 page_type 分组：
   - source；
   - concept；
   - entity；
   - topic；
   - question；
   - review。
2. 支持搜索标题。
3. 支持显示：
   - title；
   - page_type；
   - relative_path；
   - tags；
   - updated_at；
   - broken 状态。
4. Broken 页面要显示红色提示，并提供“尝试修复”。

Wiki 阅读区：

1. 正常显示 Markdown 渲染。
2. 支持源码模式。
3. 支持编辑模式。
4. 支持保存。
5. 保存前创建版本快照。
6. 保存后更新 content_hash。
7. 支持右侧元信息面板：
   - frontmatter；
   - sources；
   - aliases；
   - relationships；
   - backlinks；
   - versions。
8. 如果页面文件不存在，提供：
   - 重新生成；
   - 从 review item 创建；
   - 标记为 broken；
   - 删除记录。

新建页面：

1. 用户可手动新建页面。
2. 选择类型。
3. 生成规范路径。
4. 使用模板创建 frontmatter 和 section。
5. 更新 wiki_pages。

验收：
Wiki 页面系统可正常增删改查，不再只是列表壳子。

============================================================
八、完善知识图谱页面
============================================================

当前图谱页面为空，需要接入真实数据。

图谱数据来源：

1. graph_nodes 表。
2. graph_edges 表。
3. relationships 表。
4. wiki_pages 表。
5. knowledge_items 表。

图谱页面至少实现：

1. 节点类型筛选：
   - source；
   - concept；
   - entity；
   - topic；
   - wiki_page；
   - question。
2. 置信度筛选：
   - high；
   - medium；
   - low。
3. 布局选择：
   - 力导向；
   - 层级；
   - 当前节点邻域。
4. 搜索节点。
5. 点击节点显示详情。
6. 点击边显示关系详情。
7. 刷新/重建图谱按钮。
8. 空状态说明和修复入口。

右侧详情面板：

选中节点显示：
1. 标题。
2. 类型。
3. 路径。
4. 摘要。
5. aliases。
6. source_count。
7. 入边/出边数量。
8. 相关页面。
9. 操作：打开页面、查看 Source、在 Chat 中询问。

选中边显示：
1. source。
2. target。
3. relation。
4. confidence。
5. evidence。
6. citation_status。
7. created_by_task。
8. 操作：查看证据、删除关系、提交审阅。

验收：
接受审阅生成页面和关系后，图谱页面能显示节点和边。

============================================================
九、任务详情页要从“文件列表”升级为“可解释任务”
============================================================

当前任务中间文件展示是好的，但需要进一步整合为 TaskDetail 页面。

TaskDetail 应包含：

1. 基本信息：
   - task_id；
   - task_type；
   - status；
   - source；
   - created_at；
   - updated_at；
   - current_agent；
   - model；
   - token usage。
2. Agent Activity Timeline。
3. 中间文件列表：
   - prompt_0.md；
   - model_raw_response_0.txt；
   - ingest_result.json；
   - resolution_result.json；
   - relationship_result.json；
   - update_plan.json。
4. 每个文件点击后弹窗或侧栏预览。
5. 结构化摘要：
   - 抽取了多少 entities；
   - 多少 concepts；
   - 多少 relationships；
   - 多少 update items；
   - 多少 conflicts。
6. 错误与重试：
   - retry；
   - cancel；
   - regenerate review；
   - repair JSON。
7. 跳转：
   - 打开 Source；
   - 打开 Review；
   - 打开生成的 Wiki 页面；
   - 打开 Graph。

验收：
任务详情能帮助开发者和用户理解整个 Agent Pipeline，而不是只看到文件名。

============================================================
十、首页 Dashboard 改造
============================================================

当前首页仅显示统计和快捷操作，需要升级为知识库管理入口。

首页结构建议：

1. 顶部：当前知识库选择器。
2. 当前知识库卡片：
   - 名称；
   - 模板类型；
   - 路径；
   - 健康状态；
   - 最近更新时间。
3. 统计卡片：
   - Wiki 页面；
   - Source 文件；
   - 待审阅；
   - 知识关系；
   - Broken 页面；
   - 失败任务。
4. 快捷操作：
   - 上传文档；
   - 新建页面；
   - 开始问答；
   - 健康检查；
   - 创建知识库；
   - 打开知识库。
5. 最近任务。
6. 最近审阅。
7. 最近更新页面。
8. 知识库列表：
   - 可切换；
   - 可创建；
   - 可打开；
   - 可删除记录。

验收：
首页不只是一个统计页，而是完整知识库管理入口。

============================================================
十一、必须保持的产品边界
============================================================

本次 v0.1.2 不要实现：

1. OCR。
2. 多模态图片理解。
3. 图片 caption。
4. 本地 PDF 深度解析。
5. PDF 页面切片。
6. 表格视觉识别。
7. 公式 OCR。
8. 自由 Agent 群聊。
9. 云同步。
10. Web 端。
11. 移动端。
12. 多人协作。

注意：
“统一 Markdown 预览”只是预览层和 source 管理层，不是知识抽取主流程，不做 PDF 深度解析，不做 OCR。

============================================================
十二、开发顺序建议
============================================================

请按以下顺序实现：

1. 修复 PathService / Wiki 页面路径规范。
2. 修复 WikiWriter 创建页面和读取页面。
3. 修复 Review 接受后写入 Markdown。
4. 修复 Dashboard 统计。
5. 实现 KnowledgeBaseManager：创建/切换/打开知识库。
6. 改造首页 Dashboard。
7. 实现 Sources 原始文件管理页面。
8. 实现 Source Preview 统一 Markdown 预览。
9. 完善 Review 页面信息和 Diff。
10. 实现 graph_nodes / graph_edges 写入。
11. 修复知识图谱页面读取和展示。
12. 完善 TaskDetail 页面。
13. 实现 Workspace Reconcile 修复 broken pages。
14. 测试完整流程：
    上传文档 → Agent 分析 → 审阅 → 接受 → 创建 Wiki → 打开页面 → 生成图谱 → 问答。

============================================================
十三、完整验收流程
============================================================

请实现后按以下流程自测：

1. 启动软件。
2. 创建一个“科研论文知识库”。
3. 上传一个 PDF 或 DOCX。
4. 系统创建 source 记录。
5. 系统生成任务。
6. Agent Timeline 正常显示。
7. 中间文件正常保存。
8. Review 页面显示完整审阅项。
9. 接受一个 update/create 项。
10. WikiWriter 创建 Markdown 文件。
11. Wiki 页面列表显示新页面。
12. 点击 Wiki 页面能正常打开，无 os error 3。
13. index.md 更新。
14. log.md 更新。
15. graph_nodes / graph_edges 有数据。
16. 知识图谱页面显示节点和边。
17. Sources 页面能看到原始文件。
18. Source Preview 能显示元信息和 Markdown/AI 摘要。
19. Dashboard 统计正确。
20. 切换知识库后数据隔离。
21. 图片上传只保存为 asset，不触发 AI 分析。
22. 关闭重启后任务和页面仍可读取。

============================================================
十四、输出要求
============================================================

请基于现有代码修改，不要推倒重写。

实现后请输出：

1. 修改了哪些文件。
2. 修复了哪些 bug。
3. 新增了哪些功能。
4. 数据库 migration 内容。
5. 需要手动测试的流程。
6. 当前还未完成的风险点。
7. 如何运行和打包。

请优先保证功能闭环真实可用，而不是只在 UI 上显示假数据。